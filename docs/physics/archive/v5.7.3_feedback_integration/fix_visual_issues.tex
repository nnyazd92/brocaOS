% ============================================================================
% FIX VISUAL ISSUES FOR v5.7
% ============================================================================
% Fixes for:
% 1. Overfull/underfull boxes
% 2. Broken references
% 3. Missing packages
% ============================================================================

% Add missing packages for better formatting
\usepackage{ragged2e}  % Better text justification
\usepackage{microtype} % Micro-typography improvements
\usepackage{parskip}   % Better paragraph spacing

% Fix overfull boxes by allowing more flexible line breaking
\emergencystretch=3em
\tolerance=1000
\hbadness=10000

% Fix the theorem dependency reference
% The label exists but might need multiple compilations
% We'll ensure it's properly placed

% Fix table formatting issues
\usepackage{tabularx}  % Better table control
\usepackage{booktabs}  % Professional tables
\usepackage{multirow}  % Multi-row cells

% Fix code listing formatting
\usepackage{listings}
\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny,
    stepnumber=1,
    numbersep=5pt
}

% Ensure all newcommands are properly defined
% (They should already be in the preamble from patch 01)

% Add this to help with reference resolution
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage{cleveref}  % Better reference formatting

% Fix potential issues with tcolorbox
\usepackage{tcolorbox}
\tcbuselibrary{breakable}  % Allow tcolorboxes to break across pages
\tcbuselibrary{skins}      % Additional skins

% Ensure the curvature fork section has proper formatting
\newcommand{\curvatureforkwarning}{%
\begin{tcolorbox}[
    colback=red!5!white,
    colframe=red!75!black,
    title=Critical Notation Rule,
    breakable,
    enhanced,
    fonttitle=\bfseries
]
\[
\Cphys \neq \Cinfo \neq \Ctopos
\]
These objects inhabit different mathematical categories:
\begin{itemize}
    \item $\Cphys$: Riemannian geometry (smooth manifolds)
    \item $\Cinfo$: Information geometry (statistical manifolds) 
    \item $\Ctopos$: Topos theory (Heyting algebras)
\end{itemize}
\textbf{Any map between them must be explicitly defined as a functor with stated properties.}
\end{tcolorbox}
}
