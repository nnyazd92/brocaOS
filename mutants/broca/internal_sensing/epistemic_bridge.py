"""
Epistemic bridge for integrating epistemic self-modeling with internal sensing.

Provides unified interface between MetacognitiveEngine and InternalSensingFramework,
enabling bidirectional flow of uncertainty, confidence, and source reliability metrics.
"""

from __future__ import annotations

import logging
from typing import Dict, Any, List, Optional, Tuple
from collections import defaultdict

from .data_quality import (
    DataQuality,
    uncertainty_for_missing_data,
    confidence_for_missing_data,
    assess_data_quality,
    create_metric_with_quality,
    propagate_uncertainty,
)

logger = logging.getLogger(__name__)
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class EpistemicBridge:
    """
    Bridge between epistemic self-modeling and internal sensing.
    
    Aggregates epistemic metrics (uncertainty, confidence, source reliability)
    into formats usable by internal sensing, and feeds internal sensing insights
    back to the epistemic engine.
    """
    
    def xǁEpistemicBridgeǁ__init____mutmut_orig(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("Initialized EpistemicBridge" + (" with epistemic engine" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_1(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = None
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("Initialized EpistemicBridge" + (" with epistemic engine" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_2(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = None
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("Initialized EpistemicBridge" + (" with epistemic engine" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_3(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = ""
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("Initialized EpistemicBridge" + (" with epistemic engine" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_4(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = ""
        
        logger.info("Initialized EpistemicBridge" + (" with epistemic engine" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_5(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info(None)
    
    def xǁEpistemicBridgeǁ__init____mutmut_6(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("Initialized EpistemicBridge" - (" with epistemic engine" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_7(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("XXInitialized EpistemicBridgeXX" + (" with epistemic engine" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_8(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("initialized epistemicbridge" + (" with epistemic engine" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_9(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("INITIALIZED EPISTEMICBRIDGE" + (" with epistemic engine" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_10(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("Initialized EpistemicBridge" + ("XX with epistemic engineXX" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_11(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("Initialized EpistemicBridge" + (" WITH EPISTEMIC ENGINE" if epistemic_engine else " (no epistemic engine)"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_12(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("Initialized EpistemicBridge" + (" with epistemic engine" if epistemic_engine else "XX (no epistemic engine)XX"))
    
    def xǁEpistemicBridgeǁ__init____mutmut_13(self, epistemic_engine: Optional[Any] = None) -> None:
        """
        Initialize epistemic bridge.
        
        Args:
            epistemic_engine: Optional MetacognitiveEngine instance
        """
        self.epistemic_engine = epistemic_engine
        self._recent_knowledge_items: Dict[str, Dict[str, Any]] = {}
        self._aggregated_uncertainty_cache: Optional[Dict[str, float]] = None
        self._aggregated_confidence_cache: Optional[float] = None
        
        logger.info("Initialized EpistemicBridge" + (" with epistemic engine" if epistemic_engine else " (NO EPISTEMIC ENGINE)"))
    
    xǁEpistemicBridgeǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁ__init____mutmut_1': xǁEpistemicBridgeǁ__init____mutmut_1, 
        'xǁEpistemicBridgeǁ__init____mutmut_2': xǁEpistemicBridgeǁ__init____mutmut_2, 
        'xǁEpistemicBridgeǁ__init____mutmut_3': xǁEpistemicBridgeǁ__init____mutmut_3, 
        'xǁEpistemicBridgeǁ__init____mutmut_4': xǁEpistemicBridgeǁ__init____mutmut_4, 
        'xǁEpistemicBridgeǁ__init____mutmut_5': xǁEpistemicBridgeǁ__init____mutmut_5, 
        'xǁEpistemicBridgeǁ__init____mutmut_6': xǁEpistemicBridgeǁ__init____mutmut_6, 
        'xǁEpistemicBridgeǁ__init____mutmut_7': xǁEpistemicBridgeǁ__init____mutmut_7, 
        'xǁEpistemicBridgeǁ__init____mutmut_8': xǁEpistemicBridgeǁ__init____mutmut_8, 
        'xǁEpistemicBridgeǁ__init____mutmut_9': xǁEpistemicBridgeǁ__init____mutmut_9, 
        'xǁEpistemicBridgeǁ__init____mutmut_10': xǁEpistemicBridgeǁ__init____mutmut_10, 
        'xǁEpistemicBridgeǁ__init____mutmut_11': xǁEpistemicBridgeǁ__init____mutmut_11, 
        'xǁEpistemicBridgeǁ__init____mutmut_12': xǁEpistemicBridgeǁ__init____mutmut_12, 
        'xǁEpistemicBridgeǁ__init____mutmut_13': xǁEpistemicBridgeǁ__init____mutmut_13
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁ__init____mutmut_orig)
    xǁEpistemicBridgeǁ__init____mutmut_orig.__name__ = 'xǁEpistemicBridgeǁ__init__'
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_orig(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_1(self, 
                                   recent_knowledge_count: int = 21) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_2(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_3(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = None
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_4(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = None
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_5(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(None)
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_6(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_7(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = None
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_8(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[+recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_9(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = None
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_10(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "XXepistemicXX": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_11(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "EPISTEMIC": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_12(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "XXaleatoricXX": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_13(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "ALEATORIC": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_14(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "XXmodelXX": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_15(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "MODEL": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_16(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "XXtotalXX": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_17(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "TOTAL": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_18(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = None
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_19(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "XXknowledge_gapsXX": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_20(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "KNOWLEDGE_GAPS": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_21(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "XXambiguityXX": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_22(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "AMBIGUITY": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_23(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "XXnoiseXX": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_24(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "NOISE": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_25(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = None
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_26(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(None)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_27(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = None
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_28(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get(None, {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_29(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", None)
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_30(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get({})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_31(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", )
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_32(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("XXuncertaintyXX", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_33(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("UNCERTAINTY", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_34(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(None)
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_35(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["XXepistemicXX"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_36(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["EPISTEMIC"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_37(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get(None, 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_38(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", None))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_39(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get(0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_40(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", ))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_41(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("XXepistemicXX", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_42(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("EPISTEMIC", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_43(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 1.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_44(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(None)
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_45(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["XXaleatoricXX"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_46(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["ALEATORIC"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_47(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get(None, 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_48(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", None))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_49(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get(0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_50(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", ))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_51(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("XXaleatoricXX", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_52(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("ALEATORIC", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_53(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 1.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_54(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(None)
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_55(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["XXmodelXX"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_56(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["MODEL"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_57(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get(None, 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_58(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", None))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_59(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get(0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_60(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", ))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_61(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("XXmodelXX", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_62(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("MODEL", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_63(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 1.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_64(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(None)
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_65(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["XXtotalXX"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_66(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["TOTAL"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_67(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get(None, 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_68(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", None))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_69(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get(0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_70(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", ))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_71(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("XXtotalXX", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_72(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("TOTAL", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_73(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 1.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_74(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = None
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_75(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get(None)
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_76(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("XXconfidence_metricsXX")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_77(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("CONFIDENCE_METRICS")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_78(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = None
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_79(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get(None, 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_80(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", None)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_81(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get(0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_82(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", )
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_83(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("XXoverall_confidenceXX", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_84(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("OVERALL_CONFIDENCE", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_85(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 1.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_86(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = None
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_87(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) - 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_88(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get(None, 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_89(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", None) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_90(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get(0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_91(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", ) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_92(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("XXverification_countXX", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_93(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("VERIFICATION_COUNT", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_94(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 1) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_95(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 2
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_96(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = None
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_97(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) / (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_98(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 + overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_99(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (2.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_100(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 * max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_101(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (2.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_102(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(None, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_103(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, None))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_104(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_105(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, ))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_106(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 2))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_107(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(None)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_108(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["XXknowledge_gapsXX"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_109(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["KNOWLEDGE_GAPS"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_110(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = None
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_111(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get(None, {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_112(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", None)
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_113(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get({})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_114(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", )
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_115(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("XXcross_validationXX", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_116(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("CROSS_VALIDATION", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_117(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = None
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_118(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get(None, 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_119(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", None)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_120(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get(0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_121(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", )
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_122(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("XXcontradictory_evidence_countXX", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_123(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("CONTRADICTORY_EVIDENCE_COUNT", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_124(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 1)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_125(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = None
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_126(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(None, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_127(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, None)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_128(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_129(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, )
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_130(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(2.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_131(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions / 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_132(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 1.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_133(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(None)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_134(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["XXambiguityXX"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_135(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["AMBIGUITY"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_136(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = None
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_137(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get(None, 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_138(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", None)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_139(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get(0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_140(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", )
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_141(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("XXconfidence_calibration_errorXX", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_142(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("CONFIDENCE_CALIBRATION_ERROR", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_143(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 1.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_144(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = None
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_145(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(None, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_146(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, None)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_147(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_148(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, )
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_149(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(2.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_150(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error / 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_151(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 3.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_152(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(None)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_153(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["XXnoiseXX"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_154(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["NOISE"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_155(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(None)
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_156(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    break
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_157(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = None
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_158(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = None
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_159(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = None
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_160(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) * len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_161(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(None) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_162(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = None
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_163(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = None
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_164(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) * len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_165(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(None) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_166(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = None
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_167(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = None
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_168(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(None)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_169(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = None
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_170(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["XXdata_qualityXX"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_171(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["DATA_QUALITY"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_172(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = None
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_173(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["XXsample_sizeXX"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_174(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["SAMPLE_SIZE"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_175(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = None
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_176(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["XXhas_dataXX"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_177(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["HAS_DATA"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_178(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples >= 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_179(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 1
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_180(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = None
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_181(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(None, exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_182(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=None)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_183(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(exc_info=True)
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_184(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", )
            return self._get_default_uncertainty()
    
    def xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_185(self, 
                                   recent_knowledge_count: int = 20) -> Dict[str, float]:
        """
        Get aggregated uncertainty metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated uncertainty breakdown:
            - epistemic: Average epistemic uncertainty
            - aleatoric: Average aleatoric uncertainty
            - model: Average model uncertainty
            - total: Average total uncertainty
            - knowledge_gaps: Uncertainty from knowledge gaps
            - ambiguity: Uncertainty from ambiguity
            - noise: Uncertainty from noise
        """
        if not self.epistemic_engine:
            # Missing epistemic engine should return explicit "missing" uncertainty
            # rather than zeros (zeros look like "perfect certainty" and pollute downstream).
            return self._get_default_uncertainty()
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_uncertainty()
            
            # Get recent knowledge items (limit to recent_knowledge_count)
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            uncertainties = {
                "epistemic": [],
                "aleatoric": [],
                "model": [],
                "total": [],
            }
            
            source_breakdown = {
                "knowledge_gaps": [],
                "ambiguity": [],
                "noise": [],
            }
            
            for knowledge_id in recent_ids:
                try:
                    # Get epistemic context
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    uncertainty = context.get("uncertainty", {})
                    
                    if uncertainty:
                        uncertainties["epistemic"].append(uncertainty.get("epistemic", 0.0))
                        uncertainties["aleatoric"].append(uncertainty.get("aleatoric", 0.0))
                        uncertainties["model"].append(uncertainty.get("model", 0.0))
                        uncertainties["total"].append(uncertainty.get("total", 0.0))
                    
                    # Estimate uncertainty sources from confidence metrics
                    metrics = context.get("confidence_metrics")
                    if metrics:
                        overall_conf = metrics.get("overall_confidence", 0.5)
                        # Knowledge gaps: inversely related to confidence and source count
                        source_count = context.get("verification_count", 0) + 1
                        knowledge_gaps = (1.0 - overall_conf) * (1.0 / max(source_count, 1))
                        source_breakdown["knowledge_gaps"].append(knowledge_gaps)
                        
                        # Ambiguity: related to contradictory evidence
                        cross_val = metrics.get("cross_validation", {})
                        contradictions = cross_val.get("contradictory_evidence_count", 0)
                        ambiguity = min(1.0, contradictions * 0.2)
                        source_breakdown["ambiguity"].append(ambiguity)
                        
                        # Noise: related to calibration error
                        calibration_error = metrics.get("confidence_calibration_error", 0.0)
                        noise = min(1.0, calibration_error * 2.0)
                        source_breakdown["noise"].append(noise)
                        
                except Exception as e:
                    logger.debug(f"Error getting uncertainty for {knowledge_id}: {e}")
                    continue
            
            # Calculate averages with data quality tracking
            aggregated = {}
            total_samples = len(recent_ids)
            
            for key, values in uncertainties.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            for key, values in source_breakdown.items():
                if values:
                    aggregated[key] = sum(values) / len(values)
                else:
                    # Missing data: use high uncertainty
                    aggregated[key] = uncertainty_for_missing_data()
            
            # Add data quality metadata
            data_quality = assess_data_quality(total_samples)
            aggregated["data_quality"] = data_quality.value
            aggregated["sample_size"] = total_samples
            aggregated["has_data"] = total_samples > 0
            
            # Cache result
            self._aggregated_uncertainty_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic uncertainty: {e}", exc_info=False)
            return self._get_default_uncertainty()
    
    xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_1': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_1, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_2': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_2, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_3': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_3, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_4': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_4, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_5': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_5, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_6': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_6, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_7': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_7, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_8': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_8, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_9': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_9, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_10': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_10, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_11': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_11, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_12': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_12, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_13': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_13, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_14': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_14, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_15': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_15, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_16': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_16, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_17': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_17, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_18': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_18, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_19': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_19, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_20': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_20, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_21': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_21, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_22': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_22, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_23': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_23, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_24': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_24, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_25': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_25, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_26': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_26, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_27': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_27, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_28': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_28, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_29': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_29, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_30': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_30, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_31': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_31, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_32': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_32, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_33': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_33, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_34': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_34, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_35': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_35, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_36': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_36, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_37': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_37, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_38': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_38, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_39': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_39, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_40': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_40, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_41': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_41, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_42': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_42, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_43': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_43, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_44': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_44, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_45': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_45, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_46': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_46, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_47': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_47, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_48': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_48, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_49': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_49, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_50': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_50, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_51': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_51, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_52': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_52, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_53': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_53, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_54': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_54, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_55': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_55, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_56': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_56, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_57': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_57, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_58': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_58, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_59': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_59, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_60': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_60, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_61': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_61, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_62': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_62, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_63': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_63, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_64': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_64, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_65': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_65, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_66': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_66, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_67': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_67, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_68': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_68, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_69': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_69, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_70': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_70, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_71': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_71, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_72': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_72, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_73': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_73, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_74': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_74, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_75': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_75, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_76': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_76, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_77': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_77, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_78': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_78, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_79': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_79, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_80': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_80, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_81': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_81, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_82': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_82, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_83': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_83, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_84': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_84, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_85': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_85, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_86': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_86, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_87': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_87, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_88': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_88, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_89': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_89, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_90': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_90, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_91': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_91, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_92': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_92, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_93': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_93, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_94': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_94, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_95': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_95, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_96': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_96, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_97': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_97, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_98': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_98, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_99': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_99, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_100': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_100, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_101': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_101, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_102': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_102, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_103': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_103, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_104': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_104, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_105': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_105, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_106': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_106, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_107': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_107, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_108': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_108, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_109': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_109, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_110': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_110, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_111': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_111, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_112': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_112, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_113': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_113, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_114': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_114, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_115': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_115, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_116': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_116, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_117': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_117, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_118': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_118, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_119': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_119, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_120': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_120, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_121': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_121, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_122': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_122, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_123': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_123, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_124': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_124, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_125': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_125, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_126': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_126, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_127': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_127, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_128': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_128, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_129': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_129, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_130': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_130, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_131': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_131, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_132': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_132, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_133': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_133, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_134': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_134, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_135': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_135, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_136': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_136, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_137': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_137, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_138': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_138, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_139': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_139, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_140': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_140, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_141': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_141, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_142': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_142, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_143': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_143, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_144': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_144, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_145': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_145, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_146': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_146, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_147': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_147, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_148': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_148, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_149': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_149, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_150': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_150, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_151': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_151, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_152': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_152, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_153': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_153, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_154': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_154, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_155': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_155, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_156': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_156, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_157': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_157, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_158': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_158, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_159': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_159, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_160': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_160, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_161': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_161, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_162': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_162, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_163': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_163, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_164': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_164, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_165': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_165, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_166': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_166, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_167': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_167, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_168': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_168, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_169': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_169, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_170': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_170, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_171': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_171, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_172': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_172, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_173': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_173, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_174': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_174, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_175': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_175, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_176': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_176, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_177': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_177, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_178': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_178, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_179': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_179, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_180': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_180, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_181': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_181, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_182': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_182, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_183': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_183, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_184': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_184, 
        'xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_185': xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_185
    }
    
    def get_aggregated_uncertainty(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_aggregated_uncertainty.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_orig)
    xǁEpistemicBridgeǁget_aggregated_uncertainty__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁget_aggregated_uncertainty'
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_orig(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_1(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = None
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_2(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "XXepistemicXX": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_3(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "EPISTEMIC": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_4(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "XXaleatoricXX": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_5(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "ALEATORIC": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_6(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty / 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_7(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 1.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_8(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "XXmodelXX": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_9(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "MODEL": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_10(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty / 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_11(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 1.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_12(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "XXtotalXX": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_13(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "TOTAL": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_14(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "XXknowledge_gapsXX": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_15(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "KNOWLEDGE_GAPS": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_16(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "XXambiguityXX": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_17(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "AMBIGUITY": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_18(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty / 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_19(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 1.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_20(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "XXnoiseXX": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_21(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "NOISE": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_22(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty / 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_23(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 1.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_24(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "XXdata_qualityXX": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_25(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "DATA_QUALITY": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_26(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "XXsample_sizeXX": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_27(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "SAMPLE_SIZE": 0,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_28(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 1,
            "has_data": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_29(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "XXhas_dataXX": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_30(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "HAS_DATA": False,
        }
    
    def xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_31(self) -> Dict[str, Any]:
        """
        Return high uncertainty values when data is missing.
        
        Uses high uncertainty (0.9) to indicate missing data rather than
        neutral defaults that could be mistaken for real measurements.
        """
        missing_uncertainty = uncertainty_for_missing_data()
        return {
            "epistemic": missing_uncertainty,
            "aleatoric": missing_uncertainty * 0.3,  # Some aleatoric uncertainty even when missing
            "model": missing_uncertainty * 0.5,  # Model uncertainty when no data
            "total": missing_uncertainty,
            "knowledge_gaps": missing_uncertainty,
            "ambiguity": missing_uncertainty * 0.7,
            "noise": missing_uncertainty * 0.2,
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": True,
        }
    
    xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_1': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_1, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_2': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_2, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_3': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_3, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_4': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_4, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_5': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_5, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_6': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_6, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_7': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_7, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_8': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_8, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_9': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_9, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_10': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_10, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_11': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_11, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_12': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_12, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_13': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_13, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_14': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_14, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_15': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_15, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_16': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_16, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_17': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_17, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_18': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_18, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_19': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_19, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_20': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_20, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_21': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_21, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_22': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_22, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_23': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_23, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_24': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_24, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_25': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_25, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_26': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_26, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_27': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_27, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_28': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_28, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_29': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_29, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_30': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_30, 
        'xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_31': xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_31
    }
    
    def _get_default_uncertainty(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _get_default_uncertainty.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_orig)
    xǁEpistemicBridgeǁ_get_default_uncertainty__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁ_get_default_uncertainty'
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_orig(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_1(self, 
                                 recent_knowledge_count: int = 21) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_2(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_3(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = None
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_4(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "XXoverall_confidenceXX": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_5(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "OVERALL_CONFIDENCE": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_6(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "XXconfidence_intervalXX": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_7(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "CONFIDENCE_INTERVAL": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_8(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "XXcalibration_errorXX": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_9(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "CALIBRATION_ERROR": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_10(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "XXeceXX": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_11(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ECE": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_12(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "XXbrier_scoreXX": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_13(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "BRIER_SCORE": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_14(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "XXreliabilityXX": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_15(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "RELIABILITY": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_16(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "XXdata_qualityXX": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_17(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "DATA_QUALITY": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_18(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "XXsample_sizeXX": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_19(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "SAMPLE_SIZE": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_20(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 1,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_21(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "XXhas_dataXX": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_22(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "HAS_DATA": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_23(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": True,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_24(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "XXuncertaintyXX": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_25(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "UNCERTAINTY": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_26(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = None
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_27(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = None
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_28(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(None)
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_29(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_30(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = None
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_31(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[+recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_32(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = None
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_33(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = None
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_34(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = None
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_35(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(None)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_36(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = None
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_37(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get(None)
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_38(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("XXconfidence_metricsXX")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_39(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("CONFIDENCE_METRICS")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_40(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = None
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_41(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get(None)
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_42(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("XXoverall_confidenceXX")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_43(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("OVERALL_CONFIDENCE")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_44(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_45(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(None)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_46(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = None
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_47(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get(None)
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_48(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("XXconfidence_calibration_errorXX")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_49(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("CONFIDENCE_CALIBRATION_ERROR")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_50(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_51(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(None)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_52(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(None)
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_53(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    break
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_54(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = None
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_55(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = None
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_56(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(None)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_57(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size != 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_58(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 1:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_59(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = None
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_60(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) * len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_61(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(None) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_62(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = None
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_63(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) * 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_64(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) * len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_65(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum(None) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_66(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) * 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_67(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c + avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_68(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 3 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_69(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 1.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_70(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) >= 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_71(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 2 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_72(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 1.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_73(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = None  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_74(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 2.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_75(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = None
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_76(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev * (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_77(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score / std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_78(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) * 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_79(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 1.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_80(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) >= 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_81(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 2 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_82(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 1.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_83(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = None
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_84(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(None, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_85(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, None), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_86(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_87(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, ), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_88(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(1.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_89(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence + margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_90(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(None, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_91(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, None))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_92(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_93(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, ))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_94(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(2.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_95(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence - margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_96(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = None
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_97(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = None
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_98(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) * len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_99(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(None) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_100(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = ""
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_101(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = ""
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_102(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = ""
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_103(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(None, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_104(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, None):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_105(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr('calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_106(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, ):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_107(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'XXcalibratorXX'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_108(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'CALIBRATOR'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_109(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = None
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_110(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = None
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_111(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] + conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_112(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[2] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_113(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[1]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_114(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = None
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_115(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(None, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_116(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, None)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_117(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_118(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, )
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_119(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(2.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_120(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = None
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_121(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "XXoverall_confidenceXX": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_122(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "OVERALL_CONFIDENCE": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_123(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "XXconfidence_intervalXX": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_124(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "CONFIDENCE_INTERVAL": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_125(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "XXcalibration_errorXX": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_126(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "CALIBRATION_ERROR": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_127(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "XXeceXX": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_128(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ECE": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_129(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "XXbrier_scoreXX": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_130(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "BRIER_SCORE": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_131(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "XXreliabilityXX": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_132(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "RELIABILITY": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_133(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 + avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_134(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 2.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_135(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_136(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "XXdata_qualityXX": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_137(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "DATA_QUALITY": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_138(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "XXsample_sizeXX": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_139(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "SAMPLE_SIZE": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_140(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "XXhas_dataXX": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_141(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "HAS_DATA": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_142(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size >= 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_143(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 1,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_144(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "XXuncertaintyXX": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_145(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "UNCERTAINTY": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_146(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = None
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_147(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(None, exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_148(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=None)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_149(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(exc_info=True)
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_150(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", )
            return self._get_default_confidence()
    
    def xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_151(self, 
                                 recent_knowledge_count: int = 20) -> Dict[str, Any]:
        """
        Get aggregated confidence metrics from epistemic engine.
        
        Args:
            recent_knowledge_count: Number of recent knowledge items to aggregate
            
        Returns:
            Dictionary with aggregated confidence metrics:
            - overall_confidence: Average overall confidence
            - calibration_error: Average calibration error
            - ece: Expected Calibration Error (if available)
            - brier_score: Brier score (if available)
            - reliability: Overall reliability
        """
        if not self.epistemic_engine:
            # No epistemic engine: return high uncertainty with wide confidence interval
            conf_mean, conf_interval = confidence_for_missing_data()
            return {
                "overall_confidence": conf_mean,
                "confidence_interval": conf_interval,
                "calibration_error": None,  # Cannot compute without data
                "ece": None,
                "brier_score": None,
                "reliability": None,  # Cannot compute without data
                "data_quality": DataQuality.MISSING.value,
                "sample_size": 0,
                "has_data": False,
                "uncertainty": uncertainty_for_missing_data(),
            }
        
        try:
            # Get recent knowledge items from epistemic layer
            epistemic_layer = self.epistemic_engine.epistemic_layer
            
            # Get knowledge IDs from knowledge_sources dict (all tracked knowledge)
            knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
            if not knowledge_ids:
                return self._get_default_confidence()
            
            recent_ids = knowledge_ids[-recent_knowledge_count:]
            
            confidences = []
            calibration_errors = []
            
            for knowledge_id in recent_ids:
                try:
                    context = self.epistemic_engine.get_epistemic_context(knowledge_id)
                    metrics = context.get("confidence_metrics")
                    
                    if metrics:
                        conf = metrics.get("overall_confidence")
                        if conf is not None:
                            confidences.append(conf)
                        cal_err = metrics.get("confidence_calibration_error")
                        if cal_err is not None:
                            calibration_errors.append(cal_err)
                        
                except Exception as e:
                    logger.debug(f"Error getting confidence for {knowledge_id}: {e}")
                    continue
            
            sample_size = len(confidences)
            data_quality = assess_data_quality(sample_size)
            
            # Use Bayesian prior if insufficient data
            if sample_size == 0:
                return self._get_default_confidence()
            
            # Calculate averages with uncertainty propagation
            if confidences:
                avg_confidence = sum(confidences) / len(confidences)
                # Calculate confidence interval based on sample size
                from .data_quality import wilson_score_interval
                # Convert confidence to success rate for interval calculation
                # Use average as point estimate
                std_dev = (sum((c - avg_confidence) ** 2 for c in confidences) / len(confidences)) ** 0.5 if len(confidences) > 1 else 0.1
                # Approximate confidence interval
                z_score = 1.96  # 95% confidence
                margin = z_score * std_dev / (len(confidences) ** 0.5) if len(confidences) > 1 else 0.4
                conf_interval = (max(0.0, avg_confidence - margin), min(1.0, avg_confidence + margin))
            else:
                # Fallback to prior
                avg_confidence, conf_interval = confidence_for_missing_data()
            
            if calibration_errors:
                avg_calibration_error = sum(calibration_errors) / len(calibration_errors)
            else:
                avg_calibration_error = None
            
            # Get calibration metrics from calibrator if available
            ece = None
            brier_score = None
            if hasattr(self.epistemic_engine, 'calibrator'):
                calibrator = self.epistemic_engine.calibrator
                # Try to get calibration curve (which includes ECE calculation)
                # Note: This would need to be implemented in ConfidenceCalibrator
                # For now, use average calibration error as proxy
            
            # Calculate uncertainty from confidence interval width
            interval_width = conf_interval[1] - conf_interval[0]
            uncertainty = min(1.0, interval_width)
            
            aggregated = {
                "overall_confidence": avg_confidence,
                "confidence_interval": conf_interval,
                "calibration_error": avg_calibration_error,
                "ece": ece,
                "brier_score": brier_score,
                "reliability": 1.0 - avg_calibration_error if avg_calibration_error is not None else None,
                "data_quality": data_quality.value,
                "sample_size": sample_size,
                "has_data": sample_size > 0,
                "uncertainty": uncertainty,
            }
            
            # Cache result
            self._aggregated_confidence_cache = aggregated
            
            return aggregated
            
        except Exception as e:
            logger.warning(f"Error aggregating epistemic confidence: {e}", exc_info=False)
            return self._get_default_confidence()
    
    xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_1': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_1, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_2': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_2, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_3': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_3, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_4': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_4, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_5': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_5, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_6': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_6, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_7': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_7, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_8': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_8, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_9': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_9, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_10': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_10, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_11': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_11, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_12': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_12, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_13': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_13, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_14': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_14, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_15': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_15, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_16': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_16, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_17': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_17, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_18': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_18, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_19': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_19, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_20': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_20, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_21': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_21, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_22': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_22, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_23': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_23, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_24': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_24, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_25': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_25, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_26': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_26, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_27': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_27, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_28': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_28, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_29': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_29, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_30': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_30, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_31': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_31, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_32': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_32, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_33': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_33, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_34': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_34, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_35': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_35, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_36': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_36, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_37': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_37, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_38': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_38, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_39': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_39, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_40': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_40, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_41': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_41, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_42': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_42, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_43': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_43, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_44': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_44, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_45': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_45, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_46': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_46, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_47': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_47, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_48': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_48, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_49': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_49, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_50': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_50, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_51': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_51, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_52': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_52, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_53': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_53, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_54': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_54, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_55': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_55, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_56': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_56, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_57': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_57, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_58': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_58, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_59': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_59, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_60': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_60, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_61': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_61, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_62': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_62, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_63': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_63, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_64': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_64, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_65': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_65, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_66': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_66, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_67': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_67, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_68': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_68, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_69': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_69, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_70': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_70, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_71': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_71, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_72': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_72, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_73': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_73, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_74': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_74, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_75': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_75, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_76': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_76, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_77': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_77, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_78': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_78, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_79': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_79, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_80': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_80, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_81': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_81, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_82': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_82, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_83': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_83, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_84': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_84, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_85': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_85, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_86': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_86, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_87': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_87, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_88': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_88, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_89': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_89, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_90': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_90, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_91': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_91, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_92': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_92, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_93': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_93, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_94': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_94, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_95': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_95, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_96': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_96, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_97': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_97, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_98': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_98, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_99': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_99, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_100': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_100, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_101': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_101, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_102': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_102, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_103': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_103, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_104': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_104, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_105': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_105, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_106': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_106, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_107': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_107, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_108': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_108, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_109': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_109, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_110': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_110, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_111': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_111, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_112': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_112, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_113': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_113, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_114': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_114, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_115': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_115, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_116': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_116, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_117': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_117, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_118': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_118, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_119': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_119, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_120': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_120, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_121': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_121, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_122': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_122, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_123': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_123, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_124': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_124, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_125': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_125, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_126': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_126, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_127': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_127, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_128': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_128, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_129': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_129, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_130': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_130, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_131': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_131, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_132': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_132, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_133': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_133, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_134': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_134, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_135': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_135, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_136': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_136, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_137': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_137, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_138': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_138, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_139': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_139, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_140': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_140, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_141': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_141, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_142': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_142, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_143': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_143, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_144': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_144, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_145': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_145, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_146': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_146, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_147': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_147, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_148': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_148, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_149': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_149, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_150': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_150, 
        'xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_151': xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_151
    }
    
    def get_aggregated_confidence(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_aggregated_confidence.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_orig)
    xǁEpistemicBridgeǁget_aggregated_confidence__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁget_aggregated_confidence'
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_orig(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_1(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = None
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_2(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = None
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_3(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] + conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_4(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[2] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_5(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[1]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_6(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = None
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_7(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(None, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_8(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, None)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_9(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_10(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, )
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_11(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(2.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_12(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "XXoverall_confidenceXX": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_13(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "OVERALL_CONFIDENCE": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_14(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "XXconfidence_intervalXX": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_15(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "CONFIDENCE_INTERVAL": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_16(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "XXcalibration_errorXX": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_17(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "CALIBRATION_ERROR": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_18(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "XXeceXX": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_19(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ECE": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_20(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "XXbrier_scoreXX": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_21(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "BRIER_SCORE": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_22(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "XXreliabilityXX": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_23(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "RELIABILITY": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_24(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "XXdata_qualityXX": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_25(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "DATA_QUALITY": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_26(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "XXsample_sizeXX": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_27(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "SAMPLE_SIZE": 0,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_28(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 1,
            "has_data": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_29(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "XXhas_dataXX": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_30(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "HAS_DATA": False,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_31(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": True,
            "uncertainty": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_32(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "XXuncertaintyXX": uncertainty,
        }
    
    def xǁEpistemicBridgeǁ_get_default_confidence__mutmut_33(self) -> Dict[str, Any]:
        """
        Return high uncertainty confidence values when data is missing.
        
        Uses uniform prior (Beta(1,1)) which gives mean 0.5 with wide
        confidence interval to indicate missing data.
        """
        conf_mean, conf_interval = confidence_for_missing_data()
        interval_width = conf_interval[1] - conf_interval[0]
        uncertainty = min(1.0, interval_width)
        
        return {
            "overall_confidence": conf_mean,
            "confidence_interval": conf_interval,
            "calibration_error": None,  # Cannot compute without data
            "ece": None,
            "brier_score": None,
            "reliability": None,  # Cannot compute without data
            "data_quality": DataQuality.MISSING.value,
            "sample_size": 0,
            "has_data": False,
            "UNCERTAINTY": uncertainty,
        }
    
    xǁEpistemicBridgeǁ_get_default_confidence__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_1': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_1, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_2': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_2, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_3': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_3, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_4': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_4, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_5': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_5, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_6': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_6, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_7': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_7, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_8': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_8, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_9': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_9, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_10': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_10, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_11': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_11, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_12': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_12, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_13': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_13, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_14': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_14, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_15': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_15, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_16': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_16, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_17': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_17, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_18': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_18, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_19': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_19, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_20': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_20, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_21': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_21, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_22': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_22, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_23': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_23, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_24': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_24, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_25': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_25, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_26': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_26, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_27': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_27, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_28': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_28, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_29': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_29, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_30': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_30, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_31': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_31, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_32': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_32, 
        'xǁEpistemicBridgeǁ_get_default_confidence__mutmut_33': xǁEpistemicBridgeǁ_get_default_confidence__mutmut_33
    }
    
    def _get_default_confidence(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁ_get_default_confidence__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁ_get_default_confidence__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _get_default_confidence.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁ_get_default_confidence__mutmut_orig)
    xǁEpistemicBridgeǁ_get_default_confidence__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁ_get_default_confidence'
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_orig(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_1(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine and not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_2(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_3(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_4(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(None, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_5(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, None):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_6(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr('validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_7(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, ):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_8(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'XXvalidatorXX'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_9(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'VALIDATOR'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_10(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = None
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_11(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = None
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_12(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(None, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_13(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, None):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_14(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr('_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_15(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, ):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_16(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, 'XX_tool_executionsXX'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_17(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_TOOL_EXECUTIONS'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_18(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None and source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_19(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is not None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_20(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type != "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_21(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "XXtoolXX":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_22(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "TOOL":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_23(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = None
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_24(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(None)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_25(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = None
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_26(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(None, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_27(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, None):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_28(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr('_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_29(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, ):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_30(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, 'XX_memory_retrievalsXX'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_31(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_MEMORY_RETRIEVALS'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_32(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None and source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_33(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is not None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_34(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type != "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_35(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "XXmemoryXX":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_36(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "MEMORY":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_37(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = None
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_38(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(None)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_39(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = None
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_40(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(None, exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_41(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=None)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_42(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(exc_info=True)
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_43(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", )
            return {}
    
    def xǁEpistemicBridgeǁget_source_reliability__mutmut_44(self, source_type: Optional[str] = None) -> Dict[str, float]:
        """
        Get source reliability scores from epistemic validator.
        
        Args:
            source_type: Optional source type to filter by (e.g., "tool", "memory")
            
        Returns:
            Dictionary mapping source identifiers to reliability scores (0.0-1.0)
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return {}
        
        try:
            validator = self.epistemic_engine.validator
            reliability_scores = {}
            
            # Get tool reliability scores
            if hasattr(validator, '_tool_executions'):
                for tool_type, executions in validator._tool_executions.items():
                    if source_type is None or source_type == "tool":
                        reliability = validator.assess_tool_reliability(tool_type)
                        reliability_scores[f"tool:{tool_type}"] = reliability
            
            # Get memory quality scores
            if hasattr(validator, '_memory_retrievals'):
                for memory_id, retrievals in validator._memory_retrievals.items():
                    if source_type is None or source_type == "memory":
                        quality = validator.assess_memory_quality(memory_id)
                        reliability_scores[f"memory:{memory_id}"] = quality
            
            return reliability_scores
            
        except Exception as e:
            logger.warning(f"Error getting source reliability: {e}", exc_info=False)
            return {}
    
    xǁEpistemicBridgeǁget_source_reliability__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁget_source_reliability__mutmut_1': xǁEpistemicBridgeǁget_source_reliability__mutmut_1, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_2': xǁEpistemicBridgeǁget_source_reliability__mutmut_2, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_3': xǁEpistemicBridgeǁget_source_reliability__mutmut_3, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_4': xǁEpistemicBridgeǁget_source_reliability__mutmut_4, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_5': xǁEpistemicBridgeǁget_source_reliability__mutmut_5, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_6': xǁEpistemicBridgeǁget_source_reliability__mutmut_6, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_7': xǁEpistemicBridgeǁget_source_reliability__mutmut_7, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_8': xǁEpistemicBridgeǁget_source_reliability__mutmut_8, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_9': xǁEpistemicBridgeǁget_source_reliability__mutmut_9, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_10': xǁEpistemicBridgeǁget_source_reliability__mutmut_10, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_11': xǁEpistemicBridgeǁget_source_reliability__mutmut_11, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_12': xǁEpistemicBridgeǁget_source_reliability__mutmut_12, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_13': xǁEpistemicBridgeǁget_source_reliability__mutmut_13, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_14': xǁEpistemicBridgeǁget_source_reliability__mutmut_14, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_15': xǁEpistemicBridgeǁget_source_reliability__mutmut_15, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_16': xǁEpistemicBridgeǁget_source_reliability__mutmut_16, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_17': xǁEpistemicBridgeǁget_source_reliability__mutmut_17, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_18': xǁEpistemicBridgeǁget_source_reliability__mutmut_18, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_19': xǁEpistemicBridgeǁget_source_reliability__mutmut_19, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_20': xǁEpistemicBridgeǁget_source_reliability__mutmut_20, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_21': xǁEpistemicBridgeǁget_source_reliability__mutmut_21, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_22': xǁEpistemicBridgeǁget_source_reliability__mutmut_22, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_23': xǁEpistemicBridgeǁget_source_reliability__mutmut_23, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_24': xǁEpistemicBridgeǁget_source_reliability__mutmut_24, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_25': xǁEpistemicBridgeǁget_source_reliability__mutmut_25, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_26': xǁEpistemicBridgeǁget_source_reliability__mutmut_26, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_27': xǁEpistemicBridgeǁget_source_reliability__mutmut_27, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_28': xǁEpistemicBridgeǁget_source_reliability__mutmut_28, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_29': xǁEpistemicBridgeǁget_source_reliability__mutmut_29, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_30': xǁEpistemicBridgeǁget_source_reliability__mutmut_30, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_31': xǁEpistemicBridgeǁget_source_reliability__mutmut_31, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_32': xǁEpistemicBridgeǁget_source_reliability__mutmut_32, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_33': xǁEpistemicBridgeǁget_source_reliability__mutmut_33, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_34': xǁEpistemicBridgeǁget_source_reliability__mutmut_34, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_35': xǁEpistemicBridgeǁget_source_reliability__mutmut_35, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_36': xǁEpistemicBridgeǁget_source_reliability__mutmut_36, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_37': xǁEpistemicBridgeǁget_source_reliability__mutmut_37, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_38': xǁEpistemicBridgeǁget_source_reliability__mutmut_38, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_39': xǁEpistemicBridgeǁget_source_reliability__mutmut_39, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_40': xǁEpistemicBridgeǁget_source_reliability__mutmut_40, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_41': xǁEpistemicBridgeǁget_source_reliability__mutmut_41, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_42': xǁEpistemicBridgeǁget_source_reliability__mutmut_42, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_43': xǁEpistemicBridgeǁget_source_reliability__mutmut_43, 
        'xǁEpistemicBridgeǁget_source_reliability__mutmut_44': xǁEpistemicBridgeǁget_source_reliability__mutmut_44
    }
    
    def get_source_reliability(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁget_source_reliability__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁget_source_reliability__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_source_reliability.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁget_source_reliability__mutmut_orig)
    xǁEpistemicBridgeǁget_source_reliability__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁget_source_reliability'
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_orig(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_1(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine and not hasattr(self.epistemic_engine, 'validator'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_2(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_3(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or hasattr(self.epistemic_engine, 'validator'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_4(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or not hasattr(None, 'validator'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_5(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, None):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_6(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or not hasattr('validator'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_7(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, ):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_8(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'XXvalidatorXX'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_9(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'VALIDATOR'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_10(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(None)
        except Exception as e:
            logger.debug(f"Error getting tool reliability for {tool_name}: {e}")
            return None
    
    def xǁEpistemicBridgeǁget_tool_reliability__mutmut_11(self, tool_name: str) -> Optional[float]:
        """
        Get reliability score for a specific tool.
        
        Args:
            tool_name: Name of the tool
            
        Returns:
            Reliability score (0.0-1.0) or None if unavailable
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'validator'):
            return None
        
        try:
            return self.epistemic_engine.validator.assess_tool_reliability(tool_name)
        except Exception as e:
            logger.debug(None)
            return None
    
    xǁEpistemicBridgeǁget_tool_reliability__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁget_tool_reliability__mutmut_1': xǁEpistemicBridgeǁget_tool_reliability__mutmut_1, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_2': xǁEpistemicBridgeǁget_tool_reliability__mutmut_2, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_3': xǁEpistemicBridgeǁget_tool_reliability__mutmut_3, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_4': xǁEpistemicBridgeǁget_tool_reliability__mutmut_4, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_5': xǁEpistemicBridgeǁget_tool_reliability__mutmut_5, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_6': xǁEpistemicBridgeǁget_tool_reliability__mutmut_6, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_7': xǁEpistemicBridgeǁget_tool_reliability__mutmut_7, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_8': xǁEpistemicBridgeǁget_tool_reliability__mutmut_8, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_9': xǁEpistemicBridgeǁget_tool_reliability__mutmut_9, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_10': xǁEpistemicBridgeǁget_tool_reliability__mutmut_10, 
        'xǁEpistemicBridgeǁget_tool_reliability__mutmut_11': xǁEpistemicBridgeǁget_tool_reliability__mutmut_11
    }
    
    def get_tool_reliability(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁget_tool_reliability__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁget_tool_reliability__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_tool_reliability.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁget_tool_reliability__mutmut_orig)
    xǁEpistemicBridgeǁget_tool_reliability__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁget_tool_reliability'
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_orig(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(info.get("value", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_1(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = None
        try:
            return max(0.0, min(1.0, float(info.get("value", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_2(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(None)
        try:
            return max(0.0, min(1.0, float(info.get("value", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_3(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(None, min(1.0, float(info.get("value", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_4(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, None)
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_5(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(min(1.0, float(info.get("value", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_6(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, )
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_7(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(1.0, min(1.0, float(info.get("value", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_8(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(None, float(info.get("value", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_9(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, None))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_10(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(float(info.get("value", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_11(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, ))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_12(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(2.0, float(info.get("value", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_13(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(None)))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_14(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(info.get(None, 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_15(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(info.get("value", None))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_16(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(info.get(0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_17(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(info.get("value", ))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_18(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(info.get("XXvalueXX", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_19(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(info.get("VALUE", 0.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_20(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(info.get("value", 1.0))))
        except Exception:
            return 0.0
    
    def xǁEpistemicBridgeǁget_information_gain__mutmut_21(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> float:
        """
        Calculate information gain from epistemic uncertainty manager.
        
        Args:
            knowledge_items: Optional knowledge items dict (if None, uses recent items)
            
        Returns:
            Information gain score (0.0-1.0)
        """
        # Delegate to metadata-returning method to avoid hard-coded placeholder inputs.
        info = self.get_information_gain_info(knowledge_items)
        try:
            return max(0.0, min(1.0, float(info.get("value", 0.0))))
        except Exception:
            return 1.0
    
    xǁEpistemicBridgeǁget_information_gain__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁget_information_gain__mutmut_1': xǁEpistemicBridgeǁget_information_gain__mutmut_1, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_2': xǁEpistemicBridgeǁget_information_gain__mutmut_2, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_3': xǁEpistemicBridgeǁget_information_gain__mutmut_3, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_4': xǁEpistemicBridgeǁget_information_gain__mutmut_4, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_5': xǁEpistemicBridgeǁget_information_gain__mutmut_5, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_6': xǁEpistemicBridgeǁget_information_gain__mutmut_6, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_7': xǁEpistemicBridgeǁget_information_gain__mutmut_7, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_8': xǁEpistemicBridgeǁget_information_gain__mutmut_8, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_9': xǁEpistemicBridgeǁget_information_gain__mutmut_9, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_10': xǁEpistemicBridgeǁget_information_gain__mutmut_10, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_11': xǁEpistemicBridgeǁget_information_gain__mutmut_11, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_12': xǁEpistemicBridgeǁget_information_gain__mutmut_12, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_13': xǁEpistemicBridgeǁget_information_gain__mutmut_13, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_14': xǁEpistemicBridgeǁget_information_gain__mutmut_14, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_15': xǁEpistemicBridgeǁget_information_gain__mutmut_15, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_16': xǁEpistemicBridgeǁget_information_gain__mutmut_16, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_17': xǁEpistemicBridgeǁget_information_gain__mutmut_17, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_18': xǁEpistemicBridgeǁget_information_gain__mutmut_18, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_19': xǁEpistemicBridgeǁget_information_gain__mutmut_19, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_20': xǁEpistemicBridgeǁget_information_gain__mutmut_20, 
        'xǁEpistemicBridgeǁget_information_gain__mutmut_21': xǁEpistemicBridgeǁget_information_gain__mutmut_21
    }
    
    def get_information_gain(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁget_information_gain__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁget_information_gain__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_information_gain.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁget_information_gain__mutmut_orig)
    xǁEpistemicBridgeǁget_information_gain__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁget_information_gain'

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_orig(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_1(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine and not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_2(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_3(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_4(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(None, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_5(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, None):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_6(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr('uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_7(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, ):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_8(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'XXuncertainty_managerXX'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_9(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'UNCERTAINTY_MANAGER'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_10(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"XXvalueXX": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_11(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"VALUE": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_12(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 1.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_13(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "XXhas_dataXX": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_14(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "HAS_DATA": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_15(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": True, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_16(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "XXsample_sizeXX": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_17(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "SAMPLE_SIZE": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_18(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 1, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_19(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "XXestimatorXX": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_20(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "ESTIMATOR": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_21(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "XXmissing_engineXX"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_22(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "MISSING_ENGINE"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_23(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = None
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_24(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = True
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_25(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is not None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_26(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = None
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_27(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = None
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_28(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(None)
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_29(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = None
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_30(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[+20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_31(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-21:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_32(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = None
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_33(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(None)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_34(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = None
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_35(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get(None, {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_36(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", None)
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_37(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get({})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_38(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", )
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_39(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("XXconfidence_metricsXX", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_40(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("CONFIDENCE_METRICS", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_41(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_42(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get(None) if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_43(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("XXoverall_confidenceXX") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_44(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("OVERALL_CONFIDENCE") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_45(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is not None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_46(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = None
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_47(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = None
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_48(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = None

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_49(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = False

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_50(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = None
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_51(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "XXconfidenceXX": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_52(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "CONFIDENCE": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_53(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(None),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_54(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "XXimportanceXX": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_55(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "IMPORTANCE": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_56(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 1.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_57(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "XXusage_frequencyXX": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_58(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "USAGE_FREQUENCY": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_59(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 1.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_60(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = None
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_61(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = False
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_62(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        break

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_63(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_64(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"XXvalueXX": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_65(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"VALUE": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_66(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 1.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_67(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "XXhas_dataXX": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_68(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "HAS_DATA": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_69(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": True, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_70(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "XXsample_sizeXX": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_71(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "SAMPLE_SIZE": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_72(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 1, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_73(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "XXestimatorXX": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_74(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "ESTIMATOR": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_75(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "XXmeasuredXX"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_76(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "MEASURED"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_77(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = None
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_78(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = None

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_79(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(None)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_80(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = None
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_81(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) * len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_82(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(None) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_83(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "XXvalueXX": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_84(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "VALUE": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_85(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(None, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_86(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, None),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_87(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_88(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, ),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_89(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(2.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_90(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(None)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_91(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "XXhas_dataXX": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_92(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "HAS_DATA": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_93(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": False,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_94(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "XXsample_sizeXX": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_95(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "SAMPLE_SIZE": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_96(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "XXestimatorXX": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_97(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "ESTIMATOR": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_98(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "XXestimated_inputsXX" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_99(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "ESTIMATED_INPUTS" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_100(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "XXmeasuredXX",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_101(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "MEASURED",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_102(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"XXvalueXX": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_103(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"VALUE": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_104(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 1.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_105(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "XXhas_dataXX": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_106(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "HAS_DATA": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_107(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": True, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_108(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "XXsample_sizeXX": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_109(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "SAMPLE_SIZE": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_110(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 1, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_111(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "XXestimatorXX": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_112(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "ESTIMATOR": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_113(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "XXmeasuredXX"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_114(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "MEASURED"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_115(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(None, exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_116(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=None)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_117(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_118(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", )
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_119(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=False)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_120(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"XXvalueXX": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_121(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"VALUE": 0.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_122(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 1.0, "has_data": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_123(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "XXhas_dataXX": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_124(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "HAS_DATA": False, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_125(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": True, "sample_size": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_126(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "XXsample_sizeXX": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_127(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "SAMPLE_SIZE": 0, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_128(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 1, "estimator": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_129(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "XXestimatorXX": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_130(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "ESTIMATOR": "error"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_131(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "XXerrorXX"}

    def xǁEpistemicBridgeǁget_information_gain_info__mutmut_132(self, knowledge_items: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
        """
        Information gain with metadata to avoid silent placeholders.

        Returns:
            Dict with:
            - value: float in [0,1]
            - has_data: bool
            - sample_size: int
            - estimator: str ("measured" | "missing_engine" | "error" | "estimated_inputs")
        """
        if not self.epistemic_engine or not hasattr(self.epistemic_engine, 'uncertainty_manager'):
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "missing_engine"}

        try:
            # If caller doesn't supply, attempt to build from recent knowledge items
            estimated_inputs = False
            if knowledge_items is None:
                epistemic_layer = self.epistemic_engine.epistemic_layer
                knowledge_ids = list(epistemic_layer.knowledge_sources.keys())
                knowledge_items = {}
                for kid in knowledge_ids[-20:]:
                    try:
                        context = self.epistemic_engine.get_epistemic_context(kid)
                        metrics = context.get("confidence_metrics", {})
                        conf_val = metrics.get("overall_confidence") if metrics else None
                        if conf_val is None:
                            conf_mean, _interval = confidence_for_missing_data()
                            conf_val = conf_mean
                            estimated_inputs = True

                        # importance/usage_frequency are currently not tracked in epistemic layer;
                        # keep conservative defaults but mark as estimated inputs.
                        knowledge_items[kid] = {
                            "confidence": float(conf_val),
                            "importance": 0.25,
                            "usage_frequency": 0.0,
                        }
                        estimated_inputs = True
                    except Exception:
                        continue

            if not knowledge_items:
                return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}

            uncertainty_manager = self.epistemic_engine.uncertainty_manager
            gains = uncertainty_manager.information_gain_calculation(knowledge_items)

            if gains:
                avg_gain = sum(gain for _, gain in gains) / len(gains)
                return {
                    "value": min(1.0, float(avg_gain)),
                    "has_data": True,
                    "sample_size": len(gains),
                    "estimator": "estimated_inputs" if estimated_inputs else "measured",
                }

            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "measured"}
        except Exception as e:
            logger.debug(f"Error calculating information gain (info): {e}", exc_info=True)
            return {"value": 0.0, "has_data": False, "sample_size": 0, "estimator": "ERROR"}
    
    xǁEpistemicBridgeǁget_information_gain_info__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁget_information_gain_info__mutmut_1': xǁEpistemicBridgeǁget_information_gain_info__mutmut_1, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_2': xǁEpistemicBridgeǁget_information_gain_info__mutmut_2, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_3': xǁEpistemicBridgeǁget_information_gain_info__mutmut_3, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_4': xǁEpistemicBridgeǁget_information_gain_info__mutmut_4, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_5': xǁEpistemicBridgeǁget_information_gain_info__mutmut_5, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_6': xǁEpistemicBridgeǁget_information_gain_info__mutmut_6, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_7': xǁEpistemicBridgeǁget_information_gain_info__mutmut_7, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_8': xǁEpistemicBridgeǁget_information_gain_info__mutmut_8, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_9': xǁEpistemicBridgeǁget_information_gain_info__mutmut_9, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_10': xǁEpistemicBridgeǁget_information_gain_info__mutmut_10, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_11': xǁEpistemicBridgeǁget_information_gain_info__mutmut_11, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_12': xǁEpistemicBridgeǁget_information_gain_info__mutmut_12, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_13': xǁEpistemicBridgeǁget_information_gain_info__mutmut_13, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_14': xǁEpistemicBridgeǁget_information_gain_info__mutmut_14, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_15': xǁEpistemicBridgeǁget_information_gain_info__mutmut_15, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_16': xǁEpistemicBridgeǁget_information_gain_info__mutmut_16, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_17': xǁEpistemicBridgeǁget_information_gain_info__mutmut_17, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_18': xǁEpistemicBridgeǁget_information_gain_info__mutmut_18, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_19': xǁEpistemicBridgeǁget_information_gain_info__mutmut_19, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_20': xǁEpistemicBridgeǁget_information_gain_info__mutmut_20, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_21': xǁEpistemicBridgeǁget_information_gain_info__mutmut_21, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_22': xǁEpistemicBridgeǁget_information_gain_info__mutmut_22, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_23': xǁEpistemicBridgeǁget_information_gain_info__mutmut_23, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_24': xǁEpistemicBridgeǁget_information_gain_info__mutmut_24, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_25': xǁEpistemicBridgeǁget_information_gain_info__mutmut_25, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_26': xǁEpistemicBridgeǁget_information_gain_info__mutmut_26, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_27': xǁEpistemicBridgeǁget_information_gain_info__mutmut_27, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_28': xǁEpistemicBridgeǁget_information_gain_info__mutmut_28, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_29': xǁEpistemicBridgeǁget_information_gain_info__mutmut_29, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_30': xǁEpistemicBridgeǁget_information_gain_info__mutmut_30, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_31': xǁEpistemicBridgeǁget_information_gain_info__mutmut_31, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_32': xǁEpistemicBridgeǁget_information_gain_info__mutmut_32, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_33': xǁEpistemicBridgeǁget_information_gain_info__mutmut_33, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_34': xǁEpistemicBridgeǁget_information_gain_info__mutmut_34, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_35': xǁEpistemicBridgeǁget_information_gain_info__mutmut_35, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_36': xǁEpistemicBridgeǁget_information_gain_info__mutmut_36, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_37': xǁEpistemicBridgeǁget_information_gain_info__mutmut_37, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_38': xǁEpistemicBridgeǁget_information_gain_info__mutmut_38, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_39': xǁEpistemicBridgeǁget_information_gain_info__mutmut_39, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_40': xǁEpistemicBridgeǁget_information_gain_info__mutmut_40, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_41': xǁEpistemicBridgeǁget_information_gain_info__mutmut_41, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_42': xǁEpistemicBridgeǁget_information_gain_info__mutmut_42, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_43': xǁEpistemicBridgeǁget_information_gain_info__mutmut_43, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_44': xǁEpistemicBridgeǁget_information_gain_info__mutmut_44, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_45': xǁEpistemicBridgeǁget_information_gain_info__mutmut_45, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_46': xǁEpistemicBridgeǁget_information_gain_info__mutmut_46, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_47': xǁEpistemicBridgeǁget_information_gain_info__mutmut_47, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_48': xǁEpistemicBridgeǁget_information_gain_info__mutmut_48, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_49': xǁEpistemicBridgeǁget_information_gain_info__mutmut_49, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_50': xǁEpistemicBridgeǁget_information_gain_info__mutmut_50, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_51': xǁEpistemicBridgeǁget_information_gain_info__mutmut_51, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_52': xǁEpistemicBridgeǁget_information_gain_info__mutmut_52, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_53': xǁEpistemicBridgeǁget_information_gain_info__mutmut_53, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_54': xǁEpistemicBridgeǁget_information_gain_info__mutmut_54, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_55': xǁEpistemicBridgeǁget_information_gain_info__mutmut_55, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_56': xǁEpistemicBridgeǁget_information_gain_info__mutmut_56, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_57': xǁEpistemicBridgeǁget_information_gain_info__mutmut_57, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_58': xǁEpistemicBridgeǁget_information_gain_info__mutmut_58, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_59': xǁEpistemicBridgeǁget_information_gain_info__mutmut_59, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_60': xǁEpistemicBridgeǁget_information_gain_info__mutmut_60, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_61': xǁEpistemicBridgeǁget_information_gain_info__mutmut_61, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_62': xǁEpistemicBridgeǁget_information_gain_info__mutmut_62, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_63': xǁEpistemicBridgeǁget_information_gain_info__mutmut_63, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_64': xǁEpistemicBridgeǁget_information_gain_info__mutmut_64, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_65': xǁEpistemicBridgeǁget_information_gain_info__mutmut_65, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_66': xǁEpistemicBridgeǁget_information_gain_info__mutmut_66, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_67': xǁEpistemicBridgeǁget_information_gain_info__mutmut_67, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_68': xǁEpistemicBridgeǁget_information_gain_info__mutmut_68, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_69': xǁEpistemicBridgeǁget_information_gain_info__mutmut_69, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_70': xǁEpistemicBridgeǁget_information_gain_info__mutmut_70, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_71': xǁEpistemicBridgeǁget_information_gain_info__mutmut_71, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_72': xǁEpistemicBridgeǁget_information_gain_info__mutmut_72, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_73': xǁEpistemicBridgeǁget_information_gain_info__mutmut_73, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_74': xǁEpistemicBridgeǁget_information_gain_info__mutmut_74, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_75': xǁEpistemicBridgeǁget_information_gain_info__mutmut_75, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_76': xǁEpistemicBridgeǁget_information_gain_info__mutmut_76, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_77': xǁEpistemicBridgeǁget_information_gain_info__mutmut_77, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_78': xǁEpistemicBridgeǁget_information_gain_info__mutmut_78, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_79': xǁEpistemicBridgeǁget_information_gain_info__mutmut_79, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_80': xǁEpistemicBridgeǁget_information_gain_info__mutmut_80, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_81': xǁEpistemicBridgeǁget_information_gain_info__mutmut_81, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_82': xǁEpistemicBridgeǁget_information_gain_info__mutmut_82, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_83': xǁEpistemicBridgeǁget_information_gain_info__mutmut_83, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_84': xǁEpistemicBridgeǁget_information_gain_info__mutmut_84, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_85': xǁEpistemicBridgeǁget_information_gain_info__mutmut_85, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_86': xǁEpistemicBridgeǁget_information_gain_info__mutmut_86, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_87': xǁEpistemicBridgeǁget_information_gain_info__mutmut_87, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_88': xǁEpistemicBridgeǁget_information_gain_info__mutmut_88, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_89': xǁEpistemicBridgeǁget_information_gain_info__mutmut_89, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_90': xǁEpistemicBridgeǁget_information_gain_info__mutmut_90, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_91': xǁEpistemicBridgeǁget_information_gain_info__mutmut_91, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_92': xǁEpistemicBridgeǁget_information_gain_info__mutmut_92, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_93': xǁEpistemicBridgeǁget_information_gain_info__mutmut_93, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_94': xǁEpistemicBridgeǁget_information_gain_info__mutmut_94, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_95': xǁEpistemicBridgeǁget_information_gain_info__mutmut_95, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_96': xǁEpistemicBridgeǁget_information_gain_info__mutmut_96, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_97': xǁEpistemicBridgeǁget_information_gain_info__mutmut_97, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_98': xǁEpistemicBridgeǁget_information_gain_info__mutmut_98, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_99': xǁEpistemicBridgeǁget_information_gain_info__mutmut_99, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_100': xǁEpistemicBridgeǁget_information_gain_info__mutmut_100, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_101': xǁEpistemicBridgeǁget_information_gain_info__mutmut_101, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_102': xǁEpistemicBridgeǁget_information_gain_info__mutmut_102, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_103': xǁEpistemicBridgeǁget_information_gain_info__mutmut_103, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_104': xǁEpistemicBridgeǁget_information_gain_info__mutmut_104, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_105': xǁEpistemicBridgeǁget_information_gain_info__mutmut_105, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_106': xǁEpistemicBridgeǁget_information_gain_info__mutmut_106, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_107': xǁEpistemicBridgeǁget_information_gain_info__mutmut_107, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_108': xǁEpistemicBridgeǁget_information_gain_info__mutmut_108, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_109': xǁEpistemicBridgeǁget_information_gain_info__mutmut_109, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_110': xǁEpistemicBridgeǁget_information_gain_info__mutmut_110, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_111': xǁEpistemicBridgeǁget_information_gain_info__mutmut_111, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_112': xǁEpistemicBridgeǁget_information_gain_info__mutmut_112, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_113': xǁEpistemicBridgeǁget_information_gain_info__mutmut_113, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_114': xǁEpistemicBridgeǁget_information_gain_info__mutmut_114, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_115': xǁEpistemicBridgeǁget_information_gain_info__mutmut_115, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_116': xǁEpistemicBridgeǁget_information_gain_info__mutmut_116, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_117': xǁEpistemicBridgeǁget_information_gain_info__mutmut_117, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_118': xǁEpistemicBridgeǁget_information_gain_info__mutmut_118, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_119': xǁEpistemicBridgeǁget_information_gain_info__mutmut_119, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_120': xǁEpistemicBridgeǁget_information_gain_info__mutmut_120, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_121': xǁEpistemicBridgeǁget_information_gain_info__mutmut_121, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_122': xǁEpistemicBridgeǁget_information_gain_info__mutmut_122, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_123': xǁEpistemicBridgeǁget_information_gain_info__mutmut_123, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_124': xǁEpistemicBridgeǁget_information_gain_info__mutmut_124, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_125': xǁEpistemicBridgeǁget_information_gain_info__mutmut_125, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_126': xǁEpistemicBridgeǁget_information_gain_info__mutmut_126, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_127': xǁEpistemicBridgeǁget_information_gain_info__mutmut_127, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_128': xǁEpistemicBridgeǁget_information_gain_info__mutmut_128, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_129': xǁEpistemicBridgeǁget_information_gain_info__mutmut_129, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_130': xǁEpistemicBridgeǁget_information_gain_info__mutmut_130, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_131': xǁEpistemicBridgeǁget_information_gain_info__mutmut_131, 
        'xǁEpistemicBridgeǁget_information_gain_info__mutmut_132': xǁEpistemicBridgeǁget_information_gain_info__mutmut_132
    }
    
    def get_information_gain_info(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁget_information_gain_info__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁget_information_gain_info__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_information_gain_info.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁget_information_gain_info__mutmut_orig)
    xǁEpistemicBridgeǁget_information_gain_info__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁget_information_gain_info'
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_orig(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_1(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_2(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = None
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_3(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(None, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_4(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, None):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_5(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr('record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_6(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, ):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_7(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'XXrecord_uncertaintyXX'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_8(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'RECORD_UNCERTAINTY'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_9(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=None,
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_10(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=None,
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_11(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=None,
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_12(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=None,
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_13(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=None,
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_14(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=None,
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_15(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=None,
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_16(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_17(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_18(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_19(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_20(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_21(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_22(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_23(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(None)}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_24(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__(None).time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_25(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('XXtimeXX').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_26(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('TIME').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_27(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get(None, 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_28(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", None),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_29(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get(0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_30(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", ),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_31(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("XXtotalXX", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_32(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("TOTAL", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_33(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 1.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_34(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get(None, 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_35(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", None),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_36(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get(0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_37(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", ),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_38(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("XXepistemicXX", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_39(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("EPISTEMIC", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_40(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 1.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_41(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get(None, 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_42(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", None),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_43(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get(0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_44(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", ),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_45(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("XXaleatoricXX", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_46(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("ALEATORIC", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_47(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 1.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_48(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get(None, 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_49(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", None),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_50(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get(0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_51(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", ),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_52(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("XXknowledge_gapsXX", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_53(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("KNOWLEDGE_GAPS", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_54(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 1.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_55(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get(None, 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_56(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", None),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_57(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get(0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_58(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", ),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_59(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("XXambiguityXX", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_60(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("AMBIGUITY", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_61(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 1.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_62(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get(None, 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_63(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", None),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_64(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get(0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_65(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", ),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_66(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("XXnoiseXX", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_67(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("NOISE", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_68(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 1.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_69(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = None
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_70(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(None, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_71(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, None):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_72(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr('_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_73(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, ):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_74(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, 'XX_update_confidence_levelXX'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_75(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_UPDATE_CONFIDENCE_LEVEL'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_76(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(None, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_77(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, None):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_78(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr('update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_79(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, ):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_80(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'XXupdate_calibration_from_epistemicXX'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_81(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'UPDATE_CALIBRATION_FROM_EPISTEMIC'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_82(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=None,
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_83(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=None,
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_84(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=None,
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_85(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_86(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_87(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_88(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get(None),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_89(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("XXeceXX"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_90(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ECE"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_91(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get(None),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_92(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("XXbrier_scoreXX"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_93(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("BRIER_SCORE"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_94(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get(None),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_95(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("XXcalibration_errorXX"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_96(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("CALIBRATION_ERROR"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_97(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(None, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_98(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, None):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_99(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr('update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_100(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, ):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_101(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'XXupdate_certainty_affectXX'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_102(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'UPDATE_CERTAINTY_AFFECT'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_103(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = None
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_104(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get(None, 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_105(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", None)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_106(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get(0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_107(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", )
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_108(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("XXoverall_confidenceXX", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_109(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("OVERALL_CONFIDENCE", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_110(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 1.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_111(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = None
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_112(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get(None)
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_113(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("XXcalibration_errorXX")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_114(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("CALIBRATION_ERROR")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_115(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = None
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_116(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get(None)
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_117(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("XXreliabilityXX")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_118(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("RELIABILITY")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_119(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=None,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_120(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=None,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_121(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=None,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_122(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_123(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_124(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_125(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(None, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_126(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, None):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_127(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr('compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_128(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, ):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_129(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'XXcompute_curiosity_driveXX'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_130(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'COMPUTE_CURIOSITY_DRIVE'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_131(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = None
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_132(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = None
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_133(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get(None, 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_134(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", None)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_135(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get(0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_136(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", )
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_137(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("XXtotalXX", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_138(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("TOTAL", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_139(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 1.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_140(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = None  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_141(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 1.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_142(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(None, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_143(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, None):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_144(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr('states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_145(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, ):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_146(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'XXstatesXX'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_147(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'STATES'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_148(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = None
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_149(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get(None, {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_150(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", None)
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_151(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get({})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_152(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", )
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_153(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("XXattention_allocationXX", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_154(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("ATTENTION_ALLOCATION", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_155(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = None
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_156(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(None, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_157(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, None) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_158(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_159(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, ) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_160(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(2.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_161(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(None)) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_162(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 1.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_163(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=None,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_164(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=None,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_165(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=None,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_166(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_167(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_168(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_169(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(None, exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_170(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=None)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_171(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_172(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", )
    
    def xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_173(self, 
                              cognitive_monitor: Any,
                              affective_monitor: Any) -> None:
        """
        Update internal sensing monitors from epistemic engine state.
        
        Args:
            cognitive_monitor: CognitiveStateMonitor instance
            affective_monitor: ComputationalAffectMonitor instance
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Get aggregated uncertainty
            uncertainty = self.get_aggregated_uncertainty()
            
            # Update cognitive monitor with epistemic uncertainty
            if hasattr(cognitive_monitor, 'record_uncertainty'):
                cognitive_monitor.record_uncertainty(
                    question_id=f"epistemic_aggregate_{int(__import__('time').time())}",
                    uncertainty=uncertainty.get("total", 0.0),
                    epistemic=uncertainty.get("epistemic", 0.0),
                    aleatoric=uncertainty.get("aleatoric", 0.0),
                    knowledge_gaps=uncertainty.get("knowledge_gaps", 0.0),
                    ambiguity=uncertainty.get("ambiguity", 0.0),
                    noise=uncertainty.get("noise", 0.0),
                )
            
            # Get aggregated confidence
            confidence = self.get_aggregated_confidence()
            
            # Update cognitive monitor with epistemic confidence calibration
            if hasattr(cognitive_monitor, '_update_confidence_level'):
                # The _update_confidence_level will use calibration metrics if available
                # We'll set them via a method that accepts calibration data
                if hasattr(cognitive_monitor, 'update_calibration_from_epistemic'):
                    cognitive_monitor.update_calibration_from_epistemic(
                        ece=confidence.get("ece"),
                        brier_score=confidence.get("brier_score"),
                        calibration_error=confidence.get("calibration_error"),
                    )
            
            # Update affective monitor with epistemic metrics
            if hasattr(affective_monitor, 'update_certainty_affect'):
                # Use epistemic confidence for certainty affect
                epistemic_conf = confidence.get("overall_confidence", 0.5)
                calibration_error = confidence.get("calibration_error")
                reliability = confidence.get("reliability")
                
                affective_monitor.update_certainty_affect(
                    confidence=epistemic_conf,
                    calibration_error=calibration_error,
                    confidence_accuracy_correlation=reliability,
                )
            
            # Update curiosity drive with information gain
            if hasattr(affective_monitor, 'compute_curiosity_drive'):
                information_gain = self.get_information_gain()
                # Get current uncertainty for curiosity calculation
                current_uncertainty = uncertainty.get("total", 0.0)
                # Use attention as proxy for interest (would need to get from cognitive)
                interest = 0.5  # Default
                if hasattr(cognitive_monitor, 'states'):
                    attention = cognitive_monitor.states.get("attention_allocation", {})
                    interest = min(1.0, sum(attention.values())) if attention else 0.5
                
                affective_monitor.compute_curiosity_drive(
                    uncertainty=current_uncertainty,
                    interest=interest,
                    information_gain=information_gain,
                )
            
        except Exception as e:
            logger.warning(f"Error updating internal sensing from epistemic: {e}", exc_info=False)
    
    xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_1': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_1, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_2': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_2, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_3': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_3, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_4': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_4, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_5': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_5, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_6': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_6, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_7': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_7, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_8': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_8, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_9': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_9, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_10': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_10, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_11': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_11, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_12': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_12, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_13': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_13, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_14': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_14, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_15': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_15, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_16': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_16, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_17': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_17, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_18': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_18, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_19': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_19, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_20': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_20, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_21': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_21, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_22': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_22, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_23': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_23, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_24': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_24, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_25': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_25, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_26': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_26, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_27': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_27, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_28': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_28, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_29': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_29, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_30': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_30, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_31': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_31, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_32': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_32, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_33': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_33, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_34': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_34, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_35': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_35, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_36': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_36, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_37': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_37, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_38': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_38, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_39': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_39, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_40': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_40, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_41': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_41, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_42': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_42, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_43': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_43, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_44': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_44, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_45': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_45, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_46': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_46, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_47': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_47, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_48': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_48, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_49': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_49, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_50': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_50, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_51': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_51, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_52': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_52, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_53': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_53, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_54': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_54, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_55': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_55, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_56': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_56, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_57': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_57, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_58': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_58, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_59': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_59, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_60': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_60, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_61': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_61, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_62': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_62, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_63': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_63, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_64': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_64, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_65': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_65, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_66': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_66, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_67': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_67, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_68': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_68, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_69': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_69, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_70': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_70, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_71': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_71, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_72': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_72, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_73': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_73, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_74': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_74, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_75': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_75, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_76': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_76, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_77': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_77, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_78': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_78, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_79': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_79, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_80': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_80, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_81': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_81, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_82': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_82, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_83': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_83, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_84': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_84, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_85': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_85, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_86': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_86, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_87': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_87, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_88': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_88, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_89': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_89, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_90': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_90, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_91': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_91, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_92': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_92, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_93': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_93, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_94': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_94, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_95': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_95, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_96': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_96, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_97': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_97, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_98': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_98, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_99': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_99, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_100': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_100, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_101': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_101, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_102': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_102, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_103': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_103, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_104': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_104, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_105': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_105, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_106': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_106, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_107': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_107, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_108': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_108, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_109': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_109, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_110': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_110, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_111': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_111, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_112': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_112, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_113': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_113, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_114': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_114, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_115': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_115, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_116': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_116, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_117': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_117, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_118': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_118, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_119': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_119, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_120': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_120, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_121': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_121, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_122': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_122, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_123': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_123, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_124': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_124, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_125': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_125, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_126': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_126, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_127': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_127, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_128': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_128, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_129': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_129, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_130': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_130, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_131': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_131, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_132': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_132, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_133': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_133, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_134': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_134, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_135': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_135, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_136': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_136, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_137': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_137, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_138': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_138, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_139': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_139, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_140': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_140, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_141': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_141, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_142': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_142, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_143': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_143, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_144': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_144, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_145': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_145, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_146': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_146, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_147': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_147, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_148': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_148, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_149': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_149, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_150': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_150, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_151': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_151, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_152': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_152, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_153': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_153, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_154': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_154, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_155': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_155, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_156': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_156, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_157': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_157, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_158': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_158, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_159': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_159, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_160': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_160, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_161': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_161, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_162': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_162, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_163': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_163, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_164': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_164, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_165': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_165, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_166': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_166, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_167': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_167, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_168': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_168, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_169': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_169, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_170': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_170, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_171': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_171, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_172': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_172, 
        'xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_173': xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_173
    }
    
    def update_from_epistemic(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_mutants"), args, kwargs, self)
        return result 
    
    update_from_epistemic.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_orig)
    xǁEpistemicBridgeǁupdate_from_epistemic__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁupdate_from_epistemic'
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_orig(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_1(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_2(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = None
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_3(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id(None, response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_4(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", None)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_5(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id(response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_6(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", )
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_7(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("XXinternal_sensingXX", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_8(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("INTERNAL_SENSING", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_9(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = None
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_10(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=None,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_11(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type=None,
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_12(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=None,
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_13(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=None,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_14(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_15(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_16(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_17(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_18(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_19(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_20(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="XXinternal_sensing_confidenceXX",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_21(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="INTERNAL_SENSING_CONFIDENCE",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_22(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(None),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_23(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 + uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_24(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=2.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_25(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=None,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_26(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=None,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_27(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=None,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_28(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_29(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_30(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_31(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None or hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_32(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_33(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(None, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_34(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, None):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_35(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr('calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_36(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, ):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_37(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'XXcalibratorXX'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_38(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'CALIBRATOR'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_39(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=None,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_40(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=None,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_41(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_42(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_43(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(None, exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_44(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=None)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_45(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(exc_info=True)
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_46(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", )
    
    def xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_47(self,
                                       response_id: str,
                                       confidence: float,
                                       uncertainty: float,
                                       correct: Optional[bool] = None) -> None:
        """
        Feed internal sensing insights back to epistemic engine.
        
        Args:
            response_id: Identifier for the response
            confidence: Confidence level from internal sensing (0.0-1.0)
            uncertainty: Uncertainty level from internal sensing (0.0-1.0)
            correct: Optional correctness indicator for calibration
        """
        if not self.epistemic_engine:
            return
        
        try:
            # Create a knowledge ID for this response
            from ..self_model.epistemic.ids import generate_knowledge_id
            knowledge_id = generate_knowledge_id("internal_sensing", response_id)
            
            # Create source metadata for internal sensing
            from ..self_model.epistemic.models import SourceMetadata, SourceType
            from datetime import datetime, timezone
            
            source = SourceMetadata(
                source_type=SourceType.EMERGENT_PATTERN,
                pattern_type="internal_sensing_confidence",
                timestamp=datetime.now(timezone.utc),
                statistical_significance=confidence,
                predictive_accuracy=1.0 - uncertainty if uncertainty else None,
            )
            
            # Update epistemic engine with internal sensing confidence
            # Use confidence as evidence strength
            self.epistemic_engine.confidence_update_workflow(
                knowledge_id=knowledge_id,
                new_evidence=source,
                evidence_strength=confidence,
            )
            
            # Record outcome if available for calibration
            if correct is not None and hasattr(self.epistemic_engine, 'calibrator'):
                self.epistemic_engine.calibrator.track_calibration_error(
                    predicted_confidence=confidence,
                    actual_outcome=correct,
                )
            
        except Exception as e:
            logger.debug(f"Error updating epistemic from internal sensing: {e}", exc_info=False)
    
    xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_1': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_1, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_2': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_2, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_3': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_3, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_4': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_4, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_5': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_5, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_6': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_6, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_7': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_7, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_8': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_8, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_9': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_9, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_10': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_10, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_11': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_11, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_12': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_12, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_13': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_13, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_14': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_14, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_15': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_15, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_16': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_16, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_17': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_17, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_18': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_18, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_19': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_19, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_20': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_20, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_21': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_21, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_22': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_22, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_23': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_23, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_24': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_24, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_25': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_25, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_26': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_26, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_27': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_27, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_28': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_28, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_29': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_29, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_30': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_30, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_31': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_31, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_32': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_32, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_33': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_33, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_34': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_34, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_35': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_35, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_36': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_36, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_37': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_37, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_38': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_38, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_39': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_39, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_40': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_40, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_41': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_41, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_42': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_42, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_43': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_43, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_44': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_44, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_45': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_45, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_46': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_46, 
        'xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_47': xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_47
    }
    
    def update_epistemic_from_internal(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_mutants"), args, kwargs, self)
        return result 
    
    update_epistemic_from_internal.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_orig)
    xǁEpistemicBridgeǁupdate_epistemic_from_internal__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁupdate_epistemic_from_internal'
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_orig(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_1(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_2(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "XXtool_success_rateXX": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_3(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "TOOL_SUCCESS_RATE": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_4(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 1.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_5(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "XXmemory_consistencyXX": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_6(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "MEMORY_CONSISTENCY": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_7(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 1.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_8(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "XXknowledge_confidenceXX": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_9(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "KNOWLEDGE_CONFIDENCE": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_10(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 1.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_11(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = None
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_12(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(None, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_13(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, None):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_14(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr('validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_15(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, ):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_16(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'XXvalidatorXX'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_17(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'VALIDATOR'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_18(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = None
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_19(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(None, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_20(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, None):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_21(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr('_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_22(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, ):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_23(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, 'XX_tool_executionsXX'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_24(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_TOOL_EXECUTIONS'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_25(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = None
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_26(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(None)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_27(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(None)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_28(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = None
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_29(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) * len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_30(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(None) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_31(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 1.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_32(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = None
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_33(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(None, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_34(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, None):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_35(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr('validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_36(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, ):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_37(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'XXvalidatorXX'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_38(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'VALIDATOR'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_39(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = None
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_40(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(None, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_41(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, None):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_42(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr('_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_43(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, ):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_44(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, 'XX_memory_retrievalsXX'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_45(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_MEMORY_RETRIEVALS'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_46(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = None
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_47(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(None)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_48(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(None)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_49(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = None
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_50(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) * len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_51(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(None) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_52(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 1.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_53(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = None
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_54(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = None
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_55(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get(None, 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_56(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", None)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_57(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get(0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_58(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", )
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_59(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("XXoverall_confidenceXX", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_60(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("OVERALL_CONFIDENCE", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_61(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 1.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_62(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "XXtool_success_rateXX": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_63(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "TOOL_SUCCESS_RATE": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_64(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "XXmemory_consistencyXX": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_65(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "MEMORY_CONSISTENCY": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_66(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "XXknowledge_confidenceXX": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_67(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "KNOWLEDGE_CONFIDENCE": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_68(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(None)
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_69(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "XXtool_success_rateXX": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_70(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "TOOL_SUCCESS_RATE": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_71(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 1.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_72(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "XXmemory_consistencyXX": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_73(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "MEMORY_CONSISTENCY": 0.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_74(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 1.5,
                "knowledge_confidence": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_75(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "XXknowledge_confidenceXX": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_76(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "KNOWLEDGE_CONFIDENCE": 0.5,
            }
    
    def xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_77(self) -> Dict[str, float]:
        """
        Get epistemic context for valence calculation.
        
        Returns:
            Dictionary with:
            - tool_success_rate: Average tool success rate
            - memory_consistency: Average memory consistency
            - knowledge_confidence: Average knowledge confidence
        """
        if not self.epistemic_engine:
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 0.5,
            }
        
        try:
            # Get tool reliability scores
            tool_reliabilities = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_tool_executions'):
                    for tool_type in validator._tool_executions.keys():
                        reliability = validator.assess_tool_reliability(tool_type)
                        tool_reliabilities.append(reliability)
            
            tool_success_rate = sum(tool_reliabilities) / len(tool_reliabilities) if tool_reliabilities else 0.5
            
            # Get memory consistency
            memory_consistencies = []
            if hasattr(self.epistemic_engine, 'validator'):
                validator = self.epistemic_engine.validator
                if hasattr(validator, '_memory_retrievals'):
                    for memory_id in validator._memory_retrievals.keys():
                        quality = validator.assess_memory_quality(memory_id)
                        memory_consistencies.append(quality)
            
            memory_consistency = sum(memory_consistencies) / len(memory_consistencies) if memory_consistencies else 0.5
            
            # Get average knowledge confidence
            confidence = self.get_aggregated_confidence()
            knowledge_confidence = confidence.get("overall_confidence", 0.5)
            
            return {
                "tool_success_rate": tool_success_rate,
                "memory_consistency": memory_consistency,
                "knowledge_confidence": knowledge_confidence,
            }
            
        except Exception as e:
            logger.debug(f"Error getting epistemic valence context: {e}")
            return {
                "tool_success_rate": 0.5,
                "memory_consistency": 0.5,
                "knowledge_confidence": 1.5,
            }
    
    xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_1': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_1, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_2': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_2, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_3': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_3, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_4': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_4, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_5': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_5, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_6': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_6, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_7': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_7, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_8': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_8, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_9': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_9, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_10': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_10, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_11': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_11, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_12': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_12, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_13': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_13, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_14': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_14, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_15': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_15, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_16': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_16, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_17': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_17, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_18': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_18, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_19': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_19, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_20': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_20, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_21': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_21, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_22': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_22, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_23': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_23, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_24': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_24, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_25': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_25, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_26': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_26, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_27': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_27, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_28': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_28, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_29': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_29, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_30': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_30, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_31': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_31, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_32': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_32, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_33': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_33, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_34': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_34, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_35': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_35, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_36': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_36, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_37': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_37, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_38': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_38, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_39': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_39, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_40': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_40, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_41': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_41, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_42': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_42, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_43': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_43, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_44': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_44, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_45': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_45, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_46': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_46, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_47': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_47, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_48': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_48, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_49': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_49, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_50': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_50, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_51': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_51, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_52': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_52, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_53': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_53, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_54': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_54, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_55': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_55, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_56': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_56, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_57': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_57, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_58': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_58, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_59': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_59, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_60': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_60, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_61': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_61, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_62': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_62, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_63': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_63, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_64': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_64, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_65': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_65, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_66': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_66, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_67': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_67, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_68': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_68, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_69': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_69, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_70': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_70, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_71': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_71, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_72': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_72, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_73': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_73, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_74': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_74, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_75': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_75, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_76': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_76, 
        'xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_77': xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_77
    }
    
    def get_epistemic_valence_context(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_orig"), object.__getattribute__(self, "xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_epistemic_valence_context.__signature__ = _mutmut_signature(xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_orig)
    xǁEpistemicBridgeǁget_epistemic_valence_context__mutmut_orig.__name__ = 'xǁEpistemicBridgeǁget_epistemic_valence_context'

