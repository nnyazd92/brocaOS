"""
Summarization manager orchestrating the summarization pipeline.

Handles triggers, validation, merging, and storage coordination.
"""

from __future__ import annotations

import logging
from typing import Dict, Any, List, Optional
from datetime import datetime, timezone

from .event_logger import EventLogger
from .summarizer import Summarizer
from .storage import SummaryStorage
from .validation import SummarizationValidator
from .models import (
    SessionSummary,
    SummaryHeader,
    SummaryBlocks,
    TasksFile,
    Task,
    ProjectState,
    EvidenceItem,
    ConfidenceLevel
)
from .token_estimator import estimate_tokens, estimate_messages_tokens, estimate_prompt_tokens
from .prompt_builder import PromptBuilder
from ..config import config

logger = logging.getLogger(__name__)
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class SummarizationManager:
    """
    Manages the summarization pipeline.
    
    Handles triggers, delta window gathering, summarizer invocation, and merge logic.
    """
    
    def xǁSummarizationManagerǁ__init____mutmut_orig(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_1(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = None
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_2(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = None
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_3(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = None
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_4(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer and Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_5(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=None,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_6(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=None
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_7(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_8(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_9(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = None
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_10(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(None)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_11(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = None
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_12(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_13(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = None
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_14(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_15(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = None
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_16(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is None else config.summarization.context_window_size
        
        logger.debug(
            f"Initialized SummarizationManager: trigger_turns={self.trigger_turns}, "
            f"trigger_token_threshold={self.trigger_token_threshold}, "
            f"context_window_size={self.context_window_size}"
        )
    
    def xǁSummarizationManagerǁ__init____mutmut_17(
        self,
        event_logger: EventLogger,
        summary_storage: SummaryStorage,
        summarizer: Optional[Summarizer] = None,
        trigger_turns: Optional[int] = None,
        trigger_token_threshold: Optional[float] = None,
        context_window_size: Optional[int] = None
    ) -> None:
        """
        Initialize summarization manager.
        
        Args:
            event_logger: EventLogger instance
            summary_storage: SummaryStorage instance
            summarizer: Optional Summarizer instance (creates default if not provided)
            trigger_turns: Number of turns before triggering summarization (defaults to config)
            trigger_token_threshold: Token usage threshold (0.0-1.0) to trigger summarization (defaults to config)
            context_window_size: Context window size in tokens (defaults to config)
        """
        self.event_logger = event_logger
        self.summary_storage = summary_storage
        self.summarizer = summarizer or Summarizer(
            max_summary_tokens=config.summarization.max_summary_tokens,
            max_block_tokens=config.summarization.max_block_tokens
        )
        self.validator = SummarizationValidator(event_logger)
        self.trigger_turns = trigger_turns if trigger_turns is not None else config.summarization.trigger_turns
        self.trigger_token_threshold = trigger_token_threshold if trigger_token_threshold is not None else config.summarization.trigger_token_threshold
        self.context_window_size = context_window_size if context_window_size is not None else config.summarization.context_window_size
        
        logger.debug(
            None
        )
    
    xǁSummarizationManagerǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSummarizationManagerǁ__init____mutmut_1': xǁSummarizationManagerǁ__init____mutmut_1, 
        'xǁSummarizationManagerǁ__init____mutmut_2': xǁSummarizationManagerǁ__init____mutmut_2, 
        'xǁSummarizationManagerǁ__init____mutmut_3': xǁSummarizationManagerǁ__init____mutmut_3, 
        'xǁSummarizationManagerǁ__init____mutmut_4': xǁSummarizationManagerǁ__init____mutmut_4, 
        'xǁSummarizationManagerǁ__init____mutmut_5': xǁSummarizationManagerǁ__init____mutmut_5, 
        'xǁSummarizationManagerǁ__init____mutmut_6': xǁSummarizationManagerǁ__init____mutmut_6, 
        'xǁSummarizationManagerǁ__init____mutmut_7': xǁSummarizationManagerǁ__init____mutmut_7, 
        'xǁSummarizationManagerǁ__init____mutmut_8': xǁSummarizationManagerǁ__init____mutmut_8, 
        'xǁSummarizationManagerǁ__init____mutmut_9': xǁSummarizationManagerǁ__init____mutmut_9, 
        'xǁSummarizationManagerǁ__init____mutmut_10': xǁSummarizationManagerǁ__init____mutmut_10, 
        'xǁSummarizationManagerǁ__init____mutmut_11': xǁSummarizationManagerǁ__init____mutmut_11, 
        'xǁSummarizationManagerǁ__init____mutmut_12': xǁSummarizationManagerǁ__init____mutmut_12, 
        'xǁSummarizationManagerǁ__init____mutmut_13': xǁSummarizationManagerǁ__init____mutmut_13, 
        'xǁSummarizationManagerǁ__init____mutmut_14': xǁSummarizationManagerǁ__init____mutmut_14, 
        'xǁSummarizationManagerǁ__init____mutmut_15': xǁSummarizationManagerǁ__init____mutmut_15, 
        'xǁSummarizationManagerǁ__init____mutmut_16': xǁSummarizationManagerǁ__init____mutmut_16, 
        'xǁSummarizationManagerǁ__init____mutmut_17': xǁSummarizationManagerǁ__init____mutmut_17
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSummarizationManagerǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁSummarizationManagerǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁSummarizationManagerǁ__init____mutmut_orig)
    xǁSummarizationManagerǁ__init____mutmut_orig.__name__ = 'xǁSummarizationManagerǁ__init__'
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_orig(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_1(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_2(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return True  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_3(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary > self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_4(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(None)
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_5(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return False
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_6(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = None
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_7(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(None, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_8(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, None)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_9(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_10(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, )
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_11(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = None
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_12(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens * self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_13(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage > self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_14(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                None
            )
            return True
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_15(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return False
        
        return False
    
    def xǁSummarizationManagerǁshould_summarize__mutmut_16(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int
    ) -> bool:
        """
        Check if summarization should be triggered.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            True if summarization should be triggered
        """
        # Check if auto-trigger is enabled (disabled by default)
        from ..config import config
        if not config.summarization.auto_trigger_enabled:
            return False  # Manual summarization only
        
        # Check turn-based trigger
        if turns_since_last_summary >= self.trigger_turns:
            logger.debug(f"Trigger: turn count ({turns_since_last_summary} >= {self.trigger_turns})")
            return True
        
        # Check token-based trigger (estimate actual prompt size that will be sent)
        estimated_tokens = self._estimate_actual_prompt_tokens(session_id, messages)
        
        # Threshold is percentage of context window
        token_usage = estimated_tokens / self.context_window_size
        
        if token_usage >= self.trigger_token_threshold:
            logger.debug(
                f"Trigger: token usage ({token_usage:.3f} >= {self.trigger_token_threshold})"
            )
            return True
        
        return True
    
    xǁSummarizationManagerǁshould_summarize__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSummarizationManagerǁshould_summarize__mutmut_1': xǁSummarizationManagerǁshould_summarize__mutmut_1, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_2': xǁSummarizationManagerǁshould_summarize__mutmut_2, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_3': xǁSummarizationManagerǁshould_summarize__mutmut_3, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_4': xǁSummarizationManagerǁshould_summarize__mutmut_4, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_5': xǁSummarizationManagerǁshould_summarize__mutmut_5, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_6': xǁSummarizationManagerǁshould_summarize__mutmut_6, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_7': xǁSummarizationManagerǁshould_summarize__mutmut_7, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_8': xǁSummarizationManagerǁshould_summarize__mutmut_8, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_9': xǁSummarizationManagerǁshould_summarize__mutmut_9, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_10': xǁSummarizationManagerǁshould_summarize__mutmut_10, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_11': xǁSummarizationManagerǁshould_summarize__mutmut_11, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_12': xǁSummarizationManagerǁshould_summarize__mutmut_12, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_13': xǁSummarizationManagerǁshould_summarize__mutmut_13, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_14': xǁSummarizationManagerǁshould_summarize__mutmut_14, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_15': xǁSummarizationManagerǁshould_summarize__mutmut_15, 
        'xǁSummarizationManagerǁshould_summarize__mutmut_16': xǁSummarizationManagerǁshould_summarize__mutmut_16
    }
    
    def should_summarize(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSummarizationManagerǁshould_summarize__mutmut_orig"), object.__getattribute__(self, "xǁSummarizationManagerǁshould_summarize__mutmut_mutants"), args, kwargs, self)
        return result 
    
    should_summarize.__signature__ = _mutmut_signature(xǁSummarizationManagerǁshould_summarize__mutmut_orig)
    xǁSummarizationManagerǁshould_summarize__mutmut_orig.__name__ = 'xǁSummarizationManagerǁshould_summarize'
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_orig(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_1(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = None
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_2(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(None)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_3(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_4(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(None)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_5(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(None)
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_6(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(None)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_7(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = None
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_8(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 1
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_9(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = None
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_10(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=None,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_11(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=None
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_12(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_13(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_14(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = None
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_15(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(None, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_16(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, None, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_17(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_18(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_19(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, )
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_20(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens = estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_21(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens -= estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_22(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(None)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_23(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(None)
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_24(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = None
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_25(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_26(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = None  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_27(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 4  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_28(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = ""
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_29(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages or messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_30(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get(None) == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_31(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[1].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_32(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("XXroleXX") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_33(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("ROLE") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_34(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") != "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_35(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "XXsystemXX":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_36(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "SYSTEM":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_37(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = None
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_38(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[1]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_39(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = None
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_40(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get(None) != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_41(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("XXroleXX") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_42(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("ROLE") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_43(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") == "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_44(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "XXsystemXX"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_45(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "SYSTEM"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_46(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = None
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_47(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count / 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_48(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 5
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_49(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = None
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_50(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(None, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_51(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, None)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_52(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_53(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, )
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_54(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(1, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_55(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) + turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_56(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = None
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_57(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = None
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_58(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(None)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_59(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(None)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_60(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens = estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_61(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens -= estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_62(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(None)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_63(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(None)
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        return total_tokens
    
    def xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_64(
        self,
        session_id: str,
        messages: List[Dict[str, Any]]
    ) -> int:
        """
        Estimate tokens for the actual prompt that will be sent to LLM.
        
        When a summary exists, only filtered messages (summary + last K turns) are sent.
        This method estimates tokens based on what will actually be sent, not the full message history.
        
        Args:
            session_id: Session identifier
            messages: Full conversation messages
            
        Returns:
            Estimated token count for the actual prompt payload
        """
        # Check if summary exists for this session
        try:
            summary = self.summary_storage.load_session_summary(session_id)
            if not summary:
                # No summary exists yet - estimate from full messages
                return estimate_messages_tokens(messages)
        except Exception as e:
            logger.debug(f"Error checking for summary, using full messages: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(messages)
        
        # Summary exists - estimate from filtered payload (summary + last K turns)
        total_tokens = 0
        
        # 1. Estimate tokens for summary context
        try:
            prompt_builder = PromptBuilder(
                summary_storage=self.summary_storage,
                last_turns_count=config.summarization.last_turns_count
            )
            summary_context = prompt_builder.build_context(session_id, messages, system_prompt=None)
            if summary_context:
                total_tokens += estimate_tokens(summary_context)
        except Exception as e:
            logger.debug(f"Error estimating summary context tokens: {e}")
        
        # 2. Estimate tokens for filtered messages (last K turns)
        # Use same logic as ConversationSession._get_filtered_messages()
        try:
            last_turns_count = config.summarization.last_turns_count
            if not isinstance(last_turns_count, int):
                last_turns_count = 3  # Default
            
            # Get system message (if exists) - this will be included
            system_message = None
            if messages and messages[0].get("role") == "system":
                system_message = messages[0]
            
            # Get last K turns (non-system messages)
            non_system_messages = [m for m in messages if m.get("role") != "system"]
            
            # Estimate: each turn is typically 2-4 messages (user + assistant + possibly tool calls)
            # Take last current_turns*4 messages as a conservative estimate
            turns_to_keep = last_turns_count * 4
            start_idx = max(0, len(non_system_messages) - turns_to_keep)
            last_turns = non_system_messages[start_idx:]
            
            # Build filtered message list: system message + last turns
            filtered_messages = []
            if system_message:
                filtered_messages.append(system_message)
            filtered_messages.extend(last_turns)
            
            # Estimate tokens for filtered messages
            total_tokens += estimate_messages_tokens(filtered_messages)
            
        except Exception as e:
            logger.debug(f"Error estimating filtered message tokens: {e}")
            # Fall back to full message estimation
            return estimate_messages_tokens(None)
        
        return total_tokens
    
    xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_1': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_1, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_2': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_2, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_3': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_3, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_4': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_4, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_5': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_5, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_6': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_6, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_7': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_7, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_8': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_8, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_9': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_9, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_10': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_10, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_11': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_11, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_12': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_12, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_13': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_13, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_14': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_14, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_15': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_15, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_16': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_16, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_17': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_17, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_18': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_18, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_19': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_19, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_20': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_20, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_21': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_21, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_22': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_22, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_23': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_23, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_24': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_24, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_25': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_25, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_26': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_26, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_27': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_27, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_28': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_28, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_29': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_29, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_30': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_30, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_31': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_31, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_32': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_32, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_33': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_33, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_34': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_34, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_35': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_35, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_36': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_36, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_37': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_37, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_38': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_38, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_39': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_39, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_40': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_40, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_41': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_41, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_42': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_42, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_43': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_43, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_44': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_44, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_45': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_45, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_46': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_46, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_47': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_47, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_48': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_48, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_49': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_49, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_50': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_50, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_51': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_51, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_52': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_52, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_53': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_53, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_54': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_54, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_55': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_55, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_56': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_56, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_57': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_57, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_58': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_58, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_59': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_59, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_60': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_60, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_61': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_61, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_62': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_62, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_63': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_63, 
        'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_64': xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_64
    }
    
    def _estimate_actual_prompt_tokens(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_orig"), object.__getattribute__(self, "xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _estimate_actual_prompt_tokens.__signature__ = _mutmut_signature(xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_orig)
    xǁSummarizationManagerǁ_estimate_actual_prompt_tokens__mutmut_orig.__name__ = 'xǁSummarizationManagerǁ_estimate_actual_prompt_tokens'
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_orig(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 0
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if not self.should_summarize(session_id, messages, turns_since_last_summary):
            return None
        
        return self.summarize(session_id)
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_1(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 1
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if not self.should_summarize(session_id, messages, turns_since_last_summary):
            return None
        
        return self.summarize(session_id)
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_2(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 0
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if self.should_summarize(session_id, messages, turns_since_last_summary):
            return None
        
        return self.summarize(session_id)
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_3(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 0
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if not self.should_summarize(None, messages, turns_since_last_summary):
            return None
        
        return self.summarize(session_id)
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_4(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 0
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if not self.should_summarize(session_id, None, turns_since_last_summary):
            return None
        
        return self.summarize(session_id)
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_5(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 0
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if not self.should_summarize(session_id, messages, None):
            return None
        
        return self.summarize(session_id)
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_6(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 0
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if not self.should_summarize(messages, turns_since_last_summary):
            return None
        
        return self.summarize(session_id)
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_7(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 0
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if not self.should_summarize(session_id, turns_since_last_summary):
            return None
        
        return self.summarize(session_id)
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_8(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 0
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if not self.should_summarize(session_id, messages, ):
            return None
        
        return self.summarize(session_id)
    
    def xǁSummarizationManagerǁmaybe_summarize__mutmut_9(
        self,
        session_id: str,
        messages: List[Dict[str, Any]],
        turns_since_last_summary: int = 0
    ) -> Optional[SessionSummary]:
        """
        Conditionally summarize if triggers are met.
        
        Args:
            session_id: Session identifier
            messages: Current conversation messages
            turns_since_last_summary: Number of turns since last summarization
            
        Returns:
            Updated SessionSummary if summarization occurred, None otherwise
        """
        if not self.should_summarize(session_id, messages, turns_since_last_summary):
            return None
        
        return self.summarize(None)
    
    xǁSummarizationManagerǁmaybe_summarize__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSummarizationManagerǁmaybe_summarize__mutmut_1': xǁSummarizationManagerǁmaybe_summarize__mutmut_1, 
        'xǁSummarizationManagerǁmaybe_summarize__mutmut_2': xǁSummarizationManagerǁmaybe_summarize__mutmut_2, 
        'xǁSummarizationManagerǁmaybe_summarize__mutmut_3': xǁSummarizationManagerǁmaybe_summarize__mutmut_3, 
        'xǁSummarizationManagerǁmaybe_summarize__mutmut_4': xǁSummarizationManagerǁmaybe_summarize__mutmut_4, 
        'xǁSummarizationManagerǁmaybe_summarize__mutmut_5': xǁSummarizationManagerǁmaybe_summarize__mutmut_5, 
        'xǁSummarizationManagerǁmaybe_summarize__mutmut_6': xǁSummarizationManagerǁmaybe_summarize__mutmut_6, 
        'xǁSummarizationManagerǁmaybe_summarize__mutmut_7': xǁSummarizationManagerǁmaybe_summarize__mutmut_7, 
        'xǁSummarizationManagerǁmaybe_summarize__mutmut_8': xǁSummarizationManagerǁmaybe_summarize__mutmut_8, 
        'xǁSummarizationManagerǁmaybe_summarize__mutmut_9': xǁSummarizationManagerǁmaybe_summarize__mutmut_9
    }
    
    def maybe_summarize(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSummarizationManagerǁmaybe_summarize__mutmut_orig"), object.__getattribute__(self, "xǁSummarizationManagerǁmaybe_summarize__mutmut_mutants"), args, kwargs, self)
        return result 
    
    maybe_summarize.__signature__ = _mutmut_signature(xǁSummarizationManagerǁmaybe_summarize__mutmut_orig)
    xǁSummarizationManagerǁmaybe_summarize__mutmut_orig.__name__ = 'xǁSummarizationManagerǁmaybe_summarize'
    
    def xǁSummarizationManagerǁsummarize__mutmut_orig(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_1(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = None
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_2(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(None)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_3(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary or previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_4(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = None
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_5(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = None
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_6(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(None, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_7(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, None)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_8(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_9(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, )
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_10(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = None
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_11(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(None)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_12(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_13(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(None)
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_14(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                None
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_15(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'XXnoneXX'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_16(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'NONE'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_17(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = None
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_18(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(None, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_19(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, None, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_20(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, None)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_21(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_22(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_23(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, )
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_24(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_25(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(None)
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_26(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = None
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_27(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                None,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_28(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                None,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_29(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                None,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_30(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_31(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_32(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_33(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_34(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_35(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get(None) if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_36(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[+1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_37(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-2].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_38(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("XXevent_idXX") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_39(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("EVENT_ID") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_40(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_41(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(None)
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_42(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = None
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_43(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(None, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_44(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, None, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_45(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, None)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_46(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_47(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_48(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, )
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_49(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["XXhas_driftXX"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_50(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["HAS_DRIFT"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_51(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    None
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_52(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = None
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_53(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(None, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_54(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, None)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_55(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_56(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, )
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_57(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_58(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["XXmeets_thresholdXX"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_59(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["MEETS_THRESHOLD"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_60(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    None
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_61(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['XXcompression_ratioXX']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_62(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['COMPRESSION_RATIO']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_63(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(None, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_64(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, None)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_65(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_66(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, )
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_67(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result or "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_68(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "XXextractedXX" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_69(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "EXTRACTED" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_70(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" not in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_71(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "XXtasks_addedXX" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_72(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "TASKS_ADDED" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_73(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" not in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_74(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["XXextractedXX"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_75(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["EXTRACTED"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_76(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(None, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_77(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, None)
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_78(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_79(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, )
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_80(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["XXextractedXX"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_81(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["EXTRACTED"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_82(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(None)
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_83(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(None, exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_84(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=None)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_85(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(exc_info=True)
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_86(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", )
            return None
    
    def xǁSummarizationManagerǁsummarize__mutmut_87(self, session_id: str) -> Optional[SessionSummary]:
        """
        Summarize events for a session.
        
        Args:
            session_id: Session identifier
            
        Returns:
            Updated SessionSummary if successful, None otherwise
        """
        try:
            # Load previous summary
            previous_summary = self.summary_storage.load_session_summary(session_id)
            
            # Determine delta window (events to summarize)
            if previous_summary and previous_summary.header.last_summarized_event_id:
                last_event_id = previous_summary.header.last_summarized_event_id
                events = self.event_logger.get_events_after(session_id, last_event_id)
            else:
                # First summarization - summarize all events
                events = self.event_logger.get_events(session_id)
            
            if not events:
                logger.debug(f"No new events to summarize for session {session_id}")
                return previous_summary
            
            logger.info(
                f"Summarizing {len(events)} events for session {session_id} "
                f"(after event {previous_summary.header.last_summarized_event_id if previous_summary else 'none'})"
            )
            
            # Call summarizer
            result = self.summarizer.summarize_delta(session_id, events, previous_summary)
            if not result:
                logger.warning(f"Summarizer returned no result for session {session_id}")
                return previous_summary
            
            # Merge results into summary
            updated_summary = self._merge_summary_updates(
                session_id,
                previous_summary,
                result,
                events[-1].get("event_id") if events else None
            )
            
            if not updated_summary:
                logger.warning(f"Failed to merge summary updates for session {session_id}")
                return previous_summary
            
            # Validate summary for drift
            drift_result = self.validator.detect_drift(session_id, updated_summary, events)
            if drift_result["has_drift"]:
                logger.warning(
                    f"Drift detected in summary for session {session_id}: "
                    f"{len(drift_result['missing_event_ids'])} missing event IDs"
                )
            
            # Check compression ratio
            compression_result = self.validator.check_compression_ratio(events, updated_summary)
            if not compression_result["meets_threshold"]:
                logger.debug(
                    f"Low compression ratio for session {session_id}: "
                    f"{compression_result['compression_ratio']:.2f}x"
                )
            
            # Save updated summary
            self.summary_storage.save_session_summary(session_id, updated_summary)
            
            # Update tasks if any
            if "extracted" in result and "tasks_added" in result["extracted"]:
                self._update_tasks(session_id, result["extracted"])
            
            logger.info(f"Successfully summarized session {session_id}, revision {updated_summary.header.revision}")
            return updated_summary
            
        except Exception as e:
            logger.error(f"Error during summarization for session {session_id}: {e}", exc_info=False)
            return None
    
    xǁSummarizationManagerǁsummarize__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSummarizationManagerǁsummarize__mutmut_1': xǁSummarizationManagerǁsummarize__mutmut_1, 
        'xǁSummarizationManagerǁsummarize__mutmut_2': xǁSummarizationManagerǁsummarize__mutmut_2, 
        'xǁSummarizationManagerǁsummarize__mutmut_3': xǁSummarizationManagerǁsummarize__mutmut_3, 
        'xǁSummarizationManagerǁsummarize__mutmut_4': xǁSummarizationManagerǁsummarize__mutmut_4, 
        'xǁSummarizationManagerǁsummarize__mutmut_5': xǁSummarizationManagerǁsummarize__mutmut_5, 
        'xǁSummarizationManagerǁsummarize__mutmut_6': xǁSummarizationManagerǁsummarize__mutmut_6, 
        'xǁSummarizationManagerǁsummarize__mutmut_7': xǁSummarizationManagerǁsummarize__mutmut_7, 
        'xǁSummarizationManagerǁsummarize__mutmut_8': xǁSummarizationManagerǁsummarize__mutmut_8, 
        'xǁSummarizationManagerǁsummarize__mutmut_9': xǁSummarizationManagerǁsummarize__mutmut_9, 
        'xǁSummarizationManagerǁsummarize__mutmut_10': xǁSummarizationManagerǁsummarize__mutmut_10, 
        'xǁSummarizationManagerǁsummarize__mutmut_11': xǁSummarizationManagerǁsummarize__mutmut_11, 
        'xǁSummarizationManagerǁsummarize__mutmut_69': xǁSummarizationManagerǁsummarize__mutmut_69, 
        'xǁSummarizationManagerǁsummarize__mutmut_70': xǁSummarizationManagerǁsummarize__mutmut_70, 
        'xǁSummarizationManagerǁsummarize__mutmut_71': xǁSummarizationManagerǁsummarize__mutmut_71, 
        'xǁSummarizationManagerǁsummarize__mutmut_72': xǁSummarizationManagerǁsummarize__mutmut_72, 
        'xǁSummarizationManagerǁsummarize__mutmut_73': xǁSummarizationManagerǁsummarize__mutmut_73, 
        'xǁSummarizationManagerǁsummarize__mutmut_74': xǁSummarizationManagerǁsummarize__mutmut_74, 
        'xǁSummarizationManagerǁsummarize__mutmut_75': xǁSummarizationManagerǁsummarize__mutmut_75, 
        'xǁSummarizationManagerǁsummarize__mutmut_76': xǁSummarizationManagerǁsummarize__mutmut_76, 
        'xǁSummarizationManagerǁsummarize__mutmut_77': xǁSummarizationManagerǁsummarize__mutmut_77, 
        'xǁSummarizationManagerǁsummarize__mutmut_78': xǁSummarizationManagerǁsummarize__mutmut_78, 
        'xǁSummarizationManagerǁsummarize__mutmut_79': xǁSummarizationManagerǁsummarize__mutmut_79, 
        'xǁSummarizationManagerǁsummarize__mutmut_80': xǁSummarizationManagerǁsummarize__mutmut_80, 
        'xǁSummarizationManagerǁsummarize__mutmut_81': xǁSummarizationManagerǁsummarize__mutmut_81, 
        'xǁSummarizationManagerǁsummarize__mutmut_82': xǁSummarizationManagerǁsummarize__mutmut_82, 
        'xǁSummarizationManagerǁsummarize__mutmut_83': xǁSummarizationManagerǁsummarize__mutmut_83, 
        'xǁSummarizationManagerǁsummarize__mutmut_84': xǁSummarizationManagerǁsummarize__mutmut_84, 
        'xǁSummarizationManagerǁsummarize__mutmut_85': xǁSummarizationManagerǁsummarize__mutmut_85, 
        'xǁSummarizationManagerǁsummarize__mutmut_86': xǁSummarizationManagerǁsummarize__mutmut_86, 
        'xǁSummarizationManagerǁsummarize__mutmut_87': xǁSummarizationManagerǁsummarize__mutmut_87
    }
    
    def summarize(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSummarizationManagerǁsummarize__mutmut_orig"), object.__getattribute__(self, "xǁSummarizationManagerǁsummarize__mutmut_mutants"), args, kwargs, self)
        return result 
    
    summarize.__signature__ = _mutmut_signature(xǁSummarizationManagerǁsummarize__mutmut_orig)
    xǁSummarizationManagerǁsummarize__mutmut_orig.__name__ = 'xǁSummarizationManagerǁsummarize'
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_orig(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_1(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = None
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_2(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get(None, {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_3(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", None)
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_4(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get({})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_5(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", )
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_6(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("XXsummary_patchXX", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_7(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("SUMMARY_PATCH", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_8(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = None
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_9(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get(None, {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_10(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", None)
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_11(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get({})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_12(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", )
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_13(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("XXextractedXX", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_14(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("EXTRACTED", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_15(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = None
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_16(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get(None, {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_17(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", None)
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_18(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get({})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_19(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", )
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_20(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("XXbookkeepingXX", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_21(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("BOOKKEEPING", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_22(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = None
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_23(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get(None, [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_24(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", None)
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_25(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get([])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_26(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", )
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_27(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("XXconflictsXX", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_28(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("CONFLICTS", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_29(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = None
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_30(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get(None)
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_31(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("XXnew_last_summarized_event_idXX")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_32(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("NEW_LAST_SUMMARIZED_EVENT_ID")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_33(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_34(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = None
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_35(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id and ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_36(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or "XXXX"
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_37(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = None
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_38(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = None
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_39(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(None).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_40(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = None
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_41(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id and ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_42(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id and header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_43(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or "XXXX"
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_44(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision = 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_45(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision -= 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_46(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 2
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_47(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count = 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_48(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count -= 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_49(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 2
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_50(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = None
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_51(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=None,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_52(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=None,
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_53(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=None,
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_54(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=None,
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_55(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=None,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_56(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=None,
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_57(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=None  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_58(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_59(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_60(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_61(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_62(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_63(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_64(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_65(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(None).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_66(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(None).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_67(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id and "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_68(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "XXXX",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_69(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=1,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_70(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=1  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_71(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = None
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_72(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = None
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_73(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "XXcurrent_goalXX" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_74(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "CURRENT_GOAL" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_75(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" not in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_76(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = None
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_77(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["XXcurrent_goalXX"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_78(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["CURRENT_GOAL"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_79(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "XXwhat_we_builtXX" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_80(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "WHAT_WE_BUILT" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_81(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" not in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_82(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(None)
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_83(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["XXwhat_we_builtXX"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_84(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["WHAT_WE_BUILT"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_85(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = None
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_86(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[+20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_87(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-21:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_88(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "XXopen_questionsXX" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_89(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "OPEN_QUESTIONS" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_90(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" not in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_91(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(None)
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_92(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["XXopen_questionsXX"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_93(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["OPEN_QUESTIONS"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_94(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = None
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_95(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[+20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_96(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-21:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_97(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "XXconstraintsXX" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_98(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "CONSTRAINTS" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_99(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" not in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_100(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(None)
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_101(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["XXconstraintsXX"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_102(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["CONSTRAINTS"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_103(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = None
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_104(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[+20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_105(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-21:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_106(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "XXnext_stepsXX" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_107(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "NEXT_STEPS" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_108(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" not in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_109(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(None)
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_110(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["XXnext_stepsXX"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_111(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["NEXT_STEPS"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_112(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = None
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_113(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get(None, []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_114(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", None):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_115(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get([]):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_116(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", ):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_117(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("XXtasks_updatedXX", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_118(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("TASKS_UPDATED", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_119(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) or task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_120(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get(None) == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_121(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("XXstatusXX") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_122(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("STATUS") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_123(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") != "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_124(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "XXcompletedXX":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_125(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "COMPLETED":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_126(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = None
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_127(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get(None)
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_128(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("XXidXX")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_129(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("ID")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_130(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(None)
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_131(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.upper())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_132(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = None
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_133(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_134(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        break
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_135(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = None
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_136(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.upper()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_137(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = None
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_138(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        None
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_139(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower and step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_140(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id not in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_141(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower not in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_142(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_143(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(None)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_144(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = None  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_145(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[+20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_146(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-21:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_147(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = None
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_148(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = None
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_149(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get(None, []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_150(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", None):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_151(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get([]):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_152(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", ):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_153(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("XXfacts_addedXX", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_154(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("FACTS_ADDED", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_155(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(None)
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_156(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=None,
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_157(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=None
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_158(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_159(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_160(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get(None, ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_161(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", None),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_162(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get(""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_163(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_164(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("XXtextXX", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_165(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("TEXT", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_166(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", "XXXX"),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_167(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get(None, [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_168(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", None)
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_169(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get([])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_170(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", )
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_171(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("XXevent_idsXX", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_172(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("EVENT_IDS", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_173(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get(None, []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_174(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", None):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_175(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get([]):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_176(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", ):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_177(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("XXdecisions_addedXX", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_178(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("DECISIONS_ADDED", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_179(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(None)
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_180(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=None,
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_181(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=None
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_182(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_183(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_184(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get(None, ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_185(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", None),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_186(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get(""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_187(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_188(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("XXtextXX", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_189(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("TEXT", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_190(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", "XXXX"),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_191(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get(None, [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_192(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", None)
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_193(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get([])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_194(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", )
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_195(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("XXevent_idsXX", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_196(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("EVENT_IDS", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_197(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = None  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_198(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 51  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_199(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) >= max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_200(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = None
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_201(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[+max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_202(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(None)
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_203(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = None
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_204(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "XXcurrent_goalXX" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_205(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "CURRENT_GOAL" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_206(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" not in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_207(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = None
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_208(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get(None, [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_209(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", None)
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_210(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get([])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_211(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", )
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_212(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("XXfacts_addedXX", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_213(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("FACTS_ADDED", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_214(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = None
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_215(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get(None, "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_216(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", None) for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_217(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_218(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", ) for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_219(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("XXconfidenceXX", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_220(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("CONFIDENCE", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_221(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "XXmediumXX") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_222(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "MEDIUM") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_223(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(None):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_224(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c != "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_225(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "XXhighXX" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_226(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "HIGH" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_227(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = None
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_228(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "XXhighXX"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_229(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "HIGH"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_230(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(None):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_231(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c != "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_232(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "XXmediumXX" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_233(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "MEDIUM" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_234(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = None
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_235(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "XXmediumXX"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_236(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "MEDIUM"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_237(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = None
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_238(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "XXlowXX"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_239(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "LOW"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_240(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = None
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_241(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=None,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_242(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=None,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_243(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=None,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_244(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=None
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_245(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_246(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_247(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_248(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_249(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(None, exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_250(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=None)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_251(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(exc_info=True)
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_252(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", )
            return None
    
    def xǁSummarizationManagerǁ_merge_summary_updates__mutmut_253(
        self,
        session_id: str,
        previous_summary: Optional[SessionSummary],
        result: Dict[str, Any],
        latest_event_id: Optional[str]
    ) -> Optional[SessionSummary]:
        """
        Merge summarization updates into existing or new summary.
        
        Args:
            session_id: Session identifier
            previous_summary: Previous summary (None for first summarization)
            result: Summarization result from LLM
            latest_event_id: Latest event ID that was summarized
            
        Returns:
            Merged SessionSummary
        """
        try:
            summary_patch = result.get("summary_patch", {})
            extracted = result.get("extracted", {})
            bookkeeping = result.get("bookkeeping", {})
            conflicts = result.get("conflicts", [])
            
            # Create or update header
            # Ensure we always have a valid event ID with proper fallback chain
            new_last_summarized_event_id = bookkeeping.get("new_last_summarized_event_id")
            if not new_last_summarized_event_id:
                # Fallback to latest_event_id from events that were summarized
                new_last_summarized_event_id = latest_event_id or ""
            
            if previous_summary:
                header = previous_summary.header
                header.last_updated_at = datetime.now(timezone.utc).isoformat()
                # Use new ID, or fall back to previous value if somehow empty
                header.last_summarized_event_id = new_last_summarized_event_id or header.last_summarized_event_id or ""
                header.revision += 1
                # Increment summarization cycle count for gradual pruning
                header.summarization_cycle_count += 1
            else:
                header = SummaryHeader(
                    session_id=session_id,
                    created_at=datetime.now(timezone.utc).isoformat(),
                    last_updated_at=datetime.now(timezone.utc).isoformat(),
                    last_summarized_event_id=new_last_summarized_event_id or "",
                    revision=0,
                    scope=f"Session {session_id}",
                    summarization_cycle_count=0  # First summarization, cycle count starts at 0
                )
            
            # Merge summary blocks
            if previous_summary:
                blocks = previous_summary.summary_blocks
            else:
                blocks = SummaryBlocks()
            
            # Update blocks with patch
            if "current_goal" in summary_patch:
                blocks.current_goal = summary_patch["current_goal"]
            if "what_we_built" in summary_patch:
                # Append new items
                blocks.what_we_built.extend(summary_patch["what_we_built"])
                # Keep only last 20 items
                blocks.what_we_built = blocks.what_we_built[-20:]
            if "open_questions" in summary_patch:
                blocks.open_questions.extend(summary_patch["open_questions"])
                blocks.open_questions = blocks.open_questions[-20:]
            if "constraints" in summary_patch:
                blocks.constraints.extend(summary_patch["constraints"])
                blocks.constraints = blocks.constraints[-20:]
            if "next_steps" in summary_patch:
                blocks.next_steps.extend(summary_patch["next_steps"])
                
                # Filter out completed tasks (defensive check)
                completed_task_ids = set()
                for task_update in extracted.get("tasks_updated", []):
                    if isinstance(task_update, dict) and task_update.get("status") == "completed":
                        task_id = task_update.get("id")
                        if task_id:
                            completed_task_ids.add(task_id.lower())
                
                # Filter next_steps: remove items that match completed task patterns
                # Simple heuristic: if next_step contains task_id or vice versa
                filtered_next_steps = []
                for step in blocks.next_steps:
                    if not isinstance(step, str):
                        # Skip non-string items
                        continue
                    step_lower = step.lower()
                    is_completed = any(
                        task_id in step_lower or step_lower in task_id 
                        for task_id in completed_task_ids
                    )
                    if not is_completed:
                        filtered_next_steps.append(step)
                
                blocks.next_steps = filtered_next_steps[-20:]  # Keep last 20
            
            # Build evidence list
            evidence = []
            if previous_summary:
                evidence = previous_summary.evidence.copy()
            
            # Add evidence for new claims
            for fact in extracted.get("facts_added", []):
                evidence.append(EvidenceItem(
                    claim=fact.get("text", ""),
                    event_ids=fact.get("event_ids", [])
                ))
            
            for decision in extracted.get("decisions_added", []):
                evidence.append(EvidenceItem(
                    claim=decision.get("text", ""),
                    event_ids=decision.get("event_ids", [])
                ))
            
            # Cap evidence list to prevent unbounded growth
            max_evidence_items = 50  # Keep most recent 50 items
            if len(evidence) > max_evidence_items:
                evidence = evidence[-max_evidence_items:]
                logger.debug(f"Capped evidence list to {max_evidence_items} items (kept most recent)")
            
            # Update confidence (simplified - could be more sophisticated)
            confidence = ConfidenceLevel()
            if "current_goal" in summary_patch:
                # Determine confidence from extracted facts
                facts = extracted.get("facts_added", [])
                if facts:
                    confidences = [f.get("confidence", "medium") for f in facts]
                    if any(c == "high" for c in confidences):
                        confidence.current_goal = "high"
                    elif any(c == "medium" for c in confidences):
                        confidence.current_goal = "medium"
                    else:
                        confidence.current_goal = "low"
            
            # Create updated summary
            summary = SessionSummary(
                header=header,
                summary_blocks=blocks,
                evidence=evidence,
                confidence=confidence
            )
            
            return summary
            
        except Exception as e:
            logger.error(f"Error merging summary updates: {e}", exc_info=False)
            return None
    
    xǁSummarizationManagerǁ_merge_summary_updates__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_1': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_1, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_2': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_2, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_3': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_3, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_4': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_4, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_5': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_5, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_6': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_6, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_7': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_7, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_8': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_8, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_9': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_9, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_10': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_10, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_11': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_11, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_12': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_12, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_13': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_13, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_14': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_14, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_15': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_15, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_16': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_16, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_17': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_17, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_18': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_18, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_19': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_19, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_20': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_20, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_21': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_21, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_22': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_22, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_23': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_23, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_24': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_24, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut
xǁSummarizationManagerǁ_merge_summary_updates__mutmut_217, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_218': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_218, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_219': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_219, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_220': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_220, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_221': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_221, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_222': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_222, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_223': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_223, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_224': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_224, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_225': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_225, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_226': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_226, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_227': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_227, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_228': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_228, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_229': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_229, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_230': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_230, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_231': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_231, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_232': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_232, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_233': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_233, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_234': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_234, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_235': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_235, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_236': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_236, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_237': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_237, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_238': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_238, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_239': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_239, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_240': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_240, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_241': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_241, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_242': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_242, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_243': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_243, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_244': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_244, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_245': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_245, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_246': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_246, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_247': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_247, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_248': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_248, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_249': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_249, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_250': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_250, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_251': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_251, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_252': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_252, 
        'xǁSummarizationManagerǁ_merge_summary_updates__mutmut_253': xǁSummarizationManagerǁ_merge_summary_updates__mutmut_253
    }
    
    def _merge_summary_updates(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSummarizationManagerǁ_merge_summary_updates__mutmut_orig"), object.__getattribute__(self, "xǁSummarizationManagerǁ_merge_summary_updates__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _merge_summary_updates.__signature__ = _mutmut_signature(xǁSummarizationManagerǁ_merge_summary_updates__mutmut_orig)
    xǁSummarizationManagerǁ_merge_summary_updates__mutmut_orig.__name__ = 'xǁSummarizationManagerǁ_merge_summary_updates'
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_orig(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_1(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = None
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_2(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(None)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_3(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get(None, []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_4(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", None):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_5(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get([]):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_6(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", ):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_7(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("XXtasks_addedXX", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_8(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("TASKS_ADDED", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_9(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = None
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_10(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=None,
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_11(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=None,
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_12(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status=None,
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_13(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=None,
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_14(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=None
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_15(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_16(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_17(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_18(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_19(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_20(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get(None, f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_21(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", None),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_22(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get(f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_23(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", ),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_24(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("XXidXX", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_25(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("ID", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_26(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get(None, ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_27(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", None),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_28(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get(""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_29(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_30(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("XXdescriptionXX", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_31(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("DESCRIPTION", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_32(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", "XXXX"),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_33(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="XXpendingXX",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_34(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="PENDING",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_35(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get(None, []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_36(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", None),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_37(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get([]),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_38(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", ),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_39(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("XXevent_idsXX", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_40(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("EVENT_IDS", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_41(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(None).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_42(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(None)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_43(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get(None, []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_44(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", None):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_45(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get([]):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_46(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", ):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_47(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("XXtasks_updatedXX", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_48(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("TASKS_UPDATED", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_49(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = None
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_50(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get(None)
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_51(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("XXidXX")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_52(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("ID")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_53(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = None
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_54(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get(None, "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_55(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", None)
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_56(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_57(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", )
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_58(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("XXstatusXX", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_59(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("STATUS", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_60(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "XXpendingXX")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_61(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "PENDING")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_62(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id != task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_63(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = None
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_64(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get(None):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_65(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("XXevent_idsXX"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_66(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("EVENT_IDS"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_67(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(None)
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_68(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["XXevent_idsXX"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_69(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["EVENT_IDS"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_70(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        return
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_71(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(None, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_72(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, None)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_73(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_74(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, )
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_75(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(None, exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_76(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=None)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_77(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(exc_info=True)
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_78(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", )
    
    def xǁSummarizationManagerǁ_update_tasks__mutmut_79(self, session_id: str, extracted: Dict[str, Any]) -> None:
        """Update tasks file with new tasks from summarization."""
        try:
            tasks_file = self.summary_storage.load_tasks(session_id)
            
            # Add new tasks
            for task_data in extracted.get("tasks_added", []):
                task = Task(
                    id=task_data.get("id", f"task_{len(tasks_file.tasks)}"),
                    description=task_data.get("description", ""),
                    status="pending",
                    evidence_event_ids=task_data.get("event_ids", []),
                    created_at=datetime.now(timezone.utc).isoformat()
                )
                tasks_file.tasks.append(task)
            
            # Update existing tasks
            for task_update in extracted.get("tasks_updated", []):
                task_id = task_update.get("id")
                new_status = task_update.get("status", "pending")
                
                for task in tasks_file.tasks:
                    if task.id == task_id:
                        task.status = new_status
                        if task_update.get("event_ids"):
                            task.evidence_event_ids.extend(task_update["event_ids"])
                        break
            
            # Save updated tasks
            self.summary_storage.save_tasks(session_id, tasks_file)
            
        except Exception as e:
            logger.warning(f"Error updating tasks: {e}", exc_info=False)
    
    xǁSummarizationManagerǁ_update_tasks__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSummarizationManagerǁ_update_tasks__mutmut_1': xǁSummarizationManagerǁ_update_tasks__mutmut_1, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_2': xǁSummarizationManagerǁ_update_tasks__mutmut_2, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_3': xǁSummarizationManagerǁ_update_tasks__mutmut_3, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_4': xǁSummarizationManagerǁ_update_tasks__mutmut_4, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_5': xǁSummarizationManagerǁ_update_tasks__mutmut_5, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_6': xǁSummarizationManagerǁ_update_tasks__mutmut_6, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_7': xǁSummarizationManagerǁ_update_tasks__mutmut_7, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_8': xǁSummarizationManagerǁ_update_tasks__mutmut_8, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_9': xǁSummarizationManagerǁ_update_tasks__mutmut_9, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_10': xǁSummarizationManagerǁ_update_tasks__mutmut_10, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_11': xǁSummarizationManagerǁ_update_tasks__mutmut_11, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_12': xǁSummarizationManagerǁ_update_tasks__mutmut_12, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_13': xǁSummarizationManagerǁ_update_tasks__mutmut_13, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_14': xǁSummarizationManagerǁ_update_tasks__mutmut_14, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_15': xǁSummarizationManagerǁ_update_tasks__mutmut_15, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_16': xǁSummarizationManagerǁ_update_tasks__mutmut_16, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_17': xǁSummarizationManagerǁ_update_tasks__mutmut_17, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_18': xǁSummarizationManagerǁ_update_tasks__mutmut_18, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_19': xǁSummarizationManagerǁ_update_tasks__mutmut_19, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_20': xǁSummarizationManagerǁ_update_tasks__mutmut_20, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_21': xǁSummarizationManagerǁ_update_tasks__mutmut_21, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_22': xǁSummarizationManagerǁ_update_tasks__mutmut_22, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_23': xǁSummarizationManagerǁ_update_tasks__mutmut_23, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_24': xǁSummarizationManagerǁ_update_tasks__mutmut_24, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_25': xǁSummarizationManagerǁ_update_tasks__mutmut_25, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_26': xǁSummarizationManagerǁ_update_tasks__mutmut_26, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_27': xǁSummarizationManagerǁ_update_tasks__mutmut_27, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_28': xǁSummarizationManagerǁ_update_tasks__mutmut_28, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_29': xǁSummarizationManagerǁ_update_tasks__mutmut_29, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_30': xǁSummarizationManagerǁ_update_tasks__mutmut_30, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_31': xǁSummarizationManagerǁ_update_tasks__mutmut_31, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_32': xǁSummarizationManagerǁ_update_tasks__mutmut_32, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_33': xǁSummarizationManagerǁ_update_tasks__mutmut_33, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_34': xǁSummarizationManagerǁ_update_tasks__mutmut_34, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_35': xǁSummarizationManagerǁ_update_tasks__mutmut_35, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_36': xǁSummarizationManagerǁ_update_tasks__mutmut_36, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_37': xǁSummarizationManagerǁ_update_tasks__mutmut_37, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut_38': xǁSummarizationManagerǁ_update_tasks__mutmut_38, 
        'xǁSummarizationManagerǁ_update_tasks__mutmut