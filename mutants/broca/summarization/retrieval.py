"""
Simple keyword-based retrieval index for memory cards.

Provides basic keyword search over summary notes. Can use LLM for semantic search.
"""

from __future__ import annotations

import logging
import json
import re
import time
from typing import List, Dict, Any, Optional, TYPE_CHECKING
from datetime import datetime

if TYPE_CHECKING:
    from ..llm import LLMClient

logger = logging.getLogger(__name__)

# Import logger utility
try:
    from ..reasoning.llm_pattern_logger import get_logger, initialize_logger
    HAS_LOGGER = True
except ImportError:
    HAS_LOGGER = False
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class RetrievalIndex:
    """
    Simple keyword-based retrieval index for memory cards.
    
    Stores and retrieves snippets based on keyword matching.
    Can be extended with embeddings later.
    """
    
    def xǁRetrievalIndexǁ__init____mutmut_orig(self, llm_client: Optional["LLMClient"] = None) -> None:
        """
        Initialize retrieval index.
        
        Args:
            llm_client: Optional LLM client for semantic search
        """
        self._memory_cards: List[Dict[str, Any]] = []
        self.llm_client = llm_client
        self._llm_enabled = llm_client is not None
        logger.debug(f"Initialized RetrievalIndex (llm_semantic_search={self._llm_enabled})")
    
    def xǁRetrievalIndexǁ__init____mutmut_1(self, llm_client: Optional["LLMClient"] = None) -> None:
        """
        Initialize retrieval index.
        
        Args:
            llm_client: Optional LLM client for semantic search
        """
        self._memory_cards: List[Dict[str, Any]] = None
        self.llm_client = llm_client
        self._llm_enabled = llm_client is not None
        logger.debug(f"Initialized RetrievalIndex (llm_semantic_search={self._llm_enabled})")
    
    def xǁRetrievalIndexǁ__init____mutmut_2(self, llm_client: Optional["LLMClient"] = None) -> None:
        """
        Initialize retrieval index.
        
        Args:
            llm_client: Optional LLM client for semantic search
        """
        self._memory_cards: List[Dict[str, Any]] = []
        self.llm_client = None
        self._llm_enabled = llm_client is not None
        logger.debug(f"Initialized RetrievalIndex (llm_semantic_search={self._llm_enabled})")
    
    def xǁRetrievalIndexǁ__init____mutmut_3(self, llm_client: Optional["LLMClient"] = None) -> None:
        """
        Initialize retrieval index.
        
        Args:
            llm_client: Optional LLM client for semantic search
        """
        self._memory_cards: List[Dict[str, Any]] = []
        self.llm_client = llm_client
        self._llm_enabled = None
        logger.debug(f"Initialized RetrievalIndex (llm_semantic_search={self._llm_enabled})")
    
    def xǁRetrievalIndexǁ__init____mutmut_4(self, llm_client: Optional["LLMClient"] = None) -> None:
        """
        Initialize retrieval index.
        
        Args:
            llm_client: Optional LLM client for semantic search
        """
        self._memory_cards: List[Dict[str, Any]] = []
        self.llm_client = llm_client
        self._llm_enabled = llm_client is None
        logger.debug(f"Initialized RetrievalIndex (llm_semantic_search={self._llm_enabled})")
    
    def xǁRetrievalIndexǁ__init____mutmut_5(self, llm_client: Optional["LLMClient"] = None) -> None:
        """
        Initialize retrieval index.
        
        Args:
            llm_client: Optional LLM client for semantic search
        """
        self._memory_cards: List[Dict[str, Any]] = []
        self.llm_client = llm_client
        self._llm_enabled = llm_client is not None
        logger.debug(None)
    
    xǁRetrievalIndexǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRetrievalIndexǁ__init____mutmut_1': xǁRetrievalIndexǁ__init____mutmut_1, 
        'xǁRetrievalIndexǁ__init____mutmut_2': xǁRetrievalIndexǁ__init____mutmut_2, 
        'xǁRetrievalIndexǁ__init____mutmut_3': xǁRetrievalIndexǁ__init____mutmut_3, 
        'xǁRetrievalIndexǁ__init____mutmut_4': xǁRetrievalIndexǁ__init____mutmut_4, 
        'xǁRetrievalIndexǁ__init____mutmut_5': xǁRetrievalIndexǁ__init____mutmut_5
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRetrievalIndexǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁRetrievalIndexǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁRetrievalIndexǁ__init____mutmut_orig)
    xǁRetrievalIndexǁ__init____mutmut_orig.__name__ = 'xǁRetrievalIndexǁ__init__'
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_orig(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_1(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = None
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_2(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:9]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_3(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = None
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_4(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "XXidXX": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_5(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "ID": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_6(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "XXtextXX": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_7(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "TEXT": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_8(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "XXtagsXX": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_9(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "TAGS": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_10(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags and [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_11(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "XXevent_idsXX": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_12(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "EVENT_IDS": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_13(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids and [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_14(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "XXtimestampXX": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_15(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "TIMESTAMP": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_16(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp and datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_17(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(None)
        logger.debug(f"Added memory card {card_id}")
        return card_id
    
    def xǁRetrievalIndexǁadd_memory_card__mutmut_18(
        self,
        text: str,
        tags: List[str] = None,
        event_ids: List[str] = None,
        timestamp: Optional[str] = None
    ) -> str:
        """
        Add a memory card to the index.
        
        Args:
            text: Memory card text content
            tags: Optional list of tags
            event_ids: Optional list of associated event IDs
            timestamp: Optional timestamp (defaults to now)
            
        Returns:
            Memory card ID
        """
        import uuid
        
        card_id = f"card_{uuid.uuid4().hex[:8]}"
        
        card = {
            "id": card_id,
            "text": text,
            "tags": tags or [],
            "event_ids": event_ids or [],
            "timestamp": timestamp or datetime.utcnow().isoformat()
        }
        
        self._memory_cards.append(card)
        logger.debug(None)
        return card_id
    
    xǁRetrievalIndexǁadd_memory_card__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRetrievalIndexǁadd_memory_card__mutmut_1': xǁRetrievalIndexǁadd_memory_card__mutmut_1, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_2': xǁRetrievalIndexǁadd_memory_card__mutmut_2, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_3': xǁRetrievalIndexǁadd_memory_card__mutmut_3, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_4': xǁRetrievalIndexǁadd_memory_card__mutmut_4, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_5': xǁRetrievalIndexǁadd_memory_card__mutmut_5, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_6': xǁRetrievalIndexǁadd_memory_card__mutmut_6, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_7': xǁRetrievalIndexǁadd_memory_card__mutmut_7, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_8': xǁRetrievalIndexǁadd_memory_card__mutmut_8, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_9': xǁRetrievalIndexǁadd_memory_card__mutmut_9, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_10': xǁRetrievalIndexǁadd_memory_card__mutmut_10, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_11': xǁRetrievalIndexǁadd_memory_card__mutmut_11, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_12': xǁRetrievalIndexǁadd_memory_card__mutmut_12, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_13': xǁRetrievalIndexǁadd_memory_card__mutmut_13, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_14': xǁRetrievalIndexǁadd_memory_card__mutmut_14, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_15': xǁRetrievalIndexǁadd_memory_card__mutmut_15, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_16': xǁRetrievalIndexǁadd_memory_card__mutmut_16, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_17': xǁRetrievalIndexǁadd_memory_card__mutmut_17, 
        'xǁRetrievalIndexǁadd_memory_card__mutmut_18': xǁRetrievalIndexǁadd_memory_card__mutmut_18
    }
    
    def add_memory_card(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRetrievalIndexǁadd_memory_card__mutmut_orig"), object.__getattribute__(self, "xǁRetrievalIndexǁadd_memory_card__mutmut_mutants"), args, kwargs, self)
        return result 
    
    add_memory_card.__signature__ = _mutmut_signature(xǁRetrievalIndexǁadd_memory_card__mutmut_orig)
    xǁRetrievalIndexǁadd_memory_card__mutmut_orig.__name__ = 'xǁRetrievalIndexǁadd_memory_card'
    
    def xǁRetrievalIndexǁsearch__mutmut_orig(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_1(
        self,
        query: str,
        limit: int = 6,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_2(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = None
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_3(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = None
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_4(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(None)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_5(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.upper() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_6(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() not in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_7(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.upper() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_8(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get(None, [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_9(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", None)] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_10(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get([])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_11(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", )] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_12(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("XXtagsXX", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_13(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("TAGS", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_14(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_15(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled or self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_16(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(None, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_17(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, None, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_18(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, None)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_19(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_20(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_21(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, )
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_22(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(None)
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_23(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(None, candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_24(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, None, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_25(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, None)
    
    def xǁRetrievalIndexǁsearch__mutmut_26(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(candidate_cards, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_27(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, limit)
    
    def xǁRetrievalIndexǁsearch__mutmut_28(
        self,
        query: str,
        limit: int = 5,
        tags: Optional[List[str]] = None
    ) -> List[Dict[str, Any]]:
        """
        Search memory cards using semantic search (LLM) or keyword matching.
        
        Args:
            query: Search query
            limit: Maximum number of results
            tags: Optional tags to filter by
            
        Returns:
            List of matching memory cards, sorted by relevance
        """
        # Filter by tags first
        candidate_cards = self._memory_cards
        if tags:
            candidate_cards = [
                card for card in candidate_cards
                if any(tag.lower() in [t.lower() for t in card.get("tags", [])] for tag in tags)
            ]
        
        if not candidate_cards:
            return []
        
        # Use LLM semantic search if available
        if self._llm_enabled and self.llm_client:
            try:
                return self._search_with_llm(query, candidate_cards, limit)
            except Exception as e:
                logger.warning(f"LLM semantic search failed, using keyword fallback: {e}")
                # Fall through to keyword matching
        
        # Fallback to keyword matching
        return self._search_with_keywords(query, candidate_cards, )
    
    xǁRetrievalIndexǁsearch__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRetrievalIndexǁsearch__mutmut_1': xǁRetrievalIndexǁsearch__mutmut_1, 
        'xǁRetrievalIndexǁsearch__mutmut_2': xǁRetrievalIndexǁsearch__mutmut_2, 
        'xǁRetrievalIndexǁsearch__mutmut_3': xǁRetrievalIndexǁsearch__mutmut_3, 
        'xǁRetrievalIndexǁsearch__mutmut_4': xǁRetrievalIndexǁsearch__mutmut_4, 
        'xǁRetrievalIndexǁsearch__mutmut_5': xǁRetrievalIndexǁsearch__mutmut_5, 
        'xǁRetrievalIndexǁsearch__mutmut_6': xǁRetrievalIndexǁsearch__mutmut_6, 
        'xǁRetrievalIndexǁsearch__mutmut_7': xǁRetrievalIndexǁsearch__mutmut_7, 
        'xǁRetrievalIndexǁsearch__mutmut_8': xǁRetrievalIndexǁsearch__mutmut_8, 
        'xǁRetrievalIndexǁsearch__mutmut_9': xǁRetrievalIndexǁsearch__mutmut_9, 
        'xǁRetrievalIndexǁsearch__mutmut_10': xǁRetrievalIndexǁsearch__mutmut_10, 
        'xǁRetrievalIndexǁsearch__mutmut_11': xǁRetrievalIndexǁsearch__mutmut_11, 
        'xǁRetrievalIndexǁsearch__mutmut_12': xǁRetrievalIndexǁsearch__mutmut_12, 
        'xǁRetrievalIndexǁsearch__mutmut_13': xǁRetrievalIndexǁsearch__mutmut_13, 
        'xǁRetrievalIndexǁsearch__mutmut_14': xǁRetrievalIndexǁsearch__mutmut_14, 
        'xǁRetrievalIndexǁsearch__mutmut_15': xǁRetrievalIndexǁsearch__mutmut_15, 
        'xǁRetrievalIndexǁsearch__mutmut_16': xǁRetrievalIndexǁsearch__mutmut_16, 
        'xǁRetrievalIndexǁsearch__mutmut_17': xǁRetrievalIndexǁsearch__mutmut_17, 
        'xǁRetrievalIndexǁsearch__mutmut_18': xǁRetrievalIndexǁsearch__mutmut_18, 
        'xǁRetrievalIndexǁsearch__mutmut_19': xǁRetrievalIndexǁsearch__mutmut_19, 
        'xǁRetrievalIndexǁsearch__mutmut_20': xǁRetrievalIndexǁsearch__mutmut_20, 
        'xǁRetrievalIndexǁsearch__mutmut_21': xǁRetrievalIndexǁsearch__mutmut_21, 
        'xǁRetrievalIndexǁsearch__mutmut_22': xǁRetrievalIndexǁsearch__mutmut_22, 
        'xǁRetrievalIndexǁsearch__mutmut_23': xǁRetrievalIndexǁsearch__mutmut_23, 
        'xǁRetrievalIndexǁsearch__mutmut_24': xǁRetrievalIndexǁsearch__mutmut_24, 
        'xǁRetrievalIndexǁsearch__mutmut_25': xǁRetrievalIndexǁsearch__mutmut_25, 
        'xǁRetrievalIndexǁsearch__mutmut_26': xǁRetrievalIndexǁsearch__mutmut_26, 
        'xǁRetrievalIndexǁsearch__mutmut_27': xǁRetrievalIndexǁsearch__mutmut_27, 
        'xǁRetrievalIndexǁsearch__mutmut_28': xǁRetrievalIndexǁsearch__mutmut_28
    }
    
    def search(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRetrievalIndexǁsearch__mutmut_orig"), object.__getattribute__(self, "xǁRetrievalIndexǁsearch__mutmut_mutants"), args, kwargs, self)
        return result 
    
    search.__signature__ = _mutmut_signature(xǁRetrievalIndexǁsearch__mutmut_orig)
    xǁRetrievalIndexǁsearch__mutmut_orig.__name__ = 'xǁRetrievalIndexǁsearch'
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_orig(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_1(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = None
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_2(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = ""
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_3(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = None
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_4(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join(None)
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_5(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "XX\n\nXX".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_6(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get(None, '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_7(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', None)}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_8(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_9(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', )}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_10(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('XXtextXX', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_11(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('TEXT', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_12(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', 'XXXX')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_13(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(None)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_14(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = None
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_15(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = None
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_16(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "XXroleXX": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_17(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "ROLE": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_18(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "XXsystemXX",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_19(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "SYSTEM",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_20(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "XXcontentXX": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_21(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "CONTENT": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_22(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "XXYou are a semantic search engine. Rank documents by relevance to queries. Return JSON only.XX"
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_23(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "you are a semantic search engine. rank documents by relevance to queries. return json only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_24(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "YOU ARE A SEMANTIC SEARCH ENGINE. RANK DOCUMENTS BY RELEVANCE TO QUERIES. RETURN JSON ONLY."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_25(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "XXroleXX": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_26(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "ROLE": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_27(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "XXuserXX",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_28(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "USER",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_29(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "XXcontentXX": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_30(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "CONTENT": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_31(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = None
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_32(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(None, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_33(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=None)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_34(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_35(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, )
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_36(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=1.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_37(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = None
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_38(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(None)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_39(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_40(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError(None)
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_41(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("XXEmpty LLM responseXX")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_42(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("empty llm response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_43(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("EMPTY LLM RESPONSE")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_44(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = None
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_45(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "XX```jsonXX" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_46(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```JSON" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_47(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" not in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_48(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = None
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_49(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") - 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_50(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find(None) + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_51(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.rfind("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_52(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("XX```jsonXX") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_53(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```JSON") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_54(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 8
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_55(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = None
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_56(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find(None, json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_57(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", None)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_58(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find(json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_59(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", )
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_60(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.rfind("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_61(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("XX```XX", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_62(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = None
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_63(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "XX```XX" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_64(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" not in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_65(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = None
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_66(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") - 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_67(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find(None) + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_68(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.rfind("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_69(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("XX```XX") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_70(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 4
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_71(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = None
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_72(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find(None, json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_73(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", None)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_74(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find(json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_75(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", )
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_76(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.rfind("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_77(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("XX```XX", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_78(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = None
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_79(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = None
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_80(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(None, content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_81(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', None, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_82(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, None)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_83(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_84(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_85(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, )
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_86(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'XX\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]XX', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_87(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = None
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_88(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(None)
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_89(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = None
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_90(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(None)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_91(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = None
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_92(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = None
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_93(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = None
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_94(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get(None, -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_95(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", None)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_96(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get(-1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_97(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", )
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_98(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("XXcard_indexXX", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_99(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("CARD_INDEX", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_100(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", +1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_101(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -2)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_102(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 1 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_103(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 < card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_104(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx <= len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_105(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = None
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_106(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(None)
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_107(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get(None, 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_108(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", None))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_109(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get(0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_110(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", ))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_111(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("XXrelevance_scoreXX", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_112(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("RELEVANCE_SCORE", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_113(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 1.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_114(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append(None)
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_115(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(None)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_116(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=None, reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_117(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=None)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_118(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_119(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], )
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_120(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: None, reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_121(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[1], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_122(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=False)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_123(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = None
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_124(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = None
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_125(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) / 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_126(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() + start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_127(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1001
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_128(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = None
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_129(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is not None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_130(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = None
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_131(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=None,
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_132(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=None
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_133(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_134(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_135(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(None, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_136(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, None, 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_137(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', None),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_138(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr('llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_139(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_140(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', ),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_141(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'XXllm_pattern_log_pathXX', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_142(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'LLM_PATTERN_LOG_PATH', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_143(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'XXdata/llm_pattern_matching_log.csvXX'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_144(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'DATA/LLM_PATTERN_MATCHING_LOG.CSV'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_145(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(None, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_146(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, None, True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_147(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', None)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_148(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr('llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_149(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_150(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', )
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_151(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'XXllm_pattern_logging_enabledXX', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_152(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'LLM_PATTERN_LOGGING_ENABLED', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_153(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', False)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_154(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = ""
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_155(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = None
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_156(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(None, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_157(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, None, 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_158(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', None)
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_159(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr('model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_160(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_161(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', )
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_162(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'XXmodelXX', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_163(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'MODEL', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_164(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'XXunknownXX')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_165(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'UNKNOWN')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_166(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component=None,
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_167(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation=None,
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_168(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=None,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_169(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=None,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_170(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context=None,
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_171(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics=None,
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_172(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=None,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_173(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=None
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_174(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_175(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_176(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_177(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_178(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_179(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_180(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_181(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_182(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="XXRetrievalIndexXX",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_183(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="retrievalindex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_184(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RETRIEVALINDEX",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_185(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="XXsearch_with_llmXX",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_186(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="SEARCH_WITH_LLM",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_187(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"XXcard_countXX": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_188(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"CARD_COUNT": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_189(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "XXlimitXX": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_190(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "LIMIT": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_191(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "XXrelevance_scoresXX": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_192(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "RELEVANCE_SCORES": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_193(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "XXresults_countXX": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_194(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "RESULTS_COUNT": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_195(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = None
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_196(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(None)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_197(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = None
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_198(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) / 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_199(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() + start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_200(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1001
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_201(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(None, exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_202(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=None)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_203(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_204(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", )
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_205(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=False)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_206(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = None
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_207(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = None
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_208(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(None, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_209(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, None, 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_210(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', None)
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_211(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr('model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_212(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_213(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', )
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_214(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'XXmodelXX', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_215(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'MODEL', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_216(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'XXunknownXX')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_217(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'UNKNOWN')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_218(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component=None,
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_219(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation=None,
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_220(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=None,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_221(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=None,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_222(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context=None,
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_223(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=None,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_224(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=None
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_225(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_226(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_227(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_228(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_229(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_230(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_231(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_232(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="XXRetrievalIndexXX",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_233(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="retrievalindex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_234(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RETRIEVALINDEX",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_235(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="XXsearch_with_llmXX",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_236(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="SEARCH_WITH_LLM",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_237(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"XXcard_countXX": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_238(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"CARD_COUNT": len(candidate_cards), "limit": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_239(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "XXlimitXX": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    def xǁRetrievalIndexǁ_search_with_llm__mutmut_240(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Search using LLM semantic matching."""
        start_time = time.time()
        error_msg = None
        
        # Batch all cards into a single LLM call for efficiency
        cards_text = "\n\n".join([
            f"Card {i}: {card.get('text', '')}"
            for i, card in enumerate(candidate_cards)
        ])
        
        prompt = f"""Rank memory cards by semantic relevance to the query.

Query: {query}

Memory cards:
{cards_text}

For each card, determine:
- relevance_score: float 0.0-1.0 (how semantically relevant to the query)
- Consider semantic meaning, not just keyword matches

Return JSON array with one object per card:
[
  {{
    "card_index": 0,
    "relevance_score": 0.0-1.0
  }},
  ...
]

Sort by relevance_score descending. Return JSON only."""
        
        messages = [
            {
                "role": "system",
                "content": "You are a semantic search engine. Rank documents by relevance to queries. Return JSON only."
            },
            {
                "role": "user",
                "content": prompt
            }
        ]
        
        try:
            response = self.llm_client.chat(messages, temperature=0.0)
            content = self.llm_client.extract_assistant_content(response)
            
            if not content:
                raise ValueError("Empty LLM response")
            
            # Extract JSON array
            content = content.strip()
            if "```json" in content:
                json_start = content.find("```json") + 7
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            elif "```" in content:
                json_start = content.find("```") + 3
                json_end = content.find("```", json_start)
                content = content[json_start:json_end].strip()
            
            # Find JSON array
            json_match = re.search(r'\[[^\]]*(?:\{[^{}]*\}[^\]]*)*\]', content, re.DOTALL)
            if json_match:
                results = json.loads(json_match.group())
            else:
                results = json.loads(content)
            
            # Map results back to cards
            scored_cards = []
            relevance_scores = []
            for result in results:
                card_idx = result.get("card_index", -1)
                if 0 <= card_idx < len(candidate_cards):
                    score = float(result.get("relevance_score", 0.0))
                    scored_cards.append((score, candidate_cards[card_idx]))
                    relevance_scores.append(score)
            
            # Sort by score and return top N
            scored_cards.sort(key=lambda x: x[0], reverse=True)
            final_results = [card for score, card in scored_cards[:limit]]
            
            latency_ms = (time.time() - start_time) * 1000
            
            # Log the search
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance is None:
                    try:
                        from ..config import config
                        logger_instance = initialize_logger(
                            log_path=getattr(config.reasoning, 'llm_pattern_log_path', 'data/llm_pattern_matching_log.csv'),
                            enabled=getattr(config.reasoning, 'llm_pattern_logging_enabled', True)
                        )
                    except Exception:
                        logger_instance = None
                
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "limit": limit},
                        output_metrics={
                            "relevance_scores": relevance_scores[:limit],
                            "results_count": len(final_results)
                        },
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            return final_results
            
        except Exception as e:
            error_msg = str(e)
            latency_ms = (time.time() - start_time) * 1000
            logger.error(f"Error in LLM semantic search: {e}", exc_info=True)
            
            # Log error
            if HAS_LOGGER:
                logger_instance = get_logger()
                if logger_instance:
                    model = getattr(self.llm_client, 'model', 'unknown')
                    logger_instance.log(
                        component="RetrievalIndex",
                        operation="search_with_llm",
                        model=model,
                        input_text=query,
                        input_context={"card_count": len(candidate_cards), "LIMIT": limit},
                        latency_ms=latency_ms,
                        error=error_msg
                    )
            
            raise
    
    xǁRetrievalIndexǁ_search_with_llm__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRetrievalIndexǁ_search_with_llm__mutmut_1': xǁRetrievalIndexǁ_search_with_llm__mutmut_1, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_2': xǁRetrievalIndexǁ_search_with_llm__mutmut_2, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_3': xǁRetrievalIndexǁ_search_with_llm__mutmut_3, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_4': xǁRetrievalIndexǁ_search_with_llm__mutmut_4, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_5': xǁRetrievalIndexǁ_search_with_llm__mutmut_5, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_6': xǁRetrievalIndexǁ_search_with_llm__mutmut_6, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut
 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_234': xǁRetrievalIndexǁ_search_with_llm__mutmut_234, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_235': xǁRetrievalIndexǁ_search_with_llm__mutmut_235, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_236': xǁRetrievalIndexǁ_search_with_llm__mutmut_236, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_237': xǁRetrievalIndexǁ_search_with_llm__mutmut_237, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_238': xǁRetrievalIndexǁ_search_with_llm__mutmut_238, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_239': xǁRetrievalIndexǁ_search_with_llm__mutmut_239, 
        'xǁRetrievalIndexǁ_search_with_llm__mutmut_240': xǁRetrievalIndexǁ_search_with_llm__mutmut_240
    }
    
    def _search_with_llm(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRetrievalIndexǁ_search_with_llm__mutmut_orig"), object.__getattribute__(self, "xǁRetrievalIndexǁ_search_with_llm__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _search_with_llm.__signature__ = _mutmut_signature(xǁRetrievalIndexǁ_search_with_llm__mutmut_orig)
    xǁRetrievalIndexǁ_search_with_llm__mutmut_orig.__name__ = 'xǁRetrievalIndexǁ_search_with_llm'
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_orig(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_1(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = None
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_2(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.upper()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_3(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = None
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_4(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(None)
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_5(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = None
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_6(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = None
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_7(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").upper()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_8(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get(None, "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_9(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", None).lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_10(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_11(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", ).lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_12(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("XXtextXX", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_13(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("TEXT", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_14(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "XXXX").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_15(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = None
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_16(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(None)
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_17(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = None
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_18(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches != 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_19(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 1:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_20(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                break
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_21(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = None
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_22(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches * len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_23(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 1
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_24(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower not in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_25(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score = 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_26(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score -= 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_27(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 1.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_28(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append(None)
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_29(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=None, reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_30(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=None)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_31(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_32(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], )
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_33(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: None, reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_34(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[1], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_35(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=False)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_36(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = None
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:50]}")
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_37(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(None)
        return results
    
    def xǁRetrievalIndexǁ_search_with_keywords__mutmut_38(
        self,
        query: str,
        candidate_cards: List[Dict[str, Any]],
        limit: int
    ) -> List[Dict[str, Any]]:
        """Fallback keyword-based search."""
        query_lower = query.lower()
        query_words = set(query_lower.split())
        
        scored_cards = []
        
        for card in candidate_cards:
            # Score by keyword matches
            text_lower = card.get("text", "").lower()
            text_words = set(text_lower.split())
            
            # Count matching words
            matches = len(query_words & text_words)
            if matches == 0:
                continue
            
            # Score: number of matches / total query words
            score = matches / len(query_words) if query_words else 0
            
            # Boost score if query is a substring of text
            if query_lower in text_lower:
                score += 0.5
            
            scored_cards.append((score, card))
        
        # Sort by score (descending) and return top N
        scored_cards.sort(key=lambda x: x[0], reverse=True)
        results = [card for score, card in scored_cards[:limit]]
        
        logger.debug(f"Retrieved {len(results)} memory cards for query: {query[:51]}")
        return results
    
    xǁRetrievalIndexǁ_search_with_keywords__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRetrievalIndexǁ_search_with_keywords__mutmut_1': xǁRetrievalIndexǁ_search_with_keywords__mutmut_1, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_2': xǁRetrievalIndexǁ_search_with_keywords__mutmut_2, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_3': xǁRetrievalIndexǁ_search_with_keywords__mutmut_3, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_4': xǁRetrievalIndexǁ_search_with_keywords__mutmut_4, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_5': xǁRetrievalIndexǁ_search_with_keywords__mutmut_5, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_6': xǁRetrievalIndexǁ_search_with_keywords__mutmut_6, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_7': xǁRetrievalIndexǁ_search_with_keywords__mutmut_7, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_8': xǁRetrievalIndexǁ_search_with_keywords__mutmut_8, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_9': xǁRetrievalIndexǁ_search_with_keywords__mutmut_9, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_10': xǁRetrievalIndexǁ_search_with_keywords__mutmut_10, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_11': xǁRetrievalIndexǁ_search_with_keywords__mutmut_11, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_12': xǁRetrievalIndexǁ_search_with_keywords__mutmut_12, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_13': xǁRetrievalIndexǁ_search_with_keywords__mutmut_13, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_14': xǁRetrievalIndexǁ_search_with_keywords__mutmut_14, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_15': xǁRetrievalIndexǁ_search_with_keywords__mutmut_15, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_16': xǁRetrievalIndexǁ_search_with_keywords__mutmut_16, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_17': xǁRetrievalIndexǁ_search_with_keywords__mutmut_17, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_18': xǁRetrievalIndexǁ_search_with_keywords__mutmut_18, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_19': xǁRetrievalIndexǁ_search_with_keywords__mutmut_19, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_20': xǁRetrievalIndexǁ_search_with_keywords__mutmut_20, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_21': xǁRetrievalIndexǁ_search_with_keywords__mutmut_21, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_22': xǁRetrievalIndexǁ_search_with_keywords__mutmut_22, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_23': xǁRetrievalIndexǁ_search_with_keywords__mutmut_23, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_24': xǁRetrievalIndexǁ_search_with_keywords__mutmut_24, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_25': xǁRetrievalIndexǁ_search_with_keywords__mutmut_25, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_26': xǁRetrievalIndexǁ_search_with_keywords__mutmut_26, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_27': xǁRetrievalIndexǁ_search_with_keywords__mutmut_27, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_28': xǁRetrievalIndexǁ_search_with_keywords__mutmut_28, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_29': xǁRetrievalIndexǁ_search_with_keywords__mutmut_29, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_30': xǁRetrievalIndexǁ_search_with_keywords__mutmut_30, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_31': xǁRetrievalIndexǁ_search_with_keywords__mutmut_31, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_32': xǁRetrievalIndexǁ_search_with_keywords__mutmut_32, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_33': xǁRetrievalIndexǁ_search_with_keywords__mutmut_33, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_34': xǁRetrievalIndexǁ_search_with_keywords__mutmut_34, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_35': xǁRetrievalIndexǁ_search_with_keywords__mutmut_35, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_36': xǁRetrievalIndexǁ_search_with_keywords__mutmut_36, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_37': xǁRetrievalIndexǁ_search_with_keywords__mutmut_37, 
        'xǁRetrievalIndexǁ_search_with_keywords__mutmut_38': xǁRetrievalIndexǁ_search_with_keywords__mutmut_38
    }
    
    def _search_with_keywords(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRetrievalIndexǁ_search_with_keywords__mutmut_orig"), object.__getattribute__(self, "xǁRetrievalIndexǁ_search_with_keywords__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _search_with_keywords.__signature__ = _mutmut_signature(xǁRetrievalIndexǁ_search_with_keywords__mutmut_orig)
    xǁRetrievalIndexǁ_search_with_keywords__mutmut_orig.__name__ = 'xǁRetrievalIndexǁ_search_with_keywords'
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_orig(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = [
            card for card in self._memory_cards
            if any(eid in event_id_set for eid in card.get("event_ids", []))
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_1(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = None
        results = [
            card for card in self._memory_cards
            if any(eid in event_id_set for eid in card.get("event_ids", []))
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_2(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(None)
        results = [
            card for card in self._memory_cards
            if any(eid in event_id_set for eid in card.get("event_ids", []))
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_3(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = None
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_4(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = [
            card for card in self._memory_cards
            if any(None)
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_5(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = [
            card for card in self._memory_cards
            if any(eid not in event_id_set for eid in card.get("event_ids", []))
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_6(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = [
            card for card in self._memory_cards
            if any(eid in event_id_set for eid in card.get(None, []))
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_7(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = [
            card for card in self._memory_cards
            if any(eid in event_id_set for eid in card.get("event_ids", None))
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_8(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = [
            card for card in self._memory_cards
            if any(eid in event_id_set for eid in card.get([]))
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_9(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = [
            card for card in self._memory_cards
            if any(eid in event_id_set for eid in card.get("event_ids", ))
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_10(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = [
            card for card in self._memory_cards
            if any(eid in event_id_set for eid in card.get("XXevent_idsXX", []))
        ]
        return results
    
    def xǁRetrievalIndexǁget_by_event_ids__mutmut_11(self, event_ids: List[str]) -> List[Dict[str, Any]]:
        """
        Get memory cards associated with specific event IDs.
        
        Args:
            event_ids: List of event IDs
            
        Returns:
            List of matching memory cards
        """
        event_id_set = set(event_ids)
        results = [
            card for card in self._memory_cards
            if any(eid in event_id_set for eid in card.get("EVENT_IDS", []))
        ]
        return results
    
    xǁRetrievalIndexǁget_by_event_ids__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRetrievalIndexǁget_by_event_ids__mutmut_1': xǁRetrievalIndexǁget_by_event_ids__mutmut_1, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_2': xǁRetrievalIndexǁget_by_event_ids__mutmut_2, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_3': xǁRetrievalIndexǁget_by_event_ids__mutmut_3, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_4': xǁRetrievalIndexǁget_by_event_ids__mutmut_4, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_5': xǁRetrievalIndexǁget_by_event_ids__mutmut_5, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_6': xǁRetrievalIndexǁget_by_event_ids__mutmut_6, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_7': xǁRetrievalIndexǁget_by_event_ids__mutmut_7, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_8': xǁRetrievalIndexǁget_by_event_ids__mutmut_8, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_9': xǁRetrievalIndexǁget_by_event_ids__mutmut_9, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_10': xǁRetrievalIndexǁget_by_event_ids__mutmut_10, 
        'xǁRetrievalIndexǁget_by_event_ids__mutmut_11': xǁRetrievalIndexǁget_by_event_ids__mutmut_11
    }
    
    def get_by_event_ids(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRetrievalIndexǁget_by_event_ids__mutmut_orig"), object.__getattribute__(self, "xǁRetrievalIndexǁget_by_event_ids__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_by_event_ids.__signature__ = _mutmut_signature(xǁRetrievalIndexǁget_by_event_ids__mutmut_orig)
    xǁRetrievalIndexǁget_by_event_ids__mutmut_orig.__name__ = 'xǁRetrievalIndexǁget_by_event_ids'
    
    def xǁRetrievalIndexǁclear__mutmut_orig(self) -> None:
        """Clear all memory cards."""
        self._memory_cards.clear()
        logger.debug("Cleared retrieval index")
    
    def xǁRetrievalIndexǁclear__mutmut_1(self) -> None:
        """Clear all memory cards."""
        self._memory_cards.clear()
        logger.debug(None)
    
    def xǁRetrievalIndexǁclear__mutmut_2(self) -> None:
        """Clear all memory cards."""
        self._memory_cards.clear()
        logger.debug("XXCleared retrieval indexXX")
    
    def xǁRetrievalIndexǁclear__mutmut_3(self) -> None:
        """Clear all memory cards."""
        self._memory_cards.clear()
        logger.debug("cleared retrieval index")
    
    def xǁRetrievalIndexǁclear__mutmut_4(self) -> None:
        """Clear all memory cards."""
        self._memory_cards.clear()
        logger.debug("CLEARED RETRIEVAL INDEX")
    
    xǁRetrievalIndexǁclear__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRetrievalIndexǁclear__mutmut_1': xǁRetrievalIndexǁclear__mutmut_1, 
        'xǁRetrievalIndexǁclear__mutmut_2': xǁRetrievalIndexǁclear__mutmut_2, 
        'xǁRetrievalIndexǁclear__mutmut_3': xǁRetrievalIndexǁclear__mutmut_3, 
        'xǁRetrievalIndexǁclear__mutmut_4': xǁRetrievalIndexǁclear__mutmut_4
    }
    
    def clear(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRetrievalIndexǁclear__mutmut_orig"), object.__getattribute__(self, "xǁRetrievalIndexǁclear__mutmut_mutants"), args, kwargs, self)
        return result 
    
    clear.__signature__ = _mutmut_signature(xǁRetrievalIndexǁclear__mutmut_orig)
    xǁRetrievalIndexǁclear__mutmut_orig.__name__ = 'xǁRetrievalIndexǁclear'

