"""
Tool status display system for visual feedback during tool invocations.

Provides spinners, description formatting, and thread-safe display updates
for tool execution status in the REPL.
"""

from __future__ import annotations

import os
import sys
import threading
import time
import shutil
import re
from typing import Dict, Any, Optional, TYPE_CHECKING

if TYPE_CHECKING:
    from .color_profile import ColorManager

# ANSI escape code pattern
_ANSI_ESCAPE_PATTERN = re.compile(r'\x1B(?:[@-Z\\-_]|\[[0-?]*[ -/]*[@-~])')
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


def x__strip_ansi_codes__mutmut_orig(text: str) -> str:
    """
    Strip ANSI escape codes from text.
    
    Args:
        text: Text that may contain ANSI codes
        
    Returns:
        Text with ANSI codes removed
    """
    return _ANSI_ESCAPE_PATTERN.sub('', text)


def x__strip_ansi_codes__mutmut_1(text: str) -> str:
    """
    Strip ANSI escape codes from text.
    
    Args:
        text: Text that may contain ANSI codes
        
    Returns:
        Text with ANSI codes removed
    """
    return _ANSI_ESCAPE_PATTERN.sub(None, text)


def x__strip_ansi_codes__mutmut_2(text: str) -> str:
    """
    Strip ANSI escape codes from text.
    
    Args:
        text: Text that may contain ANSI codes
        
    Returns:
        Text with ANSI codes removed
    """
    return _ANSI_ESCAPE_PATTERN.sub('', None)


def x__strip_ansi_codes__mutmut_3(text: str) -> str:
    """
    Strip ANSI escape codes from text.
    
    Args:
        text: Text that may contain ANSI codes
        
    Returns:
        Text with ANSI codes removed
    """
    return _ANSI_ESCAPE_PATTERN.sub(text)


def x__strip_ansi_codes__mutmut_4(text: str) -> str:
    """
    Strip ANSI escape codes from text.
    
    Args:
        text: Text that may contain ANSI codes
        
    Returns:
        Text with ANSI codes removed
    """
    return _ANSI_ESCAPE_PATTERN.sub('', )


def x__strip_ansi_codes__mutmut_5(text: str) -> str:
    """
    Strip ANSI escape codes from text.
    
    Args:
        text: Text that may contain ANSI codes
        
    Returns:
        Text with ANSI codes removed
    """
    return _ANSI_ESCAPE_PATTERN.sub('XXXX', text)

x__strip_ansi_codes__mutmut_mutants : ClassVar[MutantDict] = {
'x__strip_ansi_codes__mutmut_1': x__strip_ansi_codes__mutmut_1, 
    'x__strip_ansi_codes__mutmut_2': x__strip_ansi_codes__mutmut_2, 
    'x__strip_ansi_codes__mutmut_3': x__strip_ansi_codes__mutmut_3, 
    'x__strip_ansi_codes__mutmut_4': x__strip_ansi_codes__mutmut_4, 
    'x__strip_ansi_codes__mutmut_5': x__strip_ansi_codes__mutmut_5
}

def _strip_ansi_codes(*args, **kwargs):
    result = _mutmut_trampoline(x__strip_ansi_codes__mutmut_orig, x__strip_ansi_codes__mutmut_mutants, args, kwargs)
    return result 

_strip_ansi_codes.__signature__ = _mutmut_signature(x__strip_ansi_codes__mutmut_orig)
x__strip_ansi_codes__mutmut_orig.__name__ = 'x__strip_ansi_codes'


def _get_visible_width(text: str) -> int:
    """
    Get the visible width of text, excluding ANSI escape codes.
    
    Args:
        text: Text that may contain ANSI codes
        
    Returns:
        Visible width in characters
    """
    return len(_strip_ansi_codes(text))


def x__get_terminal_width__mutmut_orig() -> int:
    """
    Get terminal width, with fallback to default.
    
    Returns:
        Terminal width in characters
    """
    try:
        return shutil.get_terminal_size().columns
    except (OSError, AttributeError):
        return 80  # Default fallback


def x__get_terminal_width__mutmut_1() -> int:
    """
    Get terminal width, with fallback to default.
    
    Returns:
        Terminal width in characters
    """
    try:
        return shutil.get_terminal_size().columns
    except (OSError, AttributeError):
        return 81  # Default fallback

x__get_terminal_width__mutmut_mutants : ClassVar[MutantDict] = {
'x__get_terminal_width__mutmut_1': x__get_terminal_width__mutmut_1
}

def _get_terminal_width(*args, **kwargs):
    result = _mutmut_trampoline(x__get_terminal_width__mutmut_orig, x__get_terminal_width__mutmut_mutants, args, kwargs)
    return result 

_get_terminal_width.__signature__ = _mutmut_signature(x__get_terminal_width__mutmut_orig)
x__get_terminal_width__mutmut_orig.__name__ = 'x__get_terminal_width'


class Spinner:
    """
    Terminal spinner for loading indicators.
    
    Provides animated spinner characters similar to npm's loading indicator.
    """
    
    # Spinner characters (braille patterns)
    SPINNER_CHARS = "⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏"
    
    def xǁSpinnerǁ__init____mutmut_orig(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_1(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = None
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_2(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is not None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_3(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = None
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_4(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(None, 'isatty') else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_5(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, None) else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_6(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr('isatty') else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_7(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, ) else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_8(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'XXisattyXX') else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_9(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'ISATTY') else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_10(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else True
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_11(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._index = None
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_12(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._index = 1
        self._lock = threading.Lock()
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_13(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._index = 0
        self._lock = None
        self._running = False
    
    def xǁSpinnerǁ__init____mutmut_14(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = None
    
    def xǁSpinnerǁ__init____mutmut_15(self, enabled: bool = None):
        """
        Initialize spinner.
        
        Args:
            enabled: Whether spinner is enabled (auto-detects TTY if None)
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._index = 0
        self._lock = threading.Lock()
        self._running = True
    
    xǁSpinnerǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSpinnerǁ__init____mutmut_1': xǁSpinnerǁ__init____mutmut_1, 
        'xǁSpinnerǁ__init____mutmut_2': xǁSpinnerǁ__init____mutmut_2, 
        'xǁSpinnerǁ__init____mutmut_3': xǁSpinnerǁ__init____mutmut_3, 
        'xǁSpinnerǁ__init____mutmut_4': xǁSpinnerǁ__init____mutmut_4, 
        'xǁSpinnerǁ__init____mutmut_5': xǁSpinnerǁ__init____mutmut_5, 
        'xǁSpinnerǁ__init____mutmut_6': xǁSpinnerǁ__init____mutmut_6, 
        'xǁSpinnerǁ__init____mutmut_7': xǁSpinnerǁ__init____mutmut_7, 
        'xǁSpinnerǁ__init____mutmut_8': xǁSpinnerǁ__init____mutmut_8, 
        'xǁSpinnerǁ__init____mutmut_9': xǁSpinnerǁ__init____mutmut_9, 
        'xǁSpinnerǁ__init____mutmut_10': xǁSpinnerǁ__init____mutmut_10, 
        'xǁSpinnerǁ__init____mutmut_11': xǁSpinnerǁ__init____mutmut_11, 
        'xǁSpinnerǁ__init____mutmut_12': xǁSpinnerǁ__init____mutmut_12, 
        'xǁSpinnerǁ__init____mutmut_13': xǁSpinnerǁ__init____mutmut_13, 
        'xǁSpinnerǁ__init____mutmut_14': xǁSpinnerǁ__init____mutmut_14, 
        'xǁSpinnerǁ__init____mutmut_15': xǁSpinnerǁ__init____mutmut_15
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSpinnerǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁSpinnerǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁSpinnerǁ__init____mutmut_orig)
    xǁSpinnerǁ__init____mutmut_orig.__name__ = 'xǁSpinnerǁ__init__'
    
    def xǁSpinnerǁ_get_next_char__mutmut_orig(self) -> str:
        """Get next spinner character, cycling through all characters."""
        with self._lock:
            char = self.SPINNER_CHARS[self._index]
            self._index = (self._index + 1) % len(self.SPINNER_CHARS)
            return char
    
    def xǁSpinnerǁ_get_next_char__mutmut_1(self) -> str:
        """Get next spinner character, cycling through all characters."""
        with self._lock:
            char = None
            self._index = (self._index + 1) % len(self.SPINNER_CHARS)
            return char
    
    def xǁSpinnerǁ_get_next_char__mutmut_2(self) -> str:
        """Get next spinner character, cycling through all characters."""
        with self._lock:
            char = self.SPINNER_CHARS[self._index]
            self._index = None
            return char
    
    def xǁSpinnerǁ_get_next_char__mutmut_3(self) -> str:
        """Get next spinner character, cycling through all characters."""
        with self._lock:
            char = self.SPINNER_CHARS[self._index]
            self._index = (self._index + 1) / len(self.SPINNER_CHARS)
            return char
    
    def xǁSpinnerǁ_get_next_char__mutmut_4(self) -> str:
        """Get next spinner character, cycling through all characters."""
        with self._lock:
            char = self.SPINNER_CHARS[self._index]
            self._index = (self._index - 1) % len(self.SPINNER_CHARS)
            return char
    
    def xǁSpinnerǁ_get_next_char__mutmut_5(self) -> str:
        """Get next spinner character, cycling through all characters."""
        with self._lock:
            char = self.SPINNER_CHARS[self._index]
            self._index = (self._index + 2) % len(self.SPINNER_CHARS)
            return char
    
    xǁSpinnerǁ_get_next_char__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSpinnerǁ_get_next_char__mutmut_1': xǁSpinnerǁ_get_next_char__mutmut_1, 
        'xǁSpinnerǁ_get_next_char__mutmut_2': xǁSpinnerǁ_get_next_char__mutmut_2, 
        'xǁSpinnerǁ_get_next_char__mutmut_3': xǁSpinnerǁ_get_next_char__mutmut_3, 
        'xǁSpinnerǁ_get_next_char__mutmut_4': xǁSpinnerǁ_get_next_char__mutmut_4, 
        'xǁSpinnerǁ_get_next_char__mutmut_5': xǁSpinnerǁ_get_next_char__mutmut_5
    }
    
    def _get_next_char(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSpinnerǁ_get_next_char__mutmut_orig"), object.__getattribute__(self, "xǁSpinnerǁ_get_next_char__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _get_next_char.__signature__ = _mutmut_signature(xǁSpinnerǁ_get_next_char__mutmut_orig)
    xǁSpinnerǁ_get_next_char__mutmut_orig.__name__ = 'xǁSpinnerǁ_get_next_char'
    
    def xǁSpinnerǁstart__mutmut_orig(self) -> None:
        """Start the spinner (no-op if disabled)."""
        if not self._enabled:
            return
        with self._lock:
            self._running = True
    
    def xǁSpinnerǁstart__mutmut_1(self) -> None:
        """Start the spinner (no-op if disabled)."""
        if self._enabled:
            return
        with self._lock:
            self._running = True
    
    def xǁSpinnerǁstart__mutmut_2(self) -> None:
        """Start the spinner (no-op if disabled)."""
        if not self._enabled:
            return
        with self._lock:
            self._running = None
    
    def xǁSpinnerǁstart__mutmut_3(self) -> None:
        """Start the spinner (no-op if disabled)."""
        if not self._enabled:
            return
        with self._lock:
            self._running = False
    
    xǁSpinnerǁstart__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSpinnerǁstart__mutmut_1': xǁSpinnerǁstart__mutmut_1, 
        'xǁSpinnerǁstart__mutmut_2': xǁSpinnerǁstart__mutmut_2, 
        'xǁSpinnerǁstart__mutmut_3': xǁSpinnerǁstart__mutmut_3
    }
    
    def start(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSpinnerǁstart__mutmut_orig"), object.__getattribute__(self, "xǁSpinnerǁstart__mutmut_mutants"), args, kwargs, self)
        return result 
    
    start.__signature__ = _mutmut_signature(xǁSpinnerǁstart__mutmut_orig)
    xǁSpinnerǁstart__mutmut_orig.__name__ = 'xǁSpinnerǁstart'
    
    def xǁSpinnerǁstop__mutmut_orig(self, success: bool = True) -> str:
        """
        Stop the spinner and return final indicator.
        
        Args:
            success: Whether operation succeeded
            
        Returns:
            Final indicator character (checkmark or cross)
        """
        with self._lock:
            self._running = False
        return "✓" if success else "✗"
    
    def xǁSpinnerǁstop__mutmut_1(self, success: bool = False) -> str:
        """
        Stop the spinner and return final indicator.
        
        Args:
            success: Whether operation succeeded
            
        Returns:
            Final indicator character (checkmark or cross)
        """
        with self._lock:
            self._running = False
        return "✓" if success else "✗"
    
    def xǁSpinnerǁstop__mutmut_2(self, success: bool = True) -> str:
        """
        Stop the spinner and return final indicator.
        
        Args:
            success: Whether operation succeeded
            
        Returns:
            Final indicator character (checkmark or cross)
        """
        with self._lock:
            self._running = None
        return "✓" if success else "✗"
    
    def xǁSpinnerǁstop__mutmut_3(self, success: bool = True) -> str:
        """
        Stop the spinner and return final indicator.
        
        Args:
            success: Whether operation succeeded
            
        Returns:
            Final indicator character (checkmark or cross)
        """
        with self._lock:
            self._running = True
        return "✓" if success else "✗"
    
    def xǁSpinnerǁstop__mutmut_4(self, success: bool = True) -> str:
        """
        Stop the spinner and return final indicator.
        
        Args:
            success: Whether operation succeeded
            
        Returns:
            Final indicator character (checkmark or cross)
        """
        with self._lock:
            self._running = False
        return "XX✓XX" if success else "✗"
    
    def xǁSpinnerǁstop__mutmut_5(self, success: bool = True) -> str:
        """
        Stop the spinner and return final indicator.
        
        Args:
            success: Whether operation succeeded
            
        Returns:
            Final indicator character (checkmark or cross)
        """
        with self._lock:
            self._running = False
        return "✓" if success else "XX✗XX"
    
    xǁSpinnerǁstop__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSpinnerǁstop__mutmut_1': xǁSpinnerǁstop__mutmut_1, 
        'xǁSpinnerǁstop__mutmut_2': xǁSpinnerǁstop__mutmut_2, 
        'xǁSpinnerǁstop__mutmut_3': xǁSpinnerǁstop__mutmut_3, 
        'xǁSpinnerǁstop__mutmut_4': xǁSpinnerǁstop__mutmut_4, 
        'xǁSpinnerǁstop__mutmut_5': xǁSpinnerǁstop__mutmut_5
    }
    
    def stop(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSpinnerǁstop__mutmut_orig"), object.__getattribute__(self, "xǁSpinnerǁstop__mutmut_mutants"), args, kwargs, self)
        return result 
    
    stop.__signature__ = _mutmut_signature(xǁSpinnerǁstop__mutmut_orig)
    xǁSpinnerǁstop__mutmut_orig.__name__ = 'xǁSpinnerǁstop'
    
    def xǁSpinnerǁupdate__mutmut_orig(self) -> str:
        """
        Get current spinner character for display.
        
        Returns:
            Current spinner character or empty string if disabled
        """
        if not self._enabled or not self._running:
            return ""
        return self._get_next_char()
    
    def xǁSpinnerǁupdate__mutmut_1(self) -> str:
        """
        Get current spinner character for display.
        
        Returns:
            Current spinner character or empty string if disabled
        """
        if not self._enabled and not self._running:
            return ""
        return self._get_next_char()
    
    def xǁSpinnerǁupdate__mutmut_2(self) -> str:
        """
        Get current spinner character for display.
        
        Returns:
            Current spinner character or empty string if disabled
        """
        if self._enabled or not self._running:
            return ""
        return self._get_next_char()
    
    def xǁSpinnerǁupdate__mutmut_3(self) -> str:
        """
        Get current spinner character for display.
        
        Returns:
            Current spinner character or empty string if disabled
        """
        if not self._enabled or self._running:
            return ""
        return self._get_next_char()
    
    def xǁSpinnerǁupdate__mutmut_4(self) -> str:
        """
        Get current spinner character for display.
        
        Returns:
            Current spinner character or empty string if disabled
        """
        if not self._enabled or not self._running:
            return "XXXX"
        return self._get_next_char()
    
    xǁSpinnerǁupdate__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSpinnerǁupdate__mutmut_1': xǁSpinnerǁupdate__mutmut_1, 
        'xǁSpinnerǁupdate__mutmut_2': xǁSpinnerǁupdate__mutmut_2, 
        'xǁSpinnerǁupdate__mutmut_3': xǁSpinnerǁupdate__mutmut_3, 
        'xǁSpinnerǁupdate__mutmut_4': xǁSpinnerǁupdate__mutmut_4
    }
    
    def update(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSpinnerǁupdate__mutmut_orig"), object.__getattribute__(self, "xǁSpinnerǁupdate__mutmut_mutants"), args, kwargs, self)
        return result 
    
    update.__signature__ = _mutmut_signature(xǁSpinnerǁupdate__mutmut_orig)
    xǁSpinnerǁupdate__mutmut_orig.__name__ = 'xǁSpinnerǁupdate'


class ToolDescriptionFormatter:
    """
    Formats tool names and arguments into human-readable descriptions.
    """
    
    def __init__(self):
        """Initialize formatter."""
        pass
    
    def xǁToolDescriptionFormatterǁformat__mutmut_orig(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_1(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name != "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_2(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "XXweb_searchXX":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_3(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "WEB_SEARCH":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_4(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = None
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_5(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get(None, "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_6(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", None)
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_7(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_8(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", )
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_9(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("XXqueryXX", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_10(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("QUERY", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_11(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "XXXX")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_12(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = None
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_13(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(None) if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_14(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(None).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_15(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split("XX,XX") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_16(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) >= 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_17(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 2:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_18(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" - ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_19(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(None)}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_20(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {'XX, XX'.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_21(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:4])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_22(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("XX...XX" if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_23(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) >= 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_24(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 4 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_25(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "XXXX")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_26(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "XXWeb searchingXX"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_27(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_28(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "WEB SEARCHING"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_29(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name != "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_30(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "XXterminalXX":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_31(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "TERMINAL":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_32(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = None
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_33(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get(None, "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_34(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", None)
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_35(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_36(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", )
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_37(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("XXcommandXX", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_38(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("COMMAND", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_39(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "XXXX")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_40(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = None
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_41(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(None)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_42(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if 'XX<<XX' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_43(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' not in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_44(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = None
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_45(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(None, cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_46(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', None)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_47(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_48(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', )
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_49(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'XX^([^<]*?)\s*<<XX', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_50(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = None
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_51(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(None).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_52(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(2).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_53(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "XXRunning terminal command: heredocXX"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_54(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_55(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "RUNNING TERMINAL COMMAND: HEREDOC"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_56(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) >= 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_57(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 51:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_58(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:48]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_59(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "XXRunning terminal commandXX"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_60(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_61(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "RUNNING TERMINAL COMMAND"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_62(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name != "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_63(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "XXstore_memoryXX":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_64(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "STORE_MEMORY":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_65(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = None
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_66(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get(None, "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_67(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", None)
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_68(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_69(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", )
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_70(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("XXcontentXX", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_71(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("CONTENT", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_72(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "XXXX")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_73(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = None
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_74(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(None)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_75(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) >= 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_76(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 51:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_77(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:48]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_78(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "XXStoring memoryXX"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_79(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_80(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "STORING MEMORY"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_81(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name != "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_82(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "XXretrieve_memoriesXX":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_83(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "RETRIEVE_MEMORIES":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_84(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = None
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_85(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get(None, "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_86(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", None)
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_87(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_88(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", )
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_89(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("XXqueryXX", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_90(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("QUERY", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_91(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "XXXX")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_92(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = None
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_93(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(None)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_94(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) >= 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_95(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 51:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_96(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:48]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_97(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "XXRetrieving memoriesXX"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_98(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_99(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "RETRIEVING MEMORIES"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_100(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name != "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_101(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "XXdelete_memoryXX":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_102(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "DELETE_MEMORY":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_103(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = None
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_104(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get(None, "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_105(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", None)
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_106(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_107(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", )
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_108(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("XXmemory_idXX", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_109(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("MEMORY_ID", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_110(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "XXXX")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_111(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "XXDeleting memoryXX"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_112(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_113(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "DELETING MEMORY"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_114(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name != "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_115(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "XXupdate_memoryXX":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_116(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "UPDATE_MEMORY":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_117(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = None
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_118(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get(None, "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_119(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", None)
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_120(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_121(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", )
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_122(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("XXmemory_idXX", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_123(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("MEMORY_ID", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_124(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "XXXX")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_125(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "XXUpdating memoryXX"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_126(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_127(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "UPDATING MEMORY"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_128(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name != "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_129(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "XXlink_memoriesXX":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_130(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "LINK_MEMORIES":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_131(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "XXLinking memoriesXX"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_132(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_133(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "LINKING MEMORIES"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_134(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name != "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_135(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "XXget_related_memoriesXX":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_136(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "GET_RELATED_MEMORIES":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_137(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = None
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_138(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get(None, "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_139(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", None)
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_140(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_141(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", )
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_142(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("XXmemory_idXX", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_143(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("MEMORY_ID", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_144(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "XXXX")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_145(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "XXGetting related memoriesXX"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_146(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_147(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "GETTING RELATED MEMORIES"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_148(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name != "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_149(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "XXmemory_graphXX":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_150(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "MEMORY_GRAPH":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_151(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = None
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_152(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get(None, [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_153(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", None)
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_154(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get([])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_155(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", )
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_156(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("XXmemory_idsXX", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_157(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("MEMORY_IDS", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_158(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = None
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_159(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get(None, 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_160(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", None)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_161(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get(2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_162(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", )
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_163(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("XXdepthXX", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_164(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("DEPTH", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_165(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 3)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_166(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name != "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_167(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "XXcriticXX":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_168(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "CRITIC":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_169(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "XXRunning critic checkXX"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_170(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_171(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "RUNNING CRITIC CHECK"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_172(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name != "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_173(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "XXquery_self_modelXX":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_174(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "QUERY_SELF_MODEL":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_175(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "XXQuerying self modelXX"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_176(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_177(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "QUERYING SELF MODEL"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_178(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name != "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_179(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "XXenvironment_accessXX":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_180(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "ENVIRONMENT_ACCESS":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_181(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "XXAccessing environmentXX"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_182(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_183(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "ACCESSING ENVIRONMENT"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_184(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = None
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_185(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 1
        if arg_count > 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_186(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count >= 0:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    def xǁToolDescriptionFormatterǁformat__mutmut_187(self, tool_name: str, arguments: Dict[str, Any]) -> str:
        """
        Format tool invocation into human-readable description.
        
        Args:
            tool_name: Name of the tool
            arguments: Tool arguments dictionary
            
        Returns:
            Human-readable description string
        """
        # Tool-specific formatting
        if tool_name == "web_search":
            query = arguments.get("query", "")
            if query:
                # Handle multiple queries (comma-separated)
                queries = [q.strip() for q in str(query).split(",") if q.strip()]
                if len(queries) > 1:
                    return f"Web searching {', '.join(queries[:3])}" + ("..." if len(queries) > 3 else "")
                else:
                    return f"Web searching {query}"
            return "Web searching"
        
        elif tool_name == "terminal":
            command = arguments.get("command", "")
            if command:
                # Handle heredoc commands specially - they can be very long
                cmd_str = str(command)
                # Check if it's a heredoc (contains <<)
                if '<<' in cmd_str:
                    # For heredocs, just show the command type, not the full content
                    # Extract the command before the heredoc marker
                    heredoc_match = re.search(r'^([^<]*?)\s*<<', cmd_str)
                    if heredoc_match:
                        base_cmd = heredoc_match.group(1).strip()
                        if base_cmd:
                            return f"Running terminal command: {base_cmd} << ..."
                    return "Running terminal command: heredoc"
                # For regular commands, truncate if too long
                if len(cmd_str) > 50:
                    return f"Running terminal command: {cmd_str[:47]}..."
                return f"Running terminal command: {cmd_str}"
            return "Running terminal command"
        
        elif tool_name == "store_memory":
            content = arguments.get("content", "")
            if content:
                content_str = str(content)
                if len(content_str) > 50:
                    return f"Storing memory: {content_str[:47]}..."
                return f"Storing memory: {content_str}"
            return "Storing memory"
        
        elif tool_name == "retrieve_memories":
            query = arguments.get("query", "")
            if query:
                query_str = str(query)
                if len(query_str) > 50:
                    return f"Retrieving memories: {query_str[:47]}..."
                return f"Retrieving memories: {query_str}"
            return "Retrieving memories"
        
        elif tool_name == "delete_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Deleting memory: {memory_id}"
            return "Deleting memory"
        
        elif tool_name == "update_memory":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Updating memory: {memory_id}"
            return "Updating memory"
        
        elif tool_name == "link_memories":
            return "Linking memories"
        
        elif tool_name == "get_related_memories":
            memory_id = arguments.get("memory_id", "")
            if memory_id:
                return f"Getting related memories: {memory_id}"
            return "Getting related memories"
        
        elif tool_name == "memory_graph":
            ids = arguments.get("memory_ids", [])
            depth = arguments.get("depth", 2)
            if ids:
                return f"Building memory graph from IDs {ids} (depth={depth})"
            return f"Building memory graph (depth={depth})"
        
        elif tool_name == "critic":
            return "Running critic check"
        
        elif tool_name == "query_self_model":
            return "Querying self model"
        
        elif tool_name == "environment_access":
            return "Accessing environment"
        
        # Generic fallback
        arg_count = len(arguments) if arguments else 0
        if arg_count > 1:
            return f"{tool_name} with {arg_count} argument(s)"
        return f"{tool_name}"
    
    xǁToolDescriptionFormatterǁformat__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolDescriptionFormatterǁformat__mutmut_1': xǁToolDescriptionFormatterǁformat__mutmut_1, 
        'xǁToolDescriptionFormatterǁformat__mutmut_2': xǁToolDescriptionFormatterǁformat__mutmut_2, 
        'xǁToolDescriptionFormatterǁformat__mutmut_3': xǁToolDescriptionFormatterǁformat__mutmut_3, 
        'xǁToolDescriptionFormatterǁformat__mutmut_4': xǁToolDescriptionFormatterǁformat__mutmut_4, 
        'xǁToolDescriptionFormatterǁformat__mutmut_5': xǁToolDescriptionFormatterǁformat__mutmut_5, 
        'xǁToolDescriptionFormatterǁformat__mutmut_6': xǁToolDescriptionFormatterǁformat__mutmut_6, 
        'xǁToolDescriptionFormatterǁformat__mutmut_7': xǁToolDescriptionFormatterǁformat__mutmut_7, 
        'xǁToolDescriptionFormatterǁformat__mutmut_8': xǁToolDescriptionFormatterǁformat__mutmut_8, 
        'xǁToolDescriptionFormatterǁformat__mutmut_9': xǁToolDescriptionFormatterǁformat__mutmut_9, 
        'xǁToolDescriptionFormatterǁformat__mutmut_178': xǁToolDescriptionFormatterǁformat__mutmut_178, 
        'xǁToolDescriptionFormatterǁformat__mutmut_179': xǁToolDescriptionFormatterǁformat__mutmut_179, 
        'xǁToolDescriptionFormatterǁformat__mutmut_180': xǁToolDescriptionFormatterǁformat__mutmut_180, 
        'xǁToolDescriptionFormatterǁformat__mutmut_181': xǁToolDescriptionFormatterǁformat__mutmut_181, 
        'xǁToolDescriptionFormatterǁformat__mutmut_182': xǁToolDescriptionFormatterǁformat__mutmut_182, 
        'xǁToolDescriptionFormatterǁformat__mutmut_183': xǁToolDescriptionFormatterǁformat__mutmut_183, 
        'xǁToolDescriptionFormatterǁformat__mutmut_184': xǁToolDescriptionFormatterǁformat__mutmut_184, 
        'xǁToolDescriptionFormatterǁformat__mutmut_185': xǁToolDescriptionFormatterǁformat__mutmut_185, 
        'xǁToolDescriptionFormatterǁformat__mutmut_186': xǁToolDescriptionFormatterǁformat__mutmut_186, 
        'xǁToolDescriptionFormatterǁformat__mutmut_187': xǁToolDescriptionFormatterǁformat__mutmut_187
    }
    
    def format(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolDescriptionFormatterǁformat__mutmut_orig"), object.__getattribute__(self, "xǁToolDescriptionFormatterǁformat__mutmut_mutants"), args, kwargs, self)
        return result 
    
    format.__signature__ = _mutmut_signature(xǁToolDescriptionFormatterǁformat__mutmut_orig)
    xǁToolDescriptionFormatterǁformat__mutmut_orig.__name__ = 'xǁToolDescriptionFormatterǁformat'


class ToolStatusDisplay:
    """
    Manages visual feedback for tool invocations.
    
    Provides thread-safe display of tool execution status with spinners
    and completion indicators.
    """
    
    def xǁToolStatusDisplayǁ__init____mutmut_orig(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_1(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = None
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_2(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is not None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_3(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = None
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_4(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(None, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_5(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, None) else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_6(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr('isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_7(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, ) else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_8(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'XXisattyXX') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_9(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'ISATTY') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_10(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else True
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_11(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = None
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_12(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = None
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_13(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = None
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_14(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = None
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_15(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = None
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_16(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = None
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_17(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = None
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_18(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = True
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_19(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = None
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = None
    
    def xǁToolStatusDisplayǁ__init____mutmut_20(self, enabled: bool = None, color_manager: Optional[Any] = None):
        """
        Initialize tool status display.
        
        Args:
            enabled: Whether display is enabled (auto-detects TTY if None)
            color_manager: Optional ColorManager for colorizing output
        """
        self._enabled = enabled
        if self._enabled is None:
            self._enabled = sys.stdout.isatty() if hasattr(sys.stdout, 'isatty') else False
        
        self._color_manager = color_manager
        self._lock = threading.Lock()
        self._active_spinners: Dict[str, Spinner] = {}
        self._active_descriptions: Dict[str, str] = {}
        self._spinner_threads: Dict[str, threading.Thread] = {}
        self._spinner_running: Dict[str, bool] = {}
        self._paused: bool = False
        # Track which keys have had their initial status printed
        # This helps distinguish initial prints from intermediate animation updates
        self._initial_status_printed: Dict[str, bool] = {}
        # Cache the output capture detection result
        self._output_captured: Optional[bool] = ""
    
    xǁToolStatusDisplayǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolStatusDisplayǁ__init____mutmut_1': xǁToolStatusDisplayǁ__init____mutmut_1, 
        'xǁToolStatusDisplayǁ__init____mutmut_2': xǁToolStatusDisplayǁ__init____mutmut_2, 
        'xǁToolStatusDisplayǁ__init____mutmut_3': xǁToolStatusDisplayǁ__init____mutmut_3, 
        'xǁToolStatusDisplayǁ__init____mutmut_4': xǁToolStatusDisplayǁ__init____mutmut_4, 
        'xǁToolStatusDisplayǁ__init____mutmut_5': xǁToolStatusDisplayǁ__init____mutmut_5, 
        'xǁToolStatusDisplayǁ__init____mutmut_6': xǁToolStatusDisplayǁ__init____mutmut_6, 
        'xǁToolStatusDisplayǁ__init____mutmut_7': xǁToolStatusDisplayǁ__init____mutmut_7, 
        'xǁToolStatusDisplayǁ__init____mutmut_8': xǁToolStatusDisplayǁ__init____mutmut_8, 
        'xǁToolStatusDisplayǁ__init____mutmut_9': xǁToolStatusDisplayǁ__init____mutmut_9, 
        'xǁToolStatusDisplayǁ__init____mutmut_10': xǁToolStatusDisplayǁ__init____mutmut_10, 
        'xǁToolStatusDisplayǁ__init____mutmut_11': xǁToolStatusDisplayǁ__init____mutmut_11, 
        'xǁToolStatusDisplayǁ__init____mutmut_12': xǁToolStatusDisplayǁ__init____mutmut_12, 
        'xǁToolStatusDisplayǁ__init____mutmut_13': xǁToolStatusDisplayǁ__init____mutmut_13, 
        'xǁToolStatusDisplayǁ__init____mutmut_14': xǁToolStatusDisplayǁ__init____mutmut_14, 
        'xǁToolStatusDisplayǁ__init____mutmut_15': xǁToolStatusDisplayǁ__init____mutmut_15, 
        'xǁToolStatusDisplayǁ__init____mutmut_16': xǁToolStatusDisplayǁ__init____mutmut_16, 
        'xǁToolStatusDisplayǁ__init____mutmut_17': xǁToolStatusDisplayǁ__init____mutmut_17, 
        'xǁToolStatusDisplayǁ__init____mutmut_18': xǁToolStatusDisplayǁ__init____mutmut_18, 
        'xǁToolStatusDisplayǁ__init____mutmut_19': xǁToolStatusDisplayǁ__init____mutmut_19, 
        'xǁToolStatusDisplayǁ__init____mutmut_20': xǁToolStatusDisplayǁ__init____mutmut_20
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolStatusDisplayǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁToolStatusDisplayǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁToolStatusDisplayǁ__init____mutmut_orig)
    xǁToolStatusDisplayǁ__init____mutmut_orig.__name__ = 'xǁToolStatusDisplayǁ__init__'
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_orig(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_1(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "XXXX") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_2(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_3(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = None
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_4(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = None
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_5(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(None, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_6(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, None)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_7(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_8(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, )
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_9(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = None
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_10(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id and tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_11(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = None
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_12(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=None)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_13(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = None
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_14(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = None
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_15(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = None
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_16(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = False
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_17(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(None, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_18(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, None, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_19(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, None, is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_20(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=None)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_21(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_22(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_23(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_24(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), )
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_25(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=False)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_26(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if self._is_output_captured():
                self._start_spinner_thread(key, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_27(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(None, description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_28(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, None)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_29(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(description)
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    def xǁToolStatusDisplayǁstart_tool_call__mutmut_30(self, tool_name: str, arguments: Dict[str, Any], tool_call_id: str = "") -> None:
        """
        Start displaying status for a tool call.
        
        Args:
            tool_name: Name of the tool being called
            arguments: Tool arguments
            tool_call_id: Optional unique ID for this tool call
        """
        if not self._enabled:
            return
        
        try:
            formatter = ToolDescriptionFormatter()
            description = formatter.format(tool_name, arguments)
            
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            with self._lock:
                spinner = Spinner(enabled=self._enabled)
                spinner.start()
                self._active_spinners[key] = spinner
                self._active_descriptions[key] = description
                self._spinner_running[key] = True
            
            # Print initial status line
            self._print_status(key, description, spinner.update(), is_initial=True)
            
            # Start background thread for continuous animation only if output is not captured
            # When output is captured, carriage returns don't work, so we skip animation
            # to avoid duplicate lines
            if not self._is_output_captured():
                self._start_spinner_thread(key, )
        
        except Exception:
            # Graceful degradation - don't crash if display fails
            pass
    
    xǁToolStatusDisplayǁstart_tool_call__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolStatusDisplayǁstart_tool_call__mutmut_1': xǁToolStatusDisplayǁstart_tool_call__mutmut_1, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_2': xǁToolStatusDisplayǁstart_tool_call__mutmut_2, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_3': xǁToolStatusDisplayǁstart_tool_call__mutmut_3, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_4': xǁToolStatusDisplayǁstart_tool_call__mutmut_4, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_5': xǁToolStatusDisplayǁstart_tool_call__mutmut_5, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_6': xǁToolStatusDisplayǁstart_tool_call__mutmut_6, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_7': xǁToolStatusDisplayǁstart_tool_call__mutmut_7, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_8': xǁToolStatusDisplayǁstart_tool_call__mutmut_8, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_9': xǁToolStatusDisplayǁstart_tool_call__mutmut_9, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_10': xǁToolStatusDisplayǁstart_tool_call__mutmut_10, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_11': xǁToolStatusDisplayǁstart_tool_call__mutmut_11, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_12': xǁToolStatusDisplayǁstart_tool_call__mutmut_12, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_13': xǁToolStatusDisplayǁstart_tool_call__mutmut_13, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_14': xǁToolStatusDisplayǁstart_tool_call__mutmut_14, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_15': xǁToolStatusDisplayǁstart_tool_call__mutmut_15, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_16': xǁToolStatusDisplayǁstart_tool_call__mutmut_16, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_17': xǁToolStatusDisplayǁstart_tool_call__mutmut_17, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_18': xǁToolStatusDisplayǁstart_tool_call__mutmut_18, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_19': xǁToolStatusDisplayǁstart_tool_call__mutmut_19, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_20': xǁToolStatusDisplayǁstart_tool_call__mutmut_20, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_21': xǁToolStatusDisplayǁstart_tool_call__mutmut_21, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_22': xǁToolStatusDisplayǁstart_tool_call__mutmut_22, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_23': xǁToolStatusDisplayǁstart_tool_call__mutmut_23, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_24': xǁToolStatusDisplayǁstart_tool_call__mutmut_24, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_25': xǁToolStatusDisplayǁstart_tool_call__mutmut_25, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_26': xǁToolStatusDisplayǁstart_tool_call__mutmut_26, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_27': xǁToolStatusDisplayǁstart_tool_call__mutmut_27, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_28': xǁToolStatusDisplayǁstart_tool_call__mutmut_28, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_29': xǁToolStatusDisplayǁstart_tool_call__mutmut_29, 
        'xǁToolStatusDisplayǁstart_tool_call__mutmut_30': xǁToolStatusDisplayǁstart_tool_call__mutmut_30
    }
    
    def start_tool_call(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolStatusDisplayǁstart_tool_call__mutmut_orig"), object.__getattribute__(self, "xǁToolStatusDisplayǁstart_tool_call__mutmut_mutants"), args, kwargs, self)
        return result 
    
    start_tool_call.__signature__ = _mutmut_signature(xǁToolStatusDisplayǁstart_tool_call__mutmut_orig)
    xǁToolStatusDisplayǁstart_tool_call__mutmut_orig.__name__ = 'xǁToolStatusDisplayǁstart_tool_call'
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_orig(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_1(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while False:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_2(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) and self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_3(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_4(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(None, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_5(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, None) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_6(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_7(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, ) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_8(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, True) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_9(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        return
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_10(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = None
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_11(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(None)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_12(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_13(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        return
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_14(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = None
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_15(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = None
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_16(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    return
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_17(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(None, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_18(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, None, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_19(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, None)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_20(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_21(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_22(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, )
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_23(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(None)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_24(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(1.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_25(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = None
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_26(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=None, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_27(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=None)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_28(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_29(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, )
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_30(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=False)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = thread
    
    def xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_31(self, key: str, description: str) -> None:
        """
        Start background thread to animate spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
        """
        def animate():
            """Background thread function to continuously update spinner."""
            while True:
                with self._lock:
                    if not self._spinner_running.get(key, False) or self._paused:
                        break
                    spinner = self._active_spinners.get(key)
                    if not spinner:
                        break
                    spinner_char = spinner.update()
                    is_paused = self._paused
                
                # Check paused flag outside lock to avoid blocking
                if is_paused:
                    break
                
                if spinner_char:
                    try:
                        self._print_status(key, description, spinner_char)
                    except (IOError, OSError):
                        # Handle write errors gracefully
                        pass
                
                time.sleep(0.1)  # Update every 100ms
        
        thread = threading.Thread(target=animate, daemon=True)
        thread.start()
        with self._lock:
            self._spinner_threads[key] = None
    
    xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_1': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_1, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_2': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_2, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_3': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_3, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_4': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_4, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_5': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_5, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_6': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_6, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_7': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_7, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_8': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_8, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_9': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_9, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_10': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_10, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_11': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_11, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_12': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_12, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_13': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_13, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_14': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_14, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_15': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_15, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_16': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_16, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_17': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_17, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_18': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_18, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_19': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_19, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_20': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_20, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_21': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_21, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_22': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_22, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_23': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_23, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_24': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_24, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_25': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_25, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_26': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_26, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_27': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_27, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_28': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_28, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_29': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_29, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_30': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_30, 
        'xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_31': xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_31
    }
    
    def _start_spinner_thread(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_orig"), object.__getattribute__(self, "xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _start_spinner_thread.__signature__ = _mutmut_signature(xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_orig)
    xǁToolStatusDisplayǁ_start_spinner_thread__mutmut_orig.__name__ = 'xǁToolStatusDisplayǁ_start_spinner_thread'
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_orig(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_1(self, tool_call_id: str = "XXXX", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_2(self, tool_call_id: str = "", tool_name: str = "XXXX", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_3(self, tool_call_id: str = "", tool_name: str = "", success: bool = False) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_4(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_5(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = None
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_6(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id and tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_7(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = None
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_8(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = True
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_9(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = None
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_10(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(None, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_11(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_12(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, )
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_13(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = None
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_14(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(None, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_15(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, None)
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_16(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop("")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_17(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, )
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_18(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "XXXX")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_19(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = None
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_20(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(None, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_21(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_22(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, )
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_23(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(None, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_24(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_25(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, )
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_26(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread or thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_27(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=None)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_28(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=1.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_29(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner or description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_30(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = None
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_31(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=None)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_32(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(None, description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_33(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, None, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_34(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, None, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_35(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, None)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_36(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(description, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_37(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, indicator, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_38(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, success)
        
        except Exception:
            # Graceful degradation
            pass
    
    def xǁToolStatusDisplayǁcomplete_tool_call__mutmut_39(self, tool_call_id: str = "", tool_name: str = "", success: bool = True) -> None:
        """
        Complete displaying status for a tool call.
        
        Args:
            tool_call_id: Optional unique ID for this tool call
            tool_name: Name of the tool (used as fallback key)
            success: Whether the tool call succeeded
        """
        if not self._enabled:
            return
        
        try:
            # Use tool_call_id if provided, otherwise use tool_name
            key = tool_call_id or tool_name
            
            # Stop spinner animation
            with self._lock:
                self._spinner_running[key] = False
                spinner = self._active_spinners.pop(key, None)
                description = self._active_descriptions.pop(key, "")
                thread = self._spinner_threads.pop(key, None)
                # Clean up initial status tracking
                self._initial_status_printed.pop(key, None)
            
            # Wait briefly for thread to finish current iteration
            if thread and thread.is_alive():
                thread.join(timeout=0.15)
            
            if spinner and description:
                # Get final indicator
                indicator = spinner.stop(success=success)
                # Clear the line and print final status on same line
                # Always print final status, even if output is captured
                self._print_final(key, description, indicator, )
        
        except Exception:
            # Graceful degradation
            pass
    
    xǁToolStatusDisplayǁcomplete_tool_call__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_1': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_1, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_2': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_2, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_3': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_3, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_4': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_4, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_5': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_5, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_6': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_6, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_7': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_7, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_8': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_8, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_9': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_9, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_10': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_10, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_11': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_11, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_12': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_12, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_13': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_13, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_14': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_14, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_15': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_15, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_16': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_16, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_17': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_17, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_18': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_18, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_19': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_19, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_20': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_20, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_21': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_21, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_22': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_22, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_23': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_23, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_24': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_24, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_25': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_25, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_26': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_26, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_27': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_27, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_28': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_28, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_29': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_29, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_30': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_30, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_31': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_31, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_32': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_32, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_33': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_33, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_34': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_34, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_35': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_35, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_36': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_36, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_37': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_37, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_38': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_38, 
        'xǁToolStatusDisplayǁcomplete_tool_call__mutmut_39': xǁToolStatusDisplayǁcomplete_tool_call__mutmut_39
    }
    
    def complete_tool_call(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolStatusDisplayǁcomplete_tool_call__mutmut_orig"), object.__getattribute__(self, "xǁToolStatusDisplayǁcomplete_tool_call__mutmut_mutants"), args, kwargs, self)
        return result 
    
    complete_tool_call.__signature__ = _mutmut_signature(xǁToolStatusDisplayǁcomplete_tool_call__mutmut_orig)
    xǁToolStatusDisplayǁcomplete_tool_call__mutmut_orig.__name__ = 'xǁToolStatusDisplayǁcomplete_tool_call'
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_orig(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_1(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = None
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_2(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = None
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_3(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = False
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_4(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(None):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_5(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = None
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_6(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = True
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_7(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = None
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_8(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(None)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_9(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(None)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_10(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread or thread.is_alive():
                thread.join(timeout=0.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_11(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=None)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    def xǁToolStatusDisplayǁpause_updates__mutmut_12(self) -> None:
        """
        Pause all spinner updates to prevent interference with user input.
        
        This should be called before user input to prevent spinner updates
        from overwriting what the user is typing. Waits for active threads
        to stop to ensure no updates occur while user is typing.
        
        Note: Does NOT write to stdout to avoid interfering with terminal
        line wrapping behavior during input().
        """
        threads_to_wait = []
        
        with self._lock:
            self._paused = True
            # Stop all active spinners
            for key in list(self._spinner_running.keys()):
                self._spinner_running[key] = False
                # Collect threads to wait for
                thread = self._spinner_threads.get(key)
                if thread:
                    threads_to_wait.append(thread)
        
        # Wait for threads to finish their current iteration (with timeout)
        # This ensures no spinner updates can occur while user is typing
        for thread in threads_to_wait:
            if thread and thread.is_alive():
                thread.join(timeout=1.15)  # Wait up to 150ms for thread to stop
        
        # Do NOT write anything to stdout here - it interferes with
        # terminal line wrapping when input() is called. The threads are
        # stopped, which is sufficient to prevent interference.
    
    xǁToolStatusDisplayǁpause_updates__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolStatusDisplayǁpause_updates__mutmut_1': xǁToolStatusDisplayǁpause_updates__mutmut_1, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_2': xǁToolStatusDisplayǁpause_updates__mutmut_2, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_3': xǁToolStatusDisplayǁpause_updates__mutmut_3, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_4': xǁToolStatusDisplayǁpause_updates__mutmut_4, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_5': xǁToolStatusDisplayǁpause_updates__mutmut_5, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_6': xǁToolStatusDisplayǁpause_updates__mutmut_6, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_7': xǁToolStatusDisplayǁpause_updates__mutmut_7, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_8': xǁToolStatusDisplayǁpause_updates__mutmut_8, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_9': xǁToolStatusDisplayǁpause_updates__mutmut_9, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_10': xǁToolStatusDisplayǁpause_updates__mutmut_10, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_11': xǁToolStatusDisplayǁpause_updates__mutmut_11, 
        'xǁToolStatusDisplayǁpause_updates__mutmut_12': xǁToolStatusDisplayǁpause_updates__mutmut_12
    }
    
    def pause_updates(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolStatusDisplayǁpause_updates__mutmut_orig"), object.__getattribute__(self, "xǁToolStatusDisplayǁpause_updates__mutmut_mutants"), args, kwargs, self)
        return result 
    
    pause_updates.__signature__ = _mutmut_signature(xǁToolStatusDisplayǁpause_updates__mutmut_orig)
    xǁToolStatusDisplayǁpause_updates__mutmut_orig.__name__ = 'xǁToolStatusDisplayǁpause_updates'
    
    def xǁToolStatusDisplayǁ_clear_current_line__mutmut_orig(self) -> None:
        """
        Clear the current line and ensure cursor is positioned correctly.
        
        This helper method ensures we're on a fresh line before resuming
        spinner updates to prevent display corruption.
        """
        try:
            # Write a newline to ensure we're on a fresh line
            # This prevents spinner updates from overwriting the input line
            sys.stdout.write('\n')
            sys.stdout.flush()
        except (IOError, OSError):
            # Handle write errors gracefully
            pass
    
    def xǁToolStatusDisplayǁ_clear_current_line__mutmut_1(self) -> None:
        """
        Clear the current line and ensure cursor is positioned correctly.
        
        This helper method ensures we're on a fresh line before resuming
        spinner updates to prevent display corruption.
        """
        try:
            # Write a newline to ensure we're on a fresh line
            # This prevents spinner updates from overwriting the input line
            sys.stdout.write(None)
            sys.stdout.flush()
        except (IOError, OSError):
            # Handle write errors gracefully
            pass
    
    def xǁToolStatusDisplayǁ_clear_current_line__mutmut_2(self) -> None:
        """
        Clear the current line and ensure cursor is positioned correctly.
        
        This helper method ensures we're on a fresh line before resuming
        spinner updates to prevent display corruption.
        """
        try:
            # Write a newline to ensure we're on a fresh line
            # This prevents spinner updates from overwriting the input line
            sys.stdout.write('XX\nXX')
            sys.stdout.flush()
        except (IOError, OSError):
            # Handle write errors gracefully
            pass
    
    xǁToolStatusDisplayǁ_clear_current_line__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolStatusDisplayǁ_clear_current_line__mutmut_1': xǁToolStatusDisplayǁ_clear_current_line__mutmut_1, 
        'xǁToolStatusDisplayǁ_clear_current_line__mutmut_2': xǁToolStatusDisplayǁ_clear_current_line__mutmut_2
    }
    
    def _clear_current_line(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolStatusDisplayǁ_clear_current_line__mutmut_orig"), object.__getattribute__(self, "xǁToolStatusDisplayǁ_clear_current_line__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _clear_current_line.__signature__ = _mutmut_signature(xǁToolStatusDisplayǁ_clear_current_line__mutmut_orig)
    xǁToolStatusDisplayǁ_clear_current_line__mutmut_orig.__name__ = 'xǁToolStatusDisplayǁ_clear_current_line'
    
    def xǁToolStatusDisplayǁresume_updates__mutmut_orig(self) -> None:
        """
        Resume spinner updates after user input.
        
        This should be called after user input is received to allow
        spinner updates to continue. Clears the current line first to
        prevent display corruption.
        """
        # Clear any partial spinner output and ensure we're on a new line
        # This prevents spinner updates from overwriting the user's input line
        self._clear_current_line()
        
        with self._lock:
            self._paused = False
    
    def xǁToolStatusDisplayǁresume_updates__mutmut_1(self) -> None:
        """
        Resume spinner updates after user input.
        
        This should be called after user input is received to allow
        spinner updates to continue. Clears the current line first to
        prevent display corruption.
        """
        # Clear any partial spinner output and ensure we're on a new line
        # This prevents spinner updates from overwriting the user's input line
        self._clear_current_line()
        
        with self._lock:
            self._paused = None
    
    def xǁToolStatusDisplayǁresume_updates__mutmut_2(self) -> None:
        """
        Resume spinner updates after user input.
        
        This should be called after user input is received to allow
        spinner updates to continue. Clears the current line first to
        prevent display corruption.
        """
        # Clear any partial spinner output and ensure we're on a new line
        # This prevents spinner updates from overwriting the user's input line
        self._clear_current_line()
        
        with self._lock:
            self._paused = True
    
    xǁToolStatusDisplayǁresume_updates__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolStatusDisplayǁresume_updates__mutmut_1': xǁToolStatusDisplayǁresume_updates__mutmut_1, 
        'xǁToolStatusDisplayǁresume_updates__mutmut_2': xǁToolStatusDisplayǁresume_updates__mutmut_2
    }
    
    def resume_updates(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolStatusDisplayǁresume_updates__mutmut_orig"), object.__getattribute__(self, "xǁToolStatusDisplayǁresume_updates__mutmut_mutants"), args, kwargs, self)
        return result 
    
    resume_updates.__signature__ = _mutmut_signature(xǁToolStatusDisplayǁresume_updates__mutmut_orig)
    xǁToolStatusDisplayǁresume_updates__mutmut_orig.__name__ = 'xǁToolStatusDisplayǁresume_updates'
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_orig(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_1(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_2(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') and not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_3(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_4(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(None, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_5(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, None) or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_6(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr('isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_7(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, ) or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_8(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'XXisattyXX') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_9(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'ISATTY') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_10(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_11(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = None
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_12(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = False
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_13(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return False
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_14(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = None
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_15(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'XXCURSOR_TERMINAL_CAPTUREXX',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_16(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'cursor_terminal_capture',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_17(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'XXVSCODE_TERMINAL_CAPTUREXX',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_18(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'vscode_terminal_capture',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_19(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'XXPYCHARM_TERMINAL_CAPTUREXX',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_20(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'pycharm_terminal_capture',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_21(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').upper() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_22(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(None, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_23(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, None).lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_24(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get('').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_25(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, ).lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_26(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, 'XXXX').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_27(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() not in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_28(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('XX1XX', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_29(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'XXtrueXX', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_30(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'TRUE', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_31(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'XXyesXX'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_32(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'YES'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_33(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = None
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_34(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = False
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_35(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return False
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_36(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = None
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_37(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get(None, '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_38(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', None)
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_39(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_40(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', )
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_41(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('XXTERMXX', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_42(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('term', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_43(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', 'XXXX')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_44(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term not in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_45(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('XXdumbXX', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_46(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('DUMB', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_47(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'XXunknownXX'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_48(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'UNKNOWN'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_49(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = None
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_50(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = False
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_51(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return False
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_52(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = None
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_53(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = True
        return False
    
    def xǁToolStatusDisplayǁ_is_output_captured__mutmut_54(self) -> bool:
        """
        Detect if stdout is being captured/redirected where carriage returns won't work.
        
        When output is captured (e.g., by IDE terminal capture), carriage return
        sequences don't work properly, causing each update to appear as a new line.
        
        Returns:
            True if output appears to be captured, False otherwise
        """
        # Cache the result to avoid repeated checks
        if self._output_captured is not None:
            return self._output_captured
        
        # If not a TTY, output is definitely captured/redirected
        if not hasattr(sys.stdout, 'isatty') or not sys.stdout.isatty():
            self._output_captured = True
            return True
        
        # Check for common indicators of output capture
        # Some IDEs set specific environment variables when capturing output
        env_vars_to_check = [
            'CURSOR_TERMINAL_CAPTURE',
            'VSCODE_TERMINAL_CAPTURE',
            'PYCHARM_TERMINAL_CAPTURE',
        ]
        for var in env_vars_to_check:
            if os.environ.get(var, '').lower() in ('1', 'true', 'yes'):
                self._output_captured = True
                return True
        
        # Check if TERM indicates a non-interactive terminal
        term = os.environ.get('TERM', '')
        if term in ('dumb', 'unknown'):
            self._output_captured = True
            return True
        
        # If we can't detect capture, assume it's not captured
        # This allows normal spinner behavior in interactive terminals
        self._output_captured = False
        return True
    
    xǁToolStatusDisplayǁ_is_output_captured__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolStatusDisplayǁ_is_output_captured__mutmut_1': xǁToolStatusDisplayǁ_is_output_captured__mutmut_1, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_2': xǁToolStatusDisplayǁ_is_output_captured__mutmut_2, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_3': xǁToolStatusDisplayǁ_is_output_captured__mutmut_3, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_4': xǁToolStatusDisplayǁ_is_output_captured__mutmut_4, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_5': xǁToolStatusDisplayǁ_is_output_captured__mutmut_5, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_6': xǁToolStatusDisplayǁ_is_output_captured__mutmut_6, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_7': xǁToolStatusDisplayǁ_is_output_captured__mutmut_7, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_8': xǁToolStatusDisplayǁ_is_output_captured__mutmut_8, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_9': xǁToolStatusDisplayǁ_is_output_captured__mutmut_9, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_10': xǁToolStatusDisplayǁ_is_output_captured__mutmut_10, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_11': xǁToolStatusDisplayǁ_is_output_captured__mutmut_11, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_12': xǁToolStatusDisplayǁ_is_output_captured__mutmut_12, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_13': xǁToolStatusDisplayǁ_is_output_captured__mutmut_13, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_14': xǁToolStatusDisplayǁ_is_output_captured__mutmut_14, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_15': xǁToolStatusDisplayǁ_is_output_captured__mutmut_15, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_16': xǁToolStatusDisplayǁ_is_output_captured__mutmut_16, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_17': xǁToolStatusDisplayǁ_is_output_captured__mutmut_17, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_18': xǁToolStatusDisplayǁ_is_output_captured__mutmut_18, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_19': xǁToolStatusDisplayǁ_is_output_captured__mutmut_19, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_20': xǁToolStatusDisplayǁ_is_output_captured__mutmut_20, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_21': xǁToolStatusDisplayǁ_is_output_captured__mutmut_21, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_22': xǁToolStatusDisplayǁ_is_output_captured__mutmut_22, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_23': xǁToolStatusDisplayǁ_is_output_captured__mutmut_23, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_24': xǁToolStatusDisplayǁ_is_output_captured__mutmut_24, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_25': xǁToolStatusDisplayǁ_is_output_captured__mutmut_25, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_26': xǁToolStatusDisplayǁ_is_output_captured__mutmut_26, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_27': xǁToolStatusDisplayǁ_is_output_captured__mutmut_27, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_28': xǁToolStatusDisplayǁ_is_output_captured__mutmut_28, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_29': xǁToolStatusDisplayǁ_is_output_captured__mutmut_29, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_30': xǁToolStatusDisplayǁ_is_output_captured__mutmut_30, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_31': xǁToolStatusDisplayǁ_is_output_captured__mutmut_31, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_32': xǁToolStatusDisplayǁ_is_output_captured__mutmut_32, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_33': xǁToolStatusDisplayǁ_is_output_captured__mutmut_33, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_34': xǁToolStatusDisplayǁ_is_output_captured__mutmut_34, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_35': xǁToolStatusDisplayǁ_is_output_captured__mutmut_35, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_36': xǁToolStatusDisplayǁ_is_output_captured__mutmut_36, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_37': xǁToolStatusDisplayǁ_is_output_captured__mutmut_37, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_38': xǁToolStatusDisplayǁ_is_output_captured__mutmut_38, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_39': xǁToolStatusDisplayǁ_is_output_captured__mutmut_39, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_40': xǁToolStatusDisplayǁ_is_output_captured__mutmut_40, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_41': xǁToolStatusDisplayǁ_is_output_captured__mutmut_41, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_42': xǁToolStatusDisplayǁ_is_output_captured__mutmut_42, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_43': xǁToolStatusDisplayǁ_is_output_captured__mutmut_43, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_44': xǁToolStatusDisplayǁ_is_output_captured__mutmut_44, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_45': xǁToolStatusDisplayǁ_is_output_captured__mutmut_45, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_46': xǁToolStatusDisplayǁ_is_output_captured__mutmut_46, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_47': xǁToolStatusDisplayǁ_is_output_captured__mutmut_47, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_48': xǁToolStatusDisplayǁ_is_output_captured__mutmut_48, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_49': xǁToolStatusDisplayǁ_is_output_captured__mutmut_49, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_50': xǁToolStatusDisplayǁ_is_output_captured__mutmut_50, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_51': xǁToolStatusDisplayǁ_is_output_captured__mutmut_51, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_52': xǁToolStatusDisplayǁ_is_output_captured__mutmut_52, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_53': xǁToolStatusDisplayǁ_is_output_captured__mutmut_53, 
        'xǁToolStatusDisplayǁ_is_output_captured__mutmut_54': xǁToolStatusDisplayǁ_is_output_captured__mutmut_54
    }
    
    def _is_output_captured(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolStatusDisplayǁ_is_output_captured__mutmut_orig"), object.__getattribute__(self, "xǁToolStatusDisplayǁ_is_output_captured__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _is_output_captured.__signature__ = _mutmut_signature(xǁToolStatusDisplayǁ_is_output_captured__mutmut_orig)
    xǁToolStatusDisplayǁ_is_output_captured__mutmut_orig.__name__ = 'xǁToolStatusDisplayǁ_is_output_captured'
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_orig(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_1(self, key: str, description: str, spinner_char: str, is_initial: bool = True) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_2(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_3(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = None
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_4(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') or sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_5(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(None, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_6(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, None) and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_7(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr('isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_8(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, ) and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_9(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'XXisattyXX') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_10(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'ISATTY') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_11(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = None
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_12(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = True
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_13(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_14(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() or not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_15(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_16(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_17(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(None, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_18(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, None):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_19(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_20(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, ):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_21(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, True):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_22(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = None
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_23(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = False
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_24(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = None
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_25(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = False
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_26(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = None
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_27(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split(None)
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_28(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('XX\nXX')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_29(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = None
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_30(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[1]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_31(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = None
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_32(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = None
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_33(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "XXBrocaOS> XX"
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_34(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "brocaos> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_35(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BROCAOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_36(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = None
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_37(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(None, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_38(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, None)
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_39(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize("brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_40(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, )
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_41(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "XXbrocaos_promptXX")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_42(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "BROCAOS_PROMPT")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_43(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = None
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_44(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(None)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_45(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = None
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_46(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = None  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_47(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 2  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_48(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = None
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_49(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width - space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_50(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width - spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_51(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = None
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_52(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(None, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_53(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, None)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_54(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_55(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, )
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_56(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(11, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_57(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width + 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_58(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width + reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_59(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 11)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_60(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = None
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_61(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(None)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_62(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) >= available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_63(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = None
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_64(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] - "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_65(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width + 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_66(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 4] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_67(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "XX...XX"
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_68(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = None
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_status__mutmut_69(self, key: str, description: str, spinner_char: str, is_initial: bool = False) -> None:
        """
        Print status line with spinner.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            spinner_char: Current spinner character
            is_initial: Whether this is the initial status print (not an animation update)
        """
        # Check if paused before writing
        with self._lock:
            if self._paused:
                return
        
        try:
            # Only print if spinner is enabled and we have a valid character
            if not spinner_char:
                return
            
            # Verify terminal is a TTY before using carriage return
            # Check hasattr first to handle cases where stdout might not have isatty
            # Use try/except to handle cases where isatty might raise or return unexpected values
            try:
                is_tty = hasattr(sys.stdout, 'isatty') and sys.stdout.isatty()
            except (AttributeError, TypeError):
                is_tty = False
            if not is_tty:
                return
            
            # If output is captured and this is an intermediate animation update,
            # skip it to avoid duplicate lines. Always allow initial and final status.
            if self._is_output_captured() and not is_initial:
                # Check if this is the initial status for this key
                with self._lock:
                    if not self._initial_status_printed.get(key, False):
                        # This is actually the initial status, mark it and allow it
                        self._initial_status_printed[key] = True
                    else:
                        # This is an intermediate animation update, skip it
                        return
            else:
                # Output not captured, or this is initial status - mark as printed
                with self._lock:
                    self._initial_status_printed[key] = True
            
            # Handle multi-line descriptions - take only first line
            # This prevents wrapping issues with long descriptions (especially heredocs)
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            # Account for prompt width, spinner char, and space
            prompt_width = _get_visible_width(prompt)
            spinner_width = len(spinner_char)
            space_width = 1  # Space between spinner and description
            reserved_width = prompt_width + spinner_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # Use ANSI escape code to clear the line, then carriage return
            # \033[K clears from cursor to end of line
            # \r moves cursor to beginning of line
            # This ensures we overwrite any previous content, even if it was longer
            line = f"\r\033[K{prompt}{spinner_char} {description}"
            sys.stdout.write(None)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
 
        'xǁToolStatusDisplayǁ_print_status__mutmut_49': xǁToolStatusDisplayǁ_print_status__mutmut_49, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_50': xǁToolStatusDisplayǁ_print_status__mutmut_50, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_51': xǁToolStatusDisplayǁ_print_status__mutmut_51, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_52': xǁToolStatusDisplayǁ_print_status__mutmut_52, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_53': xǁToolStatusDisplayǁ_print_status__mutmut_53, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_54': xǁToolStatusDisplayǁ_print_status__mutmut_54, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_55': xǁToolStatusDisplayǁ_print_status__mutmut_55, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_56': xǁToolStatusDisplayǁ_print_status__mutmut_56, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_57': xǁToolStatusDisplayǁ_print_status__mutmut_57, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_58': xǁToolStatusDisplayǁ_print_status__mutmut_58, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_59': xǁToolStatusDisplayǁ_print_status__mutmut_59, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_60': xǁToolStatusDisplayǁ_print_status__mutmut_60, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_61': xǁToolStatusDisplayǁ_print_status__mutmut_61, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_62': xǁToolStatusDisplayǁ_print_status__mutmut_62, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_63': xǁToolStatusDisplayǁ_print_status__mutmut_63, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_64': xǁToolStatusDisplayǁ_print_status__mutmut_64, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_65': xǁToolStatusDisplayǁ_print_status__mutmut_65, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_66': xǁToolStatusDisplayǁ_print_status__mutmut_66, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_67': xǁToolStatusDisplayǁ_print_status__mutmut_67, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_68': xǁToolStatusDisplayǁ_print_status__mutmut_68, 
        'xǁToolStatusDisplayǁ_print_status__mutmut_69': xǁToolStatusDisplayǁ_print_status__mutmut_69
    }
    
    def _print_status(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁToolStatusDisplayǁ_print_status__mutmut_orig"), object.__getattribute__(self, "xǁToolStatusDisplayǁ_print_status__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _print_status.__signature__ = _mutmut_signature(xǁToolStatusDisplayǁ_print_status__mutmut_orig)
    xǁToolStatusDisplayǁ_print_status__mutmut_orig.__name__ = 'xǁToolStatusDisplayǁ_print_status'
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_orig(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_1(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_2(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = None
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_3(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() or not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_4(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_5(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_6(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = None
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_7(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split(None)
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_8(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('XX\nXX')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_9(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = None
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_10(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[1]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_11(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = None
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_12(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(None, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_13(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, None)
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_14(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize("success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_15(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, )
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_16(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "XXsuccess_indicatorXX")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_17(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "SUCCESS_INDICATOR")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_18(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = None
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_19(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(None, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_20(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, None)
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_21(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize("error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_22(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, )
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_23(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "XXerror_indicatorXX")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_24(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "ERROR_INDICATOR")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_25(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = None
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_26(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = None
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_27(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "XXBrocaOS> XX"
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_28(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "brocaos> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_29(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BROCAOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_30(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = None
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_31(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(None, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_32(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, None)
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_33(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize("brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_34(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, )
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_35(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "XXbrocaos_promptXX")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_36(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "BROCAOS_PROMPT")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_37(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = None
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_38(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(None)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_39(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = None
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_40(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(None)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_41(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = None  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_42(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 2  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_43(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = None
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_44(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width - space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_45(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width - indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_46(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = None
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_47(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(None, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_48(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, None)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_49(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_50(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, )
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_51(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(11, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_52(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width + 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_53(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width + reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_54(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 11)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_55(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = None
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_56(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(None)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_57(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) >= available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_58(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = None
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_59(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] - "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_60(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width + 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_61(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 4] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_62(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "XX...XX"
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_63(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = None
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_64(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = None
            sys.stdout.write(line)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    def xǁToolStatusDisplayǁ_print_final__mutmut_65(self, key: str, description: str, indicator: str, success: bool) -> None:
        """
        Print final status line with completion indicator.
        
        Always prints final status, even if output is captured, so users
        can see completion status.
        
        Args:
            key: Unique key for this tool call
            description: Description text
            indicator: Final indicator (checkmark or cross)
            success: Whether operation succeeded
        """
        try:
            # Only print if enabled
            if not self._enabled:
                return
            
            # If output is captured, print without carriage return (just newline)
            # This ensures it appears as a single line even when captured
            output_captured = self._is_output_captured()
            
            # If not a TTY and we haven't detected it as captured (truly redirected),
            # don't print final status
            if not sys.stdout.isatty() and not output_captured:
                return
            
            # Handle multi-line descriptions - take only first line
            description_lines = description.split('\n')
            description = description_lines[0]
            
            # Colorize the indicator
            if self._color_manager:
                if success:
                    indicator = self._color_manager.colorize(indicator, "success_indicator")
                else:
                    indicator = self._color_manager.colorize(indicator, "error_indicator")
            
            # Get terminal width and truncate description if needed
            terminal_width = _get_terminal_width()
            prompt = "BrocaOS> "
            if self._color_manager:
                prompt = self._color_manager.colorize(prompt, "brocaos_prompt")
            
            # Calculate available width for description
            prompt_width = _get_visible_width(prompt)
            indicator_width = _get_visible_width(indicator)
            space_width = 1  # Space between indicator and description
            reserved_width = prompt_width + indicator_width + space_width
            
            # Reserve some margin (at least 10 chars) to prevent edge cases
            available_width = max(10, terminal_width - reserved_width - 10)
            
            # Truncate description if it's too long
            visible_desc = _strip_ansi_codes(description)
            if len(visible_desc) > available_width:
                description = description[:available_width - 3] + "..."
            
            # If output is captured, don't use carriage return - just print with newline
            # This prevents duplicate lines when output is captured
            if output_captured:
                # Just print the final status on a new line
                line = f"{prompt}{indicator} {description}\n"
            else:
                # Use ANSI escape code to clear the line, then carriage return, then newline
                # \033[K clears from cursor to end of line
                # \r moves cursor to beginning of line
                # This ensures we overwrite any previous content, even if it was longer
                line = f"\r\033[K{prompt}{indicator} {description}\n"
            sys.stdout.write(None)
            sys.stdout.flush()  # Ensure output is immediately visible
        except (IOError, OSError, AttributeError):
            # Handle write errors gracefully (including when stdout doesn't have isatty)
            pass
    
    xǁToolStatusDisplayǁ_print_final__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁToolStatusDisplayǁ_print_final__mutmut_1': xǁToolStatusDisplayǁ_print_final__mutmut_1, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_2': xǁToolStatusDisplayǁ_print_final__mutmut_2, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_3': xǁToolStatusDisplayǁ_print_final__mutmut_3, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_4': xǁToolStatusDisplayǁ_print_final__mutmut_4, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_5': xǁToolStatusDisplayǁ_print_final__mutmut_5, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_6': xǁToolStatusDisplayǁ_print_final__mutmut_6, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_7': xǁToolStatusDisplayǁ_print_final__mutmut_7, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_8': xǁToolStatusDisplayǁ_print_final__mutmut_8, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_9': xǁToolStatusDisplayǁ_print_final__mutmut_9, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_10': xǁToolStatusDisplayǁ_print_final__mutmut_10, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_11': xǁToolStatusDisplayǁ_print_final__mutmut_11, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_12': xǁToolStatusDisplayǁ_print_final__mutmut_12, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_13': xǁToolStatusDisplayǁ_print_final__mutmut_13, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_14': xǁToolStatusDisplayǁ_print_final__mutmut_14, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_15': xǁToolStatusDisplayǁ_print_final__mutmut_15, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_16': xǁToolStatusDisplayǁ_print_final__mutmut_16, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_17': xǁToolStatusDisplayǁ_print_final__mutmut_17, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_18': xǁToolStatusDisplayǁ_print_final__mutmut_18, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_19': xǁToolStatusDisplayǁ_print_final__mutmut_19, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_20': xǁToolStatusDisplayǁ_print_final__mutmut_20, 
        'xǁToolStatusDisplayǁ_print_final__mutmut_21': xǁToolStatusDisplayǁ_print_final__mutmut_21, 
        'xǁToolStatusDisplayǁ_print_final__mutmut